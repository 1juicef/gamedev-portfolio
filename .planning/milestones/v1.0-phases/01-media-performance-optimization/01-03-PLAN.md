---
phase: 01-media-performance-optimization
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/data/GameProjectsData.ts
  - src/App.vue
autonomous: true
requirements: [MEDIA-02, MEDIA-03]

must_haves:
  truths:
    - "Every screenshot <img> across all four projects' htmlDescription loads a .webp file with loading=\"lazy\" placed before src."
    - "Floor 0's overlay shows the real Floor0SC1-4.webp screenshots — the previously-broken flat-directory references are removed, so no broken-image icons appear (D-14; satisfies CONT-01 early)."
    - "Dispater's overlay contains a plain, non-autoplay YouTube trailer embed using video id ihPEcIQ_PwI with loading=\"lazy\" (D-09)."
    - "Drag Rush's existing overlay trailer iframe also carries loading=\"lazy\"."
    - "The SwingSpace overlay video uses preload=metadata so its multi-MB clip is not fully downloaded on overlay open."
    - "src/App.vue no longer eager-preloads the stale placeholder-icon paths, and no large source GIF/PNG or .mp4 is added to any preload/eager path (keeping initial site weight low)."
    - "Floor 0's overlay renders with zero broken-image references (edge: MEDIA-03 empty)."
    - "loading=\"lazy\" precedes src in every screenshot <img> tag, per the Firefox attribute-order bug #1647077 (edge: MEDIA-03 ordering)."
  prohibitions: []
  artifacts:
    - src/data/GameProjectsData.ts (16 lazy WebP screenshot refs, Floor 0 reference fix, Dispater trailer embed)
    - src/App.vue (pruned preload list)
  key_links:
    - "GameProjectsData.ts <img> src -> .webp files produced by plan 01-01"
    - "Dispater htmlDescription -> YouTube embed id ihPEcIQ_PwI (D-09)"
---

<objective>
Vertical slice B — the fast overlays. Repoint every project screenshot in `GameProjectsData.ts` to its compressed WebP with lazy loading, fix Floor 0's broken screenshot references, add Dispater's missing trailer embed, defer the SwingSpace overlay video, and prune the stale eager-preload list in `App.vue`.

Purpose: After this plan, opening any project's detail overlay loads crisp WebP screenshots lazily instead of multi-MB PNGs, and the app no longer eager-fetches dead placeholder icons on boot. This completes MEDIA-02 and the image half of MEDIA-03.
Output: rewired `src/data/GameProjectsData.ts` and a pruned `src/App.vue`.
</objective>

<phase_goal>
**As a** recruiter or hiring lead scanning Josef's portfolio, **I want to** have the project timeline and overlays load fast and lean — video thumbnails and compressed, lazy-loaded images instead of multi-megabyte GIFs and PNGs — **so that** the site feels polished and professional within a 10-second scan without heavy downloads.

This plan delivers the overlay half of that story: lazy, compressed WebP screenshots and a lean preload path.
</phase_goal>

<artifacts_this_phase_produces>
Symbols and files changed by this plan (no new files):
- `src/data/GameProjectsData.ts` — all 16 screenshot `<img>` tags rewritten to `loading="lazy"` + `.webp`; Floor 0's four screenshot references repointed to `img/projects/floor-0/Floor0SC1-4.webp`; a new Dispater trailer `<iframe>` (video id `ihPEcIQ_PwI`); `loading="lazy"` added to Drag Rush's existing trailer iframe; `preload="metadata"` on the SwingSpace overlay video.
- `src/App.vue` — `Helpers.preloadImages([...])` stale-path list removed (and the now-unused `Helpers` import), keeping zero eager preloads.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-media-performance-optimization/01-CONTEXT.md
@.planning/phases/01-media-performance-optimization/01-RESEARCH.md
@.planning/phases/01-media-performance-optimization/01-PATTERNS.md
@src/data/GameProjectsData.ts
@src/App.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap all screenshots to lazy WebP, fix Floor 0, add Dispater trailer</name>
  <files>src/data/GameProjectsData.ts</files>
  <read_first>
    - src/data/GameProjectsData.ts (the file being rewritten — all four htmlDescription strings, current img/iframe/video markup and the Floor 0 broken references)
    - .planning/phases/01-media-performance-optimization/01-RESEARCH.md (Pattern 2 + Pitfall 3: loading="lazy" attribute order; Pitfall 4: Floor 0 reference-swap requirement)
    - .planning/phases/01-media-performance-optimization/01-PATTERNS.md (existing screenshot/trailer markup patterns, Floor 0 fix, per-project source assets)
    - .planning/phases/01-media-performance-optimization/01-CONTEXT.md (D-09, D-12, D-13, D-14)
  </read_first>
  <action>
In `src/data/GameProjectsData.ts`, transform every screenshot `<img>` inside all four projects' `htmlDescription` strings so that `loading="lazy"` appears immediately after the tag's class attribute and BEFORE `src`, and the file extension changes `.png` -> `.webp`. Attribute order matters: `loading` must precede `src` (Firefox bug #1647077 / RESEARCH Pattern 2 / Pitfall 3) — writing `src` first can defeat lazy-loading. This applies to Drag Rush (DragrushSC1-5), Dispater (DispaterSC1-5), Floor 0 (four shots), and SwingSpace (SwingSpaceSC1-2, class `pc-screenshot swing-space-shot`).

Fix Floor 0 (D-14 / Pitfall 4): its four screenshot references currently point at old flat-directory paths that do not exist on disk. Repoint all four to the real reorganized, converted files: `img/projects/floor-0/Floor0SC1.webp` through `Floor0SC4.webp`, each with `loading="lazy"` before `src`. This is required for Success Criterion #2 to be literally true for Floor 0 and satisfies CONT-01 early — flag it in the SUMMARY for the traceability update.

Add Dispater's trailer embed (D-09): insert a `<div class="paragraph center">` containing `<iframe class="youtube" loading="lazy" src="https://www.youtube.com/embed/ihPEcIQ_PwI" frameborder="0" allowfullscreen></iframe>` after Dispater's intro paragraph and before its screenshots — mirroring the position and exact attribute set of Drag Rush's existing trailer block. Use the plain, non-autoplay embed (no autoplay param) so the visitor must click play. Also add `loading="lazy"` to Drag Rush's existing trailer iframe.

Defer the SwingSpace overlay video: add `preload="metadata"` to the existing `<video class="pc-video swing-space-video" controls>` so its multi-MB `SwingSpaceVid.mp4` is not fully fetched on overlay open (MEDIA-03 spirit). Leave the SwingSpace overlay `<video>` element otherwise intact and leave each project's `iconUrl` constructor argument untouched (unused by the timeline after 01-02; kept per D-03).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/data/GameProjectsData.ts','utf8');const noComment=s.split('\n').filter(l=>!/^\s*<!--/.test(l)).join('\n');const lazyImg=(s.match(/loading=\"lazy\" src=\"img\/projects/g)||[]).length;const brokenFloor=(noComment.match(/projects\/floor-0-/g)||[]).length;const disp=(s.match(/ihPEcIQ_PwI/g)||[]).length;const floorFix=(s.match(/floor-0\/Floor0SC1.webp/g)||[]).length;console.log('lazyImg',lazyImg,'broken',brokenFloor,'disp',disp,'floorFix',floorFix);process.exit(lazyImg>=16&&brokenFloor===0&&disp>=1&&floorFix===1?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - All sixteen screenshots are lazy WebP with correct attribute order: the automated check reports `lazyImg` >= 16 (each `<img>` has `loading="lazy"` before `src="img/projects...webp"`).
    - Floor 0's broken references are gone and repointed: the check reports `broken 0` and `floorFix 1` (`img/projects/floor-0/Floor0SC1.webp` present).
    - Dispater's trailer embed exists: the check reports `disp` >= 1 (`ihPEcIQ_PwI` present in the file).
    - `grep -c 'preload="metadata"' src/data/GameProjectsData.ts` returns 1 (SwingSpace overlay video deferred).
    - `npm run lint` passes.
  </acceptance_criteria>
  <done>Every overlay screenshot loads lazily as WebP with loading before src; Floor 0 shows real WebP screenshots; Dispater has a lazy non-autoplay trailer; SwingSpace's overlay video is deferred.</done>
</task>

<task type="auto">
  <name>Task 2: Prune the stale eager-preload list in App.vue</name>
  <files>src/App.vue</files>
  <read_first>
    - src/App.vue (the Helpers.preloadImages call site, lines 20 and 30-34)
    - src/helpers.ts (confirm what preloadImages does — App.vue is the only consumer)
    - .planning/phases/01-media-performance-optimization/01-PATTERNS.md (App.vue cleanup note)
  </read_first>
  <action>
`src/App.vue` currently eager-preloads three stale placeholder-icon paths (the project-1/2/3 icon PNGs) that are dead weight after the D-06/D-07 asset reorg and the video-thumbnail conversion — nothing renders them anymore. Grep confirms `App.vue` is the ONLY consumer of `Helpers`/`preloadImages` in `src/`. Remove the entire `Helpers.preloadImages([...])` call and the now-unused `import Helpers from './helpers';` line to avoid an unused-import lint error. Leave `src/helpers.ts` in place (do not delete it — D-03 spirit). Do NOT replace the removed preloads with new eager preloads of the posters, MP4s, or source masters — keeping zero eager preloads is the point (MEDIA-03: keep initial site weight low; the posters are already lazily loaded by LazyVideoThumbnail).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - No stale preload remains: `grep -c 'project-1-icon' src/App.vue` returns 0 and `grep -c 'preloadImages' src/App.vue` returns 0.
    - No unused import remains: `grep -c "import Helpers" src/App.vue` returns 0.
    - `npm run build` completes successfully (production build passes with the pruned App.vue).
  </acceptance_criteria>
  <done>App.vue no longer eager-preloads dead placeholder icons and no unused Helpers import remains; the production build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer-authored htmlDescription -> v-html render | Pre-existing boundary; this plan edits the trusted string content and adds `<img>`/`<iframe>` markup, but does not change the rendering mechanism or introduce user input. |
| overlay -> youtube.com iframe embed | Third-party frame loaded when an overlay opens (Drag Rush existing + Dispater new). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering (XSS via v-html) | GameProjectsData.ts htmlDescription strings | low | accept | `htmlDescription` values are developer-authored constants, never derived from user input or an external API — the new `<img>`/`<iframe>` markup adds no injection surface (RESEARCH V5: marginal, no new surface). |
| T-01-05 | Information disclosure (third-party) | Dispater/Drag Rush YouTube trailer iframes | low | accept | Plain YouTube embed is the pre-existing project pattern (D-09); the trailer loads only on overlay open and is deferred via `loading="lazy"`. No new decision to switch embed provider is in scope. |
| T-01-SC | Tampering (supply chain) | build-time dependencies | low | transfer | No package installs in this plan; dependency legitimacy is owned by plan 01-01. |
</threat_model>

<verification>
- `npm run lint` and `npm run build` both pass.
- All 16 screenshots are lazy WebP with loading before src; Floor 0 references are fixed; Dispater trailer present; SwingSpace overlay video deferred.
- App.vue has no stale preloads and no unused imports.
</verification>

<success_criteria>
Opening any project overlay loads crisp, compressed WebP screenshots lazily; Floor 0 shows real screenshots with no broken images; Dispater has its trailer; and the app boots without eager-fetching dead placeholder assets.
</success_criteria>

<output>
Create `.planning/phases/01-media-performance-optimization/01-03-SUMMARY.md` when done.
</output>
