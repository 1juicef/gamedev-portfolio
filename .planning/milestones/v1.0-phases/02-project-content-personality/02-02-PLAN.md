---
phase: 02-project-content-personality
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: []
autonomous: false
requirements: [POLISH-02]
must_haves:
  truths:
    - "A full top-to-bottom read-through confirms the mascot stays confined to header/footer only (Guy.gif / Guy2.gif) and does not leak into the timeline or overlays — POLISH-02"
    - "The hero tone reads warm-but-short and professional, not as a 'quirky hobby project' — POLISH-02 / ROADMAP SC5"
    - "Josef signs off that personality (mascot + tone) stays within the intended professional boundary before shipping"
  artifacts:
    - "None — this is a verification/sign-off checkpoint. It produces a recorded human approval in the plan summary, not a new file or symbol."
  key_links:
    - "The read-through spans the hero (src/views/GameProjects.vue) plus all 4 project overlays whose content was finalized in 02-01 (src/data/GameProjectsData.ts) — so this plan must run after 02-01"
    - "Mascot boundary is defined by src/components/Header.vue (Guy.gif) and src/components/Footer.vue (Guy2.gif) — the read-through confirms it appears nowhere else"
---

## Phase Goal

**As a** recruiter or technical lead browsing the portfolio, **I want to** see accurate, warm, technically substantive project information without ever seeing code, **so that** personality comes through within a deliberate, professional boundary.

<objective>
Verify the personality boundary (POLISH-02) with a full top-to-bottom read-through of the running site, after the content from 02-01 has landed. This is the ROADMAP Success Criterion 5 checkpoint: confirm the mascot stays in header/footer only and the hero tone reads warm-but-short and professional — not "quirky hobby project" — then get Josef's explicit sign-off. Per D (personality boundary "not discussed this session"), the boundary is decided here at UAT with Josef, not pre-decided.

Purpose: Personality is the one thing in this phase that can only be judged by reading the whole page as a recruiter would — this checkpoint is that judgment.
Output: A recorded human approval (or a list of specific boundary issues to fix). No code changes and no new files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-project-content-personality/02-CONTEXT.md
@src/views/GameProjects.vue
@src/components/Header.vue
@src/components/Footer.vue
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Full-page personality boundary read-through with Josef</name>
  <read_first>
    - .planning/phases/02-project-content-personality/02-CONTEXT.md (personality-boundary decision + POLISH-02 scope: mascot confined to header/footer, hero warm-but-short)
    - src/views/GameProjects.vue (hero copy + timeline layout the recruiter scans first)
    - src/components/Header.vue and src/components/Footer.vue (mascot placement — the running-character Guy.gif / Guy2.gif that defines the boundary)
  </read_first>
  <action>
    Pause for a blocking human-verify checkpoint (no code changes). Run the dev server, then walk Josef through the full top-to-bottom read-through described in `<how-to-verify>` below — hero tone, all 4 project overlays/blurbs, mascot confinement to header/footer, and the no-code guarantee. Record the outcome (explicit approval, or a specific punch-list of boundary/tone/copy issues) in `02-02-SUMMARY.md`. Do not proceed past this task until Josef signals approval; if issues are raised, they are handled as follow-up edits to the 02-01 files or via gap closure — POLISH-02 per ROADMAP Success Criterion 5.
  </action>
  <what-built>
    Content finalized in 02-01: Drag Rush/Dispater attribution lines, all 4 finalized "About this game" blurbs (typos fixed), Floor 0 itch.io link, and the confirmed warm hero copy. The mascot (running character) is present only in the header (`Guy.gif`) and footer (`Guy2.gif`).
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run serve` (if it fails with an OpenSSL error, set `NODE_OPTIONS=--openssl-legacy-provider` first). Open the site at the printed localhost URL — it lands on `/game-projects`.
    2. Sanity pre-check (optional, fast): confirm 02-01 content is live — `grep -Fc 'Play on itch.io' src/data/GameProjectsData.ts` returns 3, and `grep -Fq 'A team of 6' src/data/GameProjectsData.ts && grep -Fq 'A team of 7' src/data/GameProjectsData.ts && echo OK`.
    3. Read the page top to bottom as a recruiter would: the hero ("Hello there!" + bio) should feel warm and personal but land as short and professional — not a "quirky hobby project" impression.
    4. Scroll the full project timeline and open each of the 4 project overlays (Drag Rush, Dispater, Floor 0, SwingSpace). Read each "About this game" blurb — confirm attribution reads naturally, blurbs are typo-free, and Floor 0's "Play on itch.io" link is present and points to the itch.io page.
    5. Confirm the mascot (running character) appears ONLY in the header and footer — it must not appear inside the timeline rows or the project overlays.
    6. Confirm no code, code snippets, or engineering artifacts are shown anywhere on the page.
  </how-to-verify>
  <resume-signal>Type "approved" if personality stays within the boundary and the read-through passes, or describe the specific boundary/tone/copy issues to fix.</resume-signal>
  <done>Josef has completed the full-page read-through and either approved (personality within boundary) or produced a specific fix list; the outcome is recorded in 02-02-SUMMARY.md.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

None. This plan is a human-verification checkpoint. It creates no files, components, symbols, or CSS. Its output is a recorded approval (or a punch-list of boundary fixes) captured in `02-02-SUMMARY.md`. If Josef flags issues, they are handled as follow-up edits to the same two files touched in 02-01 (or via gap closure), not by new artifacts here.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan changes no code and introduces no new inputs, endpoints, links, or dependencies — it is a read-through and sign-off only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Information (impression risk, not a security threat) | Full-page personality read-through | low | accept | The checkpoint itself adds no attack surface. Its purpose is to catch tone/boundary regressions before ship; any issues found are fixed as content edits under the existing T-02-01/T-02-02 dispositions. |

**Supply chain:** No package-manager installs occur in this phase — the supply-chain checkpoint (T-{phase}-SC) is not applicable.
</threat_model>

<verification>
- The blocking human-verify checkpoint returns an explicit "approved" (or a documented fix list) from Josef.
- The optional grep pre-checks in step 2 confirm 02-01 content is live before the read-through.
</verification>

<success_criteria>
- POLISH-02: A full top-to-bottom read-through confirms personality (mascot confined to header/footer, hero warm-but-short) stays within its intended professional boundary, signed off by Josef.
</success_criteria>

<output>
Create `.planning/phases/02-project-content-personality/02-02-SUMMARY.md` when done (record the approval or the fix list).
</output>
