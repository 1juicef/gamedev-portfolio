---
phase: 3
slug: visual-polish-resume-site-metadata
status: approved
shadcn_initialized: false
preset: none
created: 2026-07-22
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — Vue 2 + `vue-cli-service` (webpack) project, not React/Next/Vite; no `components.json`, no Tailwind. shadcn gate not applicable to this stack. |
| Preset | not applicable |
| Component library | none — hand-rolled Vue SFCs (`GameProjects.vue`, `ProjectDetailsOverlay.vue`, `Resume.vue`) |
| Icon library | Font Awesome 4.7.0 (CDN, `public/index.html`), used sparingly (`fa-times` on the overlay close button, `fa-envelope`/etc. on Contact). No new icons introduced this phase — see Copywriting/affordance decision below. |
| Font | `Russo One` (headings, `.project-title-link`, nav) / `Lekton` (body) — both self-hosted via `@font-face` in `App.vue`, unchanged this phase |

---

## Spacing Scale

Declared values (must be multiples of 4), applied to the touched files this phase (`GameProjects.vue` timeline):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon/inline gaps (unchanged, none new) |
| sm | 8px | Compact element spacing (unchanged) |
| md | 16px | `.project-copy` title→summary gap (was 14px — snap to scale) |
| lg | 24px | `.project-row` gap, mobile (unchanged — already on-scale) |
| xl | 32px | `.project-row` padding, mobile (was 28px — snap to scale, tightens the divider rhythm) |
| 2xl | 48px | `.project-row` padding + gap, desktop; `.project-timeline` gap, desktop (was 40px gap — snap up to scale for more breathing room between rows) |
| 3xl | 64px | not used this phase |

Exceptions: none. This scale applies only to the timeline (`GameProjects.vue`). `Resume.vue` and `public/index.html` have no spacing changes in scope (D-06 confirms Resume.vue is already correct; META-01 is `<head>`-only).

---

## Typography

Values expressed in `em` to match this codebase's existing convention (no `px` sizing is used anywhere in the current stylesheets — converting to px would be an unrequested drive-by change).

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body (`.project-summary`) | 1.08em (~17px) | 400 | 1.7 (was 1.95em — tighten for a more restrained/premium block of text) |
| Label | not applicable this phase (no eyebrow/meta-label text exists in the timeline; nav-bar link styling is Header.vue, out of scope) | — | — |
| Heading (`.project-title-link`) | 2.1em (was 2.35em — tighten for restraint) | 100 | 1.1 (was 1.05em) |
| Display (hero `h1`, global in `App.vue`) | 2.5em | 100 | 1.1em | reference only — already finalized in Phase 2 (CONT-03), not touched this phase |

Exactly 2 weights across the touched scope: **100** (thin — hero, project titles) and **400** (regular — body/summary copy). No new weights introduced.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#000000` → `#120818` → `#2b123f` (existing page gradient, `App.vue`) | Full-page background — unchanged |
| Secondary (30%) | `rgba(255, 255, 255, 0.14)` divider + same gradient family in the overlay dialog | `.project-row` top border (existing divider pattern — flat design, no card surfaces to introduce) |
| Accent (10%) | `#f4cde6` (existing one-off hover color, already used nowhere else) | **Reserved for:** `.project-title-link` hover/focus state AND the new persistent click-affordance underline (D-03) — nothing else. Do not introduce `@accentColor` (`#6C3BAA`, defined in `variables.less` but currently visually inert/unused) into this scope; that's a separate, out-of-scope color-system cleanup. |
| Destructive | not applicable | No destructive actions in this phase (no forms, no delete/remove UI) |

**D-03 resolution (Claude's discretion, lazy/native choice):** implement the persistent title-click affordance as a subtle always-on underline (`border-bottom: 1px solid rgba(255,255,255,0.35)`) on `.project-title-link`, strengthening to the existing `#f4cde6` accent color on `:hover`/`:focus-visible`. This reuses the color and transition already defined on the element — no new icon, no new dependency, no new color token. `→ skipped: chevron/icon affordance (Font Awesome is already loaded but unused here), add only if Josef specifically asks for an icon cue after seeing the underline.`

---

## Copywriting Contract

This phase does not introduce new interactive product copy (no new CTAs, lists, or forms) — POLISH-01 is a spacing/typography/affordance pass, RESUME-01 is a verification of already-implemented static markup, and META-01 is `<head>`-only metadata. Rows below reflect that scope honestly rather than inventing copy that isn't needed.

| Element | Copy |
|---------|------|
| Primary CTA | not applicable — no new CTA this phase. Existing "Play on itch.io" links (Phase 2) and title-click affordance (D-03, an underline, not new label text) are unchanged/style-only. |
| Empty state heading | not applicable — no list/collection with an empty variant touched this phase (timeline is a fixed, always-populated array of 4 projects) |
| Empty state body | not applicable |
| Error state | not applicable — no forms or async data fetching touched this phase |
| Destructive confirmation | not applicable — no destructive actions |

**Site metadata copy (META-01, D-07/D-08/D-09) — the actual copywriting deliverable of this phase:**

| Tag | Value |
|-----|-------|
| `<title>` / `og:title` | `Josef — Game Developer Portfolio` |
| `meta[name=description]` / `og:description` | `Game dev portfolio showcasing Drag Rush, Dispater, Floor Zero, and SwingSpace.` (exact wording locked here per D-07's "keep it simple/direct" instruction — do not embellish further) |
| `og:url` | `https://1juicef.github.io/gamedev-portfolio/` |
| `og:image` | `https://1juicef.github.io/gamedev-portfolio/img/avatar.png` (absolute URL required by OG spec — relative paths will not render in link previews) |

---

## UI Considerations

Applicable state considerations resolved: 4 covered, 0 backstop, 0 unresolved

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| populated | Timeline (`.project-timeline`) | ✅ covered | Data source (`GameProjectsData.ts`) is a static, hand-authored array of exactly 4 projects — always populated, no empty/loading/error variant exists or is needed. |
| long-text | Project titles (`.project-title-link`) | ✅ covered | All 4 current names ("Drag Rush", "Dispater", "Floor Zero", "SwingSpace") fit on one line at 2.1em on both the 620px mobile breakpoint and desktop; no wrap/truncation behavior needs to be defined for this content set. |
| overflow | Timeline images (`.project-image-wrap`) | ✅ covered | Existing `width: 100%` + flex-basis split (58%/42% desktop) already caps growth relative to the row; no ultra-wide-specific overflow observed in current layout — no new cap needed beyond the site's existing `.main { max-width: 1280px }` page cap. |
| zero-one-many | Timeline rows | ✅ covered | Fixed cardinality (4), not data-driven by user input — "many" case (5th+ project) is explicitly out of scope this phase (game-jam additions deferred to a future phase per STATE.md). |

---

## Registry Safety

Not applicable — no shadcn/component registry initialized for this stack (Vue 2 + webpack, no `components.json`). No third-party UI blocks are introduced this phase.

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: PASS
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS
- [x] Dimension 5 Spacing: PASS
- [x] Dimension 6 Registry Safety: PASS

**Approval:** approved (gsd-ui-checker, 2026-07-22)
