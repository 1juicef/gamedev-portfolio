---
phase: 03-visual-polish-resume-site-metadata
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/index.html
  - src/views/Resume.vue
autonomous: true
requirements: [META-01, RESUME-01]

must_haves:
  truths:
    - "Sharing the site link renders the real title 'Josef — Game Developer Portfolio' and the locked description 'Game dev portfolio showcasing Drag Rush, Dispater, Floor Zero, and SwingSpace.' in the social preview (per D-07)"
    - "og:url is https://1juicef.github.io/gamedev-portfolio/ (the confirmed GitHub Pages project URL, per D-08)"
    - "og:image is the absolute avatar URL https://1juicef.github.io/gamedev-portfolio/img/avatar.png reusing the existing hero avatar asset (per D-09)"
    - "No placeholder domain or placeholder image values remain in the public/index.html <head> block"
    - "The Resume page displays actualResume.png as a single centered, width-capped static image with no download link and no click-to-enlarge (per D-05/D-06)"
  prohibitions:
    - "MUST NOT reference a nonexistent or relative og:image URL — og:image must be an absolute URL pointing at an asset that actually exists (public/img/avatar.png), otherwise a recruiter's shared-link preview renders blank/broken, defeating META-01's purpose."
  artifacts:
    - path: public/index.html
      provides: "Real site metadata (title, description, og:title/description/url/image)"
      contains: "1juicef.github.io/gamedev-portfolio"
    - path: src/views/Resume.vue
      provides: "Single static resume image treatment (verify against D-06; correct only if it deviates)"
      contains: "actualResume.png"
    - path: public/img/avatar.png
      provides: "Existing avatar asset that og:image resolves to (must exist)"
  key_links:
    - from: "public/index.html og:image"
      to: "public/img/avatar.png"
      via: "absolute deploy URL of the existing hero avatar asset"
      pattern: "img/avatar\\.png"
    - from: "src/views/Resume.vue <img>"
      to: "public/img/actualResume.png"
      via: "root-relative src on the resume image"
      pattern: "actualResume\\.png"
---

<objective>
Replace the placeholder OG/metadata in `public/index.html` with the real, locked site info so recruiter-shared links render a correct social preview (META-01 / D-07/D-08/D-09), and verify that `Resume.vue` already ships the single static resume image Josef chose (RESUME-01 / D-05/D-06 — verify-don't-rebuild).

Purpose: A broken/placeholder link preview undercuts the portfolio's core value the moment it's shared; the resume page is the final chrome piece. Both are low-risk, high-signal finishing touches.
Output: A corrected `<head>` metadata block, and a confirmed (committed) resume view.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-visual-polish-resume-site-metadata/03-CONTEXT.md
@.planning/phases/03-visual-polish-resume-site-metadata/03-UI-SPEC.md
@.planning/phases/03-visual-polish-resume-site-metadata/03-PATTERNS.md
@public/index.html
@src/views/Resume.vue
</context>

## Artifacts this phase produces

This plan EDITS `public/index.html` and VERIFIES `Resume.vue` (no new files/components). Values introduced:

| Kind | Symbol / value | Location |
|------|----------------|----------|
| Metadata value | `<title>` / `og:title` = `Josef — Game Developer Portfolio` | `public/index.html` |
| Metadata value | description / `og:description` = `Game dev portfolio showcasing Drag Rush, Dispater, Floor Zero, and SwingSpace.` | `public/index.html` |
| Metadata value | `og:url` = `https://1juicef.github.io/gamedev-portfolio/` | `public/index.html` |
| Metadata value | `og:image` = `https://1juicef.github.io/gamedev-portfolio/img/avatar.png` | `public/index.html` |

No new asset is produced — `og:image` reuses the existing `public/img/avatar.png` (already the hero avatar).

## Flagged Assumptions (edge-probe unresolved)

The spec-less edge probe classified RESUME-01 and META-01 as `unclassified` / `unresolved`: its boundary/null/pagination/concurrency edge taxonomy does not map onto static-metadata-config and verify-only-markup requirements, so no edge-derived acceptance criteria were auto-generated. Surfaced (not silently dropped) as explicit assumptions: **META-01's resolved state is the exact locked copywriting contract in UI-SPEC (title/description/og:url/og:image), and RESUME-01's resolved state is the D-06 already-implemented treatment — both verified by positive assertions and the end-of-phase human check, not by the edge engine.**

<tasks>

<task type="auto">
  <name>Task 1: Replace placeholder OG/metadata with the locked real values</name>
  <files>public/index.html</files>
  <read_first>
    - public/index.html — the `<head>` block (~lines 14-19) holds the current placeholder title/description/og tags to replace
    - .planning/phases/03-visual-polish-resume-site-metadata/03-UI-SPEC.md — the "Copywriting Contract" table locks the exact tag values (do not embellish)
    - .planning/phases/03-visual-polish-resume-site-metadata/03-PATTERNS.md — public/index.html section shows the exact current→target attribute values
  </read_first>
  <action>
    Edit only the content attribute values and `<title>` text in the existing `public/index.html` `<head>` block — do NOT add, remove, or restructure any tags. Apply the UI-SPEC locked copywriting contract, implementing META-01 (D-07/D-08/D-09):
    (1) `<title>` and `og:title` content → `Josef — Game Developer Portfolio` (D-07).
    (2) `meta[name=description]` and `og:description` content → `Game dev portfolio showcasing Drag Rush, Dispater, Floor Zero, and SwingSpace.` — exact wording, keep it simple/direct, do not embellish (D-07).
    (3) `og:url` content → `https://1juicef.github.io/gamedev-portfolio/` — the confirmed GitHub Pages project URL (D-08).
    (4) `og:image` content → `https://1juicef.github.io/gamedev-portfolio/img/avatar.png` — absolute URL required by the OG spec (relative paths do not render in link previews); reuses the existing hero avatar asset at `public/img/avatar.png`, no new image needed (D-09).
    Replace every current placeholder value (the fictional domain and the nonexistent avatar-og image, both visible in read_first) so none remain.
    Note per CLAUDE.md: `public/index.html` hardcodes this metadata directly — `.env` is not read at runtime, so edit this file, not `.env`.
  </action>
  <verify>
    <automated>grep -q '1juicef.github.io/gamedev-portfolio/' public/index.html && grep -q 'Game Developer Portfolio' public/index.html && grep -q 'Drag Rush, Dispater, Floor Zero, and SwingSpace' public/index.html && grep -q 'img/avatar.png' public/index.html && test -f public/img/avatar.png</automated>
    <human-check>Paste the deployed site URL into a link-preview debugger (or a Discord/Slack message once deployed): the card shows the real title, the games description, and the avatar image — not a broken/blank image.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -q '1juicef.github.io/gamedev-portfolio/' public/index.html` succeeds (real og:url present, D-08)
    - `grep -q 'Game Developer Portfolio' public/index.html` succeeds (real title, D-07)
    - `grep -q 'Drag Rush, Dispater, Floor Zero, and SwingSpace' public/index.html` succeeds (locked description, D-07)
    - `grep -q 'img/avatar.png' public/index.html` succeeds (og:image points at the real avatar asset, D-09)
    - `test -f public/img/avatar.png` passes (the og:image target asset actually exists — prohibition guard)
    - The `<title>`, `og:title`, `og:description`, `og:url`, `og:image` tag structure is unchanged (only content values edited)
  </acceptance_criteria>
  <done>All five metadata values match the UI-SPEC locked contract, og:image resolves to an existing absolute-URL asset, no placeholder values remain, tag structure untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the resume page ships the single static image (D-06)</name>
  <files>src/views/Resume.vue</files>
  <read_first>
    - src/views/Resume.vue — already-implemented per D-06 (centered `actualResume.png`, `.resume-image { width:100%; max-width:1100px }`, responsive max-width 1200px at 620px+)
    - .planning/phases/03-visual-polish-resume-site-metadata/03-PATTERNS.md — Resume.vue section confirms the expected markup/CSS (verify, don't rebuild)
    - .planning/phases/03-visual-polish-resume-site-metadata/03-CONTEXT.md — D-05 (static only, no download/enlarge/PDF) and D-06 (already implemented)
  </read_first>
  <action>
    Verify RESUME-01 against D-05/D-06 rather than re-implementing. Confirm `src/views/Resume.vue` renders `img/actualResume.png` as a single centered, width-capped static image (`.resume-page` flex-centered; `.resume-image` width:100% / max-width:1100px, and max-width:1200px at the 620px+ breakpoint) with NO download link, NO click-to-enlarge, and NO PDF/accessibility affordance (D-05 accepted tradeoff, already logged in PROJECT.md). If the file already matches this treatment (expected), make no code change and let RESUME-01 be marked satisfied on commit. ONLY if the current file deviates from the D-06 treatment, correct it to match — do not add features beyond the static image.
  </action>
  <verify>
    <automated>grep -q 'actualResume.png' src/views/Resume.vue && grep -q 'resume-image' src/views/Resume.vue && grep -q 'max-width' src/views/Resume.vue && npm run lint 2>&1 | grep -qiv 'error'</automated>
    <human-check>Run `npm run serve`, visit /resume: a single centered resume image fills the column up to its cap, with no download button, no enlarge-on-click, and no extra chrome.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -q 'actualResume.png' src/views/Resume.vue` succeeds (correct image source)
    - `grep -q 'resume-image' src/views/Resume.vue` succeeds (expected class present)
    - `grep -q 'max-width' src/views/Resume.vue` succeeds (width cap present, D-06)
    - `test -f public/img/actualResume.png` passes (the resume image asset exists)
    - `npm run lint` reports no errors
    - Resume.vue contains only the single static image treatment — no download/enlarge/PDF affordance added (D-05)
  </acceptance_criteria>
  <done>Resume.vue confirmed (or corrected) to the D-06 single static centered image treatment with no extra affordances; RESUME-01 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public metadata → external consumers | `public/index.html` `<head>` metadata is served publicly and scraped by social/link-preview crawlers |
| static asset → shipped bundle | `Resume.vue` renders a static image from `public/`; no user input, no data fetching |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Information Disclosure | `public/index.html` OG metadata (site URL, avatar image) | low | accept | The exposed values are intentionally-public marketing metadata (title, description, deploy URL, avatar) — publishing them is the feature; no secrets, PII, or credentials are placed in the `<head>` |
| T-03-04 | Spoofing / Tampering | `og:url` / `og:image` absolute URLs (GitHub Pages, not yet deployed) | low | accept | URLs are the confirmed correct canonical project URL per D-08; no security impact if Pages is enabled later. og:image points at a controlled first-party asset (`public/img/avatar.png`) |
| T-03-SC | Tampering | package installs | low | accept | No package installs occur in this plan (metadata + verification only) — no supply-chain surface introduced |
</threat_model>

<verification>
- `grep` assertions in both tasks' acceptance_criteria pass
- `public/img/avatar.png` and `public/img/actualResume.png` exist (og:image + resume targets resolve)
- `npm run lint` passes with no errors
- End-of-phase human check confirms the link preview renders correctly and the resume page shows the single static image (per human_verify_mode=end-of-phase)
</verification>

<success_criteria>
`public/index.html` carries the real locked title/description/og:url/og:image with no placeholders and a resolving absolute avatar image (META-01/D-07/D-08/D-09), and `Resume.vue` is confirmed shipping the single static resume image with no extra affordances (RESUME-01/D-05/D-06).
</success_criteria>

<output>
Create `.planning/phases/03-visual-polish-resume-site-metadata/03-02-SUMMARY.md` when done
</output>
