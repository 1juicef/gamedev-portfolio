---
phase: quick-260722-ot7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/GameProjectsData.ts
  - src/components/Footer.vue
  - src/components/Header.vue
autonomous: true
requirements:
  - QT-260722-ot7-floor-zero-blurb-swap-footer-vue-spacing
must_haves:
  truths:
    - "Floor Zero 'About this game' block closes with the new one-liner."
    - "Footer renders 'Portfolio by Juicef' with a visible space before 'by'."
    - "Footer mascot renders to the RIGHT of the text; Header mascot stays to the LEFT of the nav links."
    - "Header and Footer mascots change gif per the active route."
    - "npm run lint and npm run build both pass clean."
  artifacts:
    - src/data/GameProjectsData.ts
    - src/components/Footer.vue
    - src/components/Header.vue
  key_links:
    - "Both components read this.$route.path via a local computed (mascotSrc) — no Vuex, no mixin, no shared file."
    - "img src is bound with :src=\"mascotSrc\" in both components."
---

<objective>
Four small, independent content/UI fixes on the portfolio, scoped to three files only:
1. Swap the Floor Zero closing blurb line.
2. Roll back the Footer link+text spacing to the last-committed one-line arrangement.
3. Reorder the Footer mascot to render after (to the right of) the text.
4. Make the Header AND Footer mascot gif route-aware via a local computed property.

Purpose: Fix a visible missing-space regression, reposition the footer mascot, add per-route mascot variety, and correct the Floor Zero copy — all without introducing global state or touching unrelated files.

Output: Edited src/data/GameProjectsData.ts, src/components/Footer.vue, src/components/Header.vue; a clean lint + build.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@./.claude/CLAUDE.md

# Vue 2 (Options API, Vue.extend) + TypeScript strict + Less. No test suite exists — verify via grep + build only.
# Router paths (src/router/index.ts): /game-projects, /resume, /other-projects, /contact, /404, and '*' -> /404.
# Both Header and Footer are rendered globally by App.vue on every route, so this.$route.path reflects the active page.
# 4-space indentation in .ts, 2-space in .vue templates (match each file's existing style).
@src/data/GameProjectsData.ts
@src/components/Footer.vue
@src/components/Header.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap Floor Zero closing blurb line</name>
  <files>src/data/GameProjectsData.ts</files>
  <action>
In the floor-0 ProjectData "About this game" block, the block currently ends with a placeholder closing line (the sentence about being kept awake at night). Replace ONLY that single closing sentence with exactly:

Guaranteed no sleep for a week minimum

Note: no trailing period — match the target string exactly. Do not alter the preceding lines ("First solo project...", "Multi-state AI...", "Custom built Key/Lock...") or any other project in the file. Only this one line changes.
  </action>
  <verify>
    <automated>grep -Fc "Guaranteed no sleep for a week minimum" src/data/GameProjectsData.ts   # expect 1</automated>
    <automated>grep -c "Unsettling" src/data/GameProjectsData.ts   # expect 0 (old placeholder gone)</automated>
  </verify>
  <done>The floor-0 block's final line reads exactly "Guaranteed no sleep for a week minimum"; the prior closing line is gone; no other lines changed.</done>
</task>

<task type="auto">
  <name>Task 2: Footer.vue — spacing rollback, mascot reposition, route-aware src</name>
  <files>src/components/Footer.vue</files>
  <action>
Three coordinated changes to Footer.vue, all inside the single `<div class="left">`.

(a) Spacing rollback: The last committed version had the link and text on ONE line with a literal space before "by" (`...Portfolio</a> by Juicef`). The current working copy split them across lines, collapsing the whitespace into "Portfolioby Juicef". Restore the one-line arrangement so `</a>` is immediately followed by a space then `by Juicef` with NO line break or indentation between `</a>` and `by`.

(b) Mascot reposition: The footer mascot `<img class="footer-guy" ... />` currently sits BEFORE the text. Move it so it renders LAST inside `.left` (to the right of the text). Keep the existing `.left { display: inline-flex; align-items: center; }` layout and all `.footer-guy` styles (width/height/object-fit/vertical-align/margin/transform) exactly as-is — reorder only; do not restyle. Keep the img's class and alt="Running character" attributes.

(c) Route-aware src: Replace the hardcoded `src` on the footer img with `:src="mascotSrc"`. Add a `computed` property `mascotSrc(): string` to the Vue.extend options that returns a gif path based on `this.$route.path`:
  /game-projects -> img/projects/Guy2.gif
  /resume        -> img/projects/Guy3.gif
  /contact       -> img/projects/Guy5.gif
  default (any other path) -> img/projects/Guy2.gif
Use a plain switch or map on this.$route.path with an explicit `: string` return type (strict mode). Do NOT add Vuex, a mixin, or any new shared file — a local computed reading this.$route.path directly is the intended minimal-footprint approach.

Resulting `.left` should be a single line equivalent to:
`<div class="left"><a href="https://github.com/schouffy/gamedev-portfolio" target="blank">Portfolio</a> by Juicef <img class="footer-guy" :src="mascotSrc" alt="Running character" /></div>`

Do NOT touch `.footer` background-color, the `.right` div, or any other existing style/media-query.
  </action>
  <verify>
    <automated>grep -Fc 'Portfolio</a> by Juicef <img class="footer-guy" :src="mascotSrc"' src/components/Footer.vue   # expect 1 (one-line arrangement, space before "by", img after text, dynamic src)</automated>
    <automated>grep -c 'src="img/projects/Guy2.gif"' src/components/Footer.vue   # expect 0 (no hardcoded template src remains)</automated>
    <automated>grep -c 'this.$route.path' src/components/Footer.vue   # expect >=1 (computed reads the route)</automated>
    <automated>grep -c 'class="left"><img' src/components/Footer.vue   # expect 0 (mascot no longer first inside .left)</automated>
  </verify>
  <done>Footer .left is one line: link, then a space, then "by Juicef", then the mascot img with :src="mascotSrc". A mascotSrc computed maps /game-projects,/resume,/contact to Guy2/Guy3/Guy5 with Guy2 default. No hardcoded footer src, no new global state, and no unrelated style changes.</done>
</task>

<task type="auto">
  <name>Task 3: Header.vue — route-aware mascot src (position unchanged)</name>
  <files>src/components/Header.vue</files>
  <action>
Make the Header mascot route-aware WITHOUT moving it. The `<img class="header-guy" ... />` must stay exactly where it is (before the three router-links inside `.nav-bar`) — do not reorder it and do not change any Header template markup or CSS other than the img's `src` attribute.

Change the header img's hardcoded `src` to `:src="mascotSrc"`. Add a `computed` property `mascotSrc(): string` to the Vue.extend options returning a gif path based on `this.$route.path`:
  /game-projects -> img/projects/Guy.gif
  /resume        -> img/projects/Guy1.gif
  /contact       -> img/projects/Guy4.gif
  default (any other path) -> img/projects/Guy.gif
Use a plain switch or map with an explicit `: string` return type. Keep the img's class="header-guy" and alt="Running character" attributes. Do NOT add Vuex, a mixin, or any shared file — a local computed is the intended approach. The existing commented-out props block can stay or be removed; do not add new props.
  </action>
  <verify>
    <automated>grep -Fc '<img class="header-guy" :src="mascotSrc" alt="Running character" />' src/components/Header.vue   # expect 1</automated>
    <automated>grep -c 'src="img/projects/Guy.gif"' src/components/Header.vue   # expect 0 (no hardcoded template src remains)</automated>
    <automated>grep -c 'this.$route.path' src/components/Header.vue   # expect >=1</automated>
  </verify>
  <done>Header mascot img uses :src="mascotSrc" in its original position (before the nav links); a mascotSrc computed maps /game-projects,/resume,/contact to Guy/Guy1/Guy4 with Guy default; no template reordering, no CSS change, no global state.</done>
</task>

<task type="auto">
  <name>Task 4: Verify — lint and build pass clean</name>
  <files>(no files modified)</files>
  <action>
Run the project's lint and production build as the final gate for all three edited files. Both scripts already wrap vue-cli-service with NODE_OPTIONS=--openssl-legacy-provider via cross-env, so no manual env setup is needed. If either fails on a TypeScript strict error around this.$route (e.g. an implicit-any return), add the explicit `: string` return annotation on the mascotSrc computed and re-run. Fix any lint error introduced by the edits; do not disable rules to pass.
  </action>
  <verify>
    <automated>npm run lint</automated>
    <automated>npm run build</automated>
  </verify>
  <done>npm run lint completes with no errors; npm run build produces dist/ with no errors.</done>
</task>

</tasks>

<threat_model>
No new trust boundaries. All changes are static template/data/computed edits: no external/user input is parsed, no network calls, no new npm/pip/cargo dependencies installed. this.$route.path is framework-controlled routing state, not untrusted input. No STRIDE-relevant surface introduced by this plan.
</threat_model>

<verification>
- grep gates in each task pass with the stated expected counts.
- npm run lint: clean.
- npm run build: clean (dist/ generated).
- Manual spot (optional): navigate /game-projects, /resume, /contact and confirm Header/Footer mascots swap per the mapping and the footer reads "Portfolio by Juicef" with a space, mascot on the right.
</verification>

<success_criteria>
- Floor Zero block closes with "Guaranteed no sleep for a week minimum".
- Footer link+text is one line with a space before "by"; footer mascot renders to the right of the text.
- Header mascot position unchanged (left of nav links).
- Header and Footer mascot gifs change with the active route per the mapping, driven by a local computed reading this.$route.path (no Vuex/mixin/new file).
- lint and build pass clean.
- Only src/data/GameProjectsData.ts, src/components/Footer.vue, and src/components/Header.vue changed.
</success_criteria>

<output>
Create `.planning/quick/260722-ot7-floor-zero-blurb-swap-footer-vue-spacing/260722-ot7-SUMMARY.md` when done.
</output>
