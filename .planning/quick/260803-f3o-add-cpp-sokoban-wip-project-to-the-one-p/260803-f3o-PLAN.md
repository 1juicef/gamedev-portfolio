---
phase: quick-260803-f3o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/GameProjects.vue
  - src/data/GameProjectsData.ts
  - public/img/projects/cpp-sokoban/currentprogress.mp4
  - public/img/projects/cpp-sokoban/super-mario-icons-square-yellow-box-with-question-mark-illustration-thumbnail.jpg
autonomous: false
requirements: [QUICK-260803-f3o]

must_haves:
  truths:
    - "Visiting /game-projects shows a 'Work In Progress' section containing a Sokoban card, positioned above the 'Previous Projects' heading."
    - "Clicking the Sokoban card (thumbnail, title, or View Details) opens the project details overlay with the gameplay video and the Technical Overview content."
    - "Sokoban appears exactly once on the page — it is not also rendered in the Previous Projects timeline."
    - "The Sokoban thumbnail video and poster image load from git-tracked assets, so a fresh clone + production build renders the card correctly."
    - "The Sokoban overlay's Technical Overview reads as an always-visible static section, matching the other four projects (no click-to-expand dropdown)."
    - "No unrelated untracked files (.continue/, .claude/launch.json, skills-lock.json, public/Postmortum descriptions.txt) and no unrelated modified files are swept into the commits."
  artifacts:
    - "src/data/GameProjectsData.ts — cpp-sokoban ProjectData entry committed to git (currently uncommitted working-tree content)"
    - "public/img/projects/cpp-sokoban/currentprogress.mp4 — tracked in git"
    - "public/img/projects/cpp-sokoban/super-mario-icons-square-yellow-box-with-question-mark-illustration-thumbnail.jpg — tracked in git"
  key_links:
    - "GameProjects.vue wipProject computed → must resolve the cpp-sokoban entry out of gameProjectsData by id; if it returns undefined the entire WIP section is v-if'd away and nothing renders."
    - "The ProjectData id string 'cpp-sokoban' → the keys of the already-committed thumbVideos / thumbPosters / summaries maps in GameProjects.vue. An id mismatch silently renders an empty thumbnail and blank summary."
    - "timelineProjects filter (p.id !== 'cpp-sokoban') → the same id string; a mismatch causes Sokoban to render twice."
    - "htmlDescription asset src paths (img/projects/cpp-sokoban/currentprogress.mp4) → the git-tracked file under public/; untracked assets render as broken media in a deployed build."
---

<objective>
Intentionally land the C++ Sokoban work-in-progress project on the game projects page (the "one-pager" — /game-projects, which /one-page also stacks), after it was twice accidentally committed and reverted.

Purpose: The scaffolding is already in place and committed but deliberately switched off. `src/views/GameProjects.vue` already ships a full `Work In Progress` section, plus `thumbVideos`, `thumbPosters`, and `summaries` entries keyed `cpp-sokoban`, and a `timelineProjects` filter that excludes it — but its `wipProject` computed hard-returns `undefined` with the comment that it is held back until the write-up is ready. The write-up now exists (uncommitted, in `src/data/GameProjectsData.ts`), so this plan turns the section on, tracks the two assets it references, and brings the overlay markup in line with the static-section convention adopted by quick tasks 28 and 29.

Output: A visible, clickable Sokoban WIP card on /game-projects backed by git-tracked assets, in two atomic commits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.claude/CLAUDE.md

@src/views/GameProjects.vue
@src/data/GameProjectsData.ts
@src/css/projects.less
</context>

<discovery_findings>
Verified during planning — the executor can trust these and does not need to re-derive them:

**Already committed and correctly wired in `src/views/GameProjects.vue` — do not re-add:**
- The `<section class="wip-section" v-if="wipProject">` template block (lines 24-51), including its `Work In Progress` heading, `LazyVideoThumbnail`, title button, summary, and `View Details` cue.
- `thumbVideos["cpp-sokoban"] = "img/projects/cpp-sokoban/currentprogress.mp4"` (line 141).
- `thumbPosters["cpp-sokoban"] = "img/projects/cpp-sokoban/super-mario-icons-square-yellow-box-with-question-mark-illustration-thumbnail.jpg"` (line 148).
- `summaries["cpp-sokoban"]` — the one-line timeline blurb (line 155).
- `timelineProjects` computed filtering out `cpp-sokoban` (lines 172-174), so the WIP card will not be duplicated in the Previous Projects timeline.
- `.wip-section`, `.wip-section-title`, and `.wip-row` scoped styles (lines 232-250).

The single blocker is the `wipProject` computed at lines 168-171, which returns `undefined`.

**Already present but uncommitted in `src/data/GameProjectsData.ts`:**
- A complete `cpp-sokoban` `ProjectData` entry at lines 4-168 — first element of the exported array, accent `#E08E32`, `isHigh=false`, `isWide=false`, thumbnail set to the question-block jpg.
- The description copy already states the project is unfinished twice (lines 6 and 21), so the WIP framing requirement is satisfied by the existing copy.
- Its Technical Overview uses the **older dropdown markup**: `<details class="tech-overview">` (line 23), `<summary>Technical Overview</summary>` (line 24), closing `</details>` (line 167). All four other projects were converted to static sections by quick tasks 28/29 and now use `<div class="tech-overview tech-overview--static">` + `<h2 class="tech-overview-heading">…</h2>` + `</div>` (see Drag Rush at lines 202-204 and 223-225 for the canonical pattern).

**CSS — no changes required to `src/css/projects.less`:**
Every class used by the new entry already exists there: `.paragraph` (5), `.center` (9), `.pc-video` (26), `.tech-overview` (63), `.tech-overview-content` (98), `.tech-overview-heading` (102), `.tech-snippet` (110), `.tech-caption` (143). `.tech-overview--static` is an intentional no-op hook class already used by the four committed projects — it has no dedicated rule and needs none.

**Assets in `public/img/projects/cpp-sokoban/` — all five currently untracked (`??`):**
| File | Size | Referenced? |
|------|------|-------------|
| `currentprogress.mp4` | 784 KB | Yes — overlay video + timeline thumbnail video |
| `super-mario-icons-square-yellow-box-with-question-mark-illustration-thumbnail.jpg` | 17 KB | Yes — `iconUrl` + thumbnail poster |
| `WIP.gif` | 10 KB | No |
| `mario-block.gif` | 79 KB | No |
| `what-how.gif` | 414 KB | No |

Only the two referenced files get tracked. The three unreferenced gifs stay exactly where they are, untracked — not deleted, not moved.
</discovery_findings>

<tasks>

<task type="tracer">
  <name>Task 1: Turn on the Sokoban WIP card end-to-end (assets → data → view)</name>
  <files>src/views/GameProjects.vue, public/img/projects/cpp-sokoban/currentprogress.mp4, public/img/projects/cpp-sokoban/super-mario-icons-square-yellow-box-with-question-mark-illustration-thumbnail.jpg, src/data/GameProjectsData.ts</files>
  <precondition>The five files listed in discovery_findings exist on disk under public/img/projects/cpp-sokoban/, and `git status --porcelain src/data/GameProjectsData.ts` reports it as modified (the uncommitted Sokoban entry is still in the working tree). If the entry is gone, halt and report — it must not be rewritten from scratch.</precondition>
  <action>
Wire the single path from asset to rendered card, then commit it.

First, in `src/views/GameProjects.vue`, replace the body of the `wipProject` computed (currently the held-back comment plus `return undefined;`) so it resolves the entry out of the already-imported `gameProjectsData` by id, looking for the id string `cpp-sokoban`. Use the same `find` shape and the same `(p: ProjectData) =>` parameter typing already used by the `mounted` hook and the `timelineProjects` computed in this file, so the declared `ProjectData | undefined` return type still holds without a cast. Remove the now-stale held-back comment; do not leave it behind describing behaviour that no longer applies. Change nothing else in this file — the template block, the three lookup maps, the timeline filter, and the scoped styles are all already correct.

Second, stage the two referenced assets by their exact full paths only. Do not stage the containing directory, do not use `git add -A`, `git add .`, or `git add public/img/projects/cpp-sokoban/`. This repo has twice had this directory swept into a commit by accident and reverted; the three unreferenced gifs must remain untracked, and the unrelated untracked entries in the working tree (`.continue/`, `.claude/launch.json`, `skills-lock.json`, `public/Postmortum descriptions.txt`) plus the unrelated modified planning SUMMARY must stay out of the commit.

Third, commit exactly four paths in one commit: the two asset files, `src/views/GameProjects.vue`, and `src/data/GameProjectsData.ts` (which carries the pre-existing uncommitted entry — commit it as-is; markup conventions are Task 2's job). List every path explicitly on the `git commit` command line rather than committing staged state, and run `git status --porcelain` afterwards to confirm the unrelated untracked and modified entries are all still present and uncommitted.
  </action>
  <verify>
    <automated>cd "D:/Kodning/Portfolio/gamedev-portfolio" &amp;&amp; npm run lint &amp;&amp; git ls-files --error-unmatch "public/img/projects/cpp-sokoban/currentprogress.mp4" "public/img/projects/cpp-sokoban/super-mario-icons-square-yellow-box-with-question-mark-illustration-thumbnail.jpg" &amp;&amp; test "$(git ls-files public/img/projects/cpp-sokoban/ | wc -l)" -eq 2 &amp;&amp; test "$(git diff --name-only HEAD~1 HEAD | wc -l)" -eq 4 &amp;&amp; test "$(grep -c 'return undefined' src/views/GameProjects.vue)" -eq 0 &amp;&amp; grep -q 'cpp-sokoban' src/views/GameProjects.vue &amp;&amp; npm run build</automated>
  </verify>
  <done>`npm run lint` and `npm run build` both succeed. Exactly two files under `public/img/projects/cpp-sokoban/` are tracked by git (the mp4 and the jpg); the three gifs remain untracked. The commit contains exactly four paths. `wipProject` no longer returns undefined, so the `v-if="wipProject"` WIP section renders. `git status --porcelain` still lists the unrelated untracked and modified entries as uncommitted.</done>
</task>

<task type="auto">
  <name>Task 2: Convert the Sokoban Technical Overview to the static-section convention</name>
  <files>src/data/GameProjectsData.ts</files>
  <action>
Bring the Sokoban entry's Technical Overview in line with the convention the other four projects were converted to by quick tasks 28 and 29 — an always-visible section instead of a click-to-expand disclosure widget.

Within the `cpp-sokoban` template literal only (currently lines 4-168), make three edits:
  1. Replace the opening disclosure element (line 23) with a `div` carrying the class list `tech-overview tech-overview--static`, exactly as Drag Rush does at line 202.
  2. Replace the caption element on line 24 with an `h2` carrying class `tech-overview-heading` and the same visible text `Technical Overview`, exactly as Drag Rush does at line 224.
  3. Replace the matching closing tag on line 167 with a plain closing `div` tag so the element nesting stays balanced.

Preserve everything else in the entry byte-for-byte: the inner `tech-overview-content` wrapper, all four `tech-snippet` blocks with their `pre`/`code` contents and HTML-escaped operators (`&amp;lt;&amp;lt;`, `&amp;gt;`, `&amp;amp;`), every `tech-caption` paragraph, the intro paragraphs, the video block, and the constructor arguments `"#E08E32", false, false`. Do not touch any other project entry in this file, and do not re-word the description copy — the unfinished/WIP framing is already stated in the intro paragraph and the closing line of the About section, and per project convention the author's own phrasing is kept as written.

No changes to `src/css/projects.less` are needed — all classes used are already defined there.

Commit `src/data/GameProjectsData.ts` alone, by explicit path.
  </action>
  <verify>
    <automated>cd "D:/Kodning/Portfolio/gamedev-portfolio" &amp;&amp; test "$(grep -c 'tech-overview tech-overview--static' src/data/GameProjectsData.ts)" -eq 9 &amp;&amp; test "$(grep -c 'tech-overview-heading' src/data/GameProjectsData.ts)" -eq 9 &amp;&amp; test "$(grep -c '<details' src/data/GameProjectsData.ts)" -eq 0 &amp;&amp; test "$(grep -c '</details>' src/data/GameProjectsData.ts)" -eq 0 &amp;&amp; test "$(grep -c '<summary>' src/data/GameProjectsData.ts)" -eq 0 &amp;&amp; test "$(grep -c 'tech-snippet' src/data/GameProjectsData.ts)" -ge 4 &amp;&amp; test "$(git diff --name-only HEAD~1 HEAD | wc -l)" -eq 1 &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <done>The file contains 9 static-section wrappers and 9 static-section headings (8 pre-existing across the four other projects, plus 1 for Sokoban), and zero disclosure-widget tags of any kind. All four Sokoban code snippets survive intact. The commit touches exactly one file. Lint and production build both pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
The C++ Sokoban WIP project is now live on the game projects page. `wipProject` in `GameProjects.vue` resolves the entry instead of returning undefined, the two referenced media assets are tracked in git, and the overlay's Technical Overview was converted from a click-to-expand dropdown to an always-visible static section matching the other four projects.
  </what-built>
  <how-to-verify>
1. Run `npm run serve` and open http://localhost:8080/#/game-projects
2. Above the "Previous Projects" heading, confirm a "Work In Progress" section (orange heading, orange bottom border) with a Sokoban card whose thumbnail plays the gameplay video, and whose summary mentions SDL3 + C++, the memory arena, and the hot-reloadable DLL.
3. Confirm the card sits in the reversed layout (image on the right on a wide window) and that Sokoban does NOT also appear a second time down in the Previous Projects timeline.
4. Click the thumbnail, then the title, then "View Details" — each should open the details overlay.
5. In the overlay, confirm: the gameplay video plays with controls; the "About this project" block reads correctly; the "Technical Overview" section is expanded by default with no arrow/triangle to click, and shows all four code snippets (Arena, Behaviour/Entity bitmask, DLL hot-reload, undo command buffer) with readable monospace formatting and italic captions underneath.
6. Confirm the code snippets render the C++ operators literally — `1 << 0`, `>=`, `&` — rather than showing `&lt;` style escape text on screen.
7. Also check http://localhost:8080/#/one-page — the same WIP section should appear inside the stacked Projects section.
8. On a narrow window (under 620px), confirm the WIP card stacks cleanly and the code blocks scroll horizontally instead of overflowing the overlay.
9. Finally, run `git status` and confirm `WIP.gif`, `mario-block.gif`, and `what-how.gif` are still listed as untracked, and that `.continue/`, `.claude/launch.json`, `skills-lock.json`, and `public/Postmortum descriptions.txt` were not committed.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looks wrong</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author-written HTML string → `v-html` in `ProjectDetailsOverlay` | Static, compile-time content authored in a TypeScript source file; no user or network input crosses this boundary at runtime. |
| Repo working tree → git commit | Untracked files adjacent to the intended change can be swept into a commit; this has already happened twice on this exact directory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q803-01 | Tampering | `v-html` render of `htmlDescription` in `ProjectDetailsOverlay.vue` | low | accept | Content is a static string literal committed in `src/data/GameProjectsData.ts`, never user-supplied and never fetched at runtime. XSS requires repo write access, at which point the attacker can already modify the site directly. Matches the disposition already accepted for the four existing project entries. |
| T-Q803-02 | Information Disclosure | Accidental commit of unrelated untracked working-tree files (`.continue/`, `.claude/launch.json`, `skills-lock.json`, local notes) | medium | mitigate | Both commit tasks stage and commit by explicit full file path only; bulk staging forms are prohibited in the task actions. Task 1 asserts a 4-path commit and Task 2 a 1-path commit via `git diff --name-only HEAD~1 HEAD`, and the human-verify checkpoint re-confirms with `git status`. |
| T-Q803-03 | Denial of Service | 784 KB mp4 committed to a static-hosted repo | low | accept | Comparable in size to the already-tracked per-project thumbnail videos; loaded lazily through `LazyVideoThumbnail`. The three unreferenced gifs (503 KB combined) are deliberately left untracked rather than committed. |

No package-manager installs occur in this plan, so the package legitimacy gate does not apply.
</threat_model>

<verification>
- `npm run lint` passes with no new errors.
- `npm run build` completes successfully (remember `NODE_OPTIONS=--openssl-legacy-provider` is already wired through the npm scripts via `cross-env`).
- `git ls-files public/img/projects/cpp-sokoban/` lists exactly the two referenced assets.
- `git log --oneline -2` shows exactly two new commits, neither containing unrelated files.
- Human verification checkpoint approved.
</verification>

<success_criteria>
- The Sokoban WIP card renders in its own "Work In Progress" section on /game-projects and /one-page, above "Previous Projects", and appears exactly once on the page.
- Clicking the card opens the details overlay with a playable video and an always-visible Technical Overview containing all four code snippets.
- The overlay's Technical Overview markup matches the static-section convention used by all four other projects — zero disclosure-widget elements remain anywhere in `src/data/GameProjectsData.ts`.
- Both referenced media assets are tracked in git so a fresh clone produces a working build; the three unreferenced gifs remain untracked, undeleted, and unmoved.
- No unrelated file was committed, closing out the twice-repeated accidental-sweep problem by making the inclusion deliberate and path-scoped.
</success_criteria>

<output>
Create `.planning/quick/260803-f3o-add-cpp-sokoban-wip-project-to-the-one-p/260803-f3o-SUMMARY.md` when done.
</output>
</content>
</invoke>
