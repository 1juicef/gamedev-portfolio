---
phase: quick-260726-ny2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/GameProjectsData.ts
autonomous: true
requirements: [QT-260726-ny2-add-postmortem-dropdowns-to-swingspace-dispater-floorzero]

must_haves:
  truths:
    - Opening the SwingSpace overlay shows a collapsed "Postmortem" toggle sitting between the "About this game" block and the existing "Technical Overview" toggle
    - Opening the Dispater overlay shows a collapsed "Postmortem" toggle as the last element, below the itch.io badge
    - Opening the Floor Zero overlay shows a collapsed "Postmortem" toggle as the last element, below the itch.io badge
    - Each new toggle expands to exactly four labelled sub-sections (Scope & goals / What went well? / What went wrong? / Takeaways) whose prose matches the supplied text word for word
    - Each new toggle looks and behaves identically to the existing Drag Rush Postmortem toggle (same triangle, same rotation, same spacing) because it reuses the same two class names
    - The Drag Rush entry and all three Technical Overview blocks are byte-identical to before
    - The file still parses — `npm run lint` passes (no template literal broken)
  artifacts:
    - src/data/GameProjectsData.ts (three new `<details class="tech-overview">` Postmortem blocks)
  key_links:
    - Each new block reuses the existing `.tech-overview` / `.tech-overview-content` selectors already defined in src/css/projects.less lines 63-101 — zero new CSS class names, zero stylesheet edits
    - Markup renders through `v-html` in ProjectDetailsOverlay.vue; `<details>`/`<summary>` is native HTML, so no JS toggle logic is needed
    - The Drag Rush entry (lines 37-57) is the canonical structural reference — copy its shape, never its prose
---

<objective>
Add a Postmortem collapsible to the SwingSpace, Dispater, and Floor Zero project entries, reusing the exact `<details class="tech-overview">` structure already shipped in the Drag Rush entry, with prose supplied verbatim by the author.

Purpose: Drag Rush already has a Postmortem; the other three entries end abruptly at their itch.io badge (or at the Technical Overview). This brings all four entries to the same depth of reflection without adding a single line of CSS or JS.
Output: Three inserted markup blocks in one data file. No component changes, no stylesheet changes, no new assets.

Three tasks by design — one per project entry, each carrying its own verbatim prose. They are independent inserts into non-overlapping regions of the same file; splitting them keeps each action readable and each diff independently verifiable.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
Verified before planning:
- `src/css/projects.less` lines 63-101 already define `.tech-overview`, `.tech-overview summary`, the hidden webkit marker, the CSS-border triangle `summary::before`, the `[open]` rotation, and `.tech-overview-content`. **This plan adds zero CSS.** Anything the new blocks need is already there.
- `src/data/GameProjectsData.ts` — Drag Rush entry, lines 37-57, is the reference structure: `<details class="tech-overview">` → `<summary>Postmortem</summary>` → `<div class="tech-overview-content">` → four `<div class="paragraph"><strong>Label</strong><br/>Prose</div>` blocks → close both. Note the labels carry **no trailing colon** and the ampersand is escaped as `&amp;`.
- Current entry layout:
  - Dispater (lines 128-158) ends: About block → itch.io badge paragraph (155-157) → closing backtick line 158.
  - Floor Zero (lines 159-185) ends: About block → itch.io badge paragraph (182-184) → closing backtick line 185.
  - SwingSpace (lines 186-290): About block (200-207) → `<details>` Technical Overview (208-289) → closing backtick 290. **No itch.io badge exists in SwingSpace** — its insertion point is between the About block and the Technical Overview details.
- Today `tech-overview` appears on 6 lines (3 details + 3 content). After this plan: 12.
- Today `<summary>Postmortem</summary>` appears once; `<summary>Technical Overview</summary>` appears twice. After this plan: 4 and 2.
- None of the supplied prose contains a backtick, a `${`, or a `<` — safe inside the TS template literals. The only character needing escaping is the ampersand in the "Scope & goals" label.

Ponytail check: `<details>`/`<summary>` is the native platform feature, already styled, already proven in this file. Nothing to build, nothing to abstract, nothing to install. The only real risk in this task is prose drift — which is why every verify step greps for exact author phrases rather than for structure alone.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the SwingSpace Postmortem block</name>
  <files>src/data/GameProjectsData.ts</files>
  <read_first>src/data/GameProjectsData.ts (lines 37-57 — the Drag Rush Postmortem, for structure; and lines 200-210 — the SwingSpace About block through the opening of its Technical Overview details)</read_first>
  <action>
In the SwingSpace `ProjectData` htmlDescription template literal only, insert one new `<details class="tech-overview">` block immediately after the closing tag of the "About this game" `paragraph center` div and immediately before the opening `<details class="tech-overview">` of the Technical Overview.

Structure, copied from the Drag Rush Postmortem you just read: `<details class="tech-overview">`, then a `<summary>` whose entire content is the plain text Postmortem (no heading tag, no nested markup), then `<div class="tech-overview-content">`, then four `<div class="paragraph">` blocks, then close the content div and the details element. Indent to match the surrounding entry.

Each of the four paragraph blocks is a `<strong>` label, then `<br/>`, then the prose. Labels carry no trailing colon and the first one escapes its ampersand as `&amp;` — exactly as Drag Rush does.

Label 1 — Scope &amp; goals:
The goal of this project was to be able to create and build a smaller game for mobile. First game that came to mind was OneMoreLine, which is basically what I based SwingSpace on.

Label 2 — What went well?:
Using Unity's built in DistanceJoint2D to grapple the planets with the calculated distance was something I implemented early on and stuck with the project all the way.

Label 3 — What went wrong?:
The base game was completed quite early on, which led me to think of what I could implement to make it more engaging. The answer: a leaderboard though Firebase. Firebase in itself was not that difficult to setup with all guides available on the official site by Google. However, to make the connection to Unity was very tedious and difficult to debug. In hindsight, I might have checked for other alternatives for a game this size.

Label 4 — Takeaways:
Relying on no one but oneself has its ups and downs. In my case, it became very apparent on what I prefer coding and what i procrastinate on doing. UI is not my favorite, and especially when not working with any artists. I felt that it became solely functionality based, rather than a mixture of function and art, which I much rather prefer working with.

Transcribe that prose character for character. Do NOT reword, retitle, re-punctuate, capitalise, expand contractions, or repair anything that reads like a mistake — the author has already reviewed and approved this text as written, including the lowercase pronoun and the missing article. Wrapping it across source lines for indentation is the only permitted change.

Do NOT touch: the SwingSpace intro, video, screenshots, About block, or its Technical Overview details and snippets; the Drag Rush, Dispater, or Floor Zero entries; `src/css/projects.less`; any `.vue` file. Introduce no new class names — `tech-overview` and `tech-overview-content` are reused verbatim, which is why this task ships zero stylesheet changes.
  </action>
  <verify>
    <automated>grep -Fc 'OneMoreLine' src/data/GameProjectsData.ts # expect 1</automated>
    <automated>grep -Fc 'a leaderboard though Firebase' src/data/GameProjectsData.ts # expect 1 (author phrasing preserved)</automated>
    <automated>grep -Fc 'what i procrastinate on doing' src/data/GameProjectsData.ts # expect 1 (author phrasing preserved)</automated>
    <automated>grep -Fc '&lt;summary&gt;Postmortem&lt;/summary&gt;' src/data/GameProjectsData.ts # expect 2 (Drag Rush + SwingSpace)</automated>
    <automated>grep -Fc '&lt;summary&gt;Technical Overview&lt;/summary&gt;' src/data/GameProjectsData.ts # expect 2 (both untouched)</automated>
    <automated>grep -n '&lt;summary&gt;' src/data/GameProjectsData.ts | tail -2 # expect Postmortem line, then Technical Overview line — SwingSpace order</automated>
    <automated>git diff -- src/css/projects.less # expect empty output</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>SwingSpace has a Postmortem details block between its About block and its Technical Overview, containing the four supplied paragraphs verbatim; no other entry and no CSS file changed; `npm run lint` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Dispater Postmortem block</name>
  <files>src/data/GameProjectsData.ts</files>
  <read_first>src/data/GameProjectsData.ts (lines 155-158 — the Dispater itch.io badge paragraph and the closing backtick line)</read_first>
  <action>
In the Dispater `ProjectData` htmlDescription template literal only, append one new `<details class="tech-overview">` block immediately after the closing tag of the itch.io badge `paragraph center` div. It becomes the last element in the entry, sitting directly above the closing template-literal backtick.

Use the identical structure produced in Task 1: `<details class="tech-overview">` → `<summary>Postmortem</summary>` → `<div class="tech-overview-content">` → four `<div class="paragraph"><strong>Label</strong><br/>prose</div>` blocks → close both. Labels carry no trailing colon; the ampersand in the first label is escaped as `&amp;`.

Label 1 — Scope &amp; goals:
The upmost goal of this project was to make a 3D game Unreal Engine. We wanted to make a short, highly immersive and intense experience, which was more driven by narrative and story, rather than actual gameplay. Unfortunately, we had to chop a lot of story down, due to inexperience in scoping.

Label 2 — What went well?:
The atmosphere we were able to capture in Dispater, is in my opinion, amazing. We struggled with the lighting for sometime, but once we dove deeper and did the proper research on how to work with lighting in UE, it all came around massively. I built the task manager for this game, the engine driving the game forward, which became quite modular. Other objects could inherit the interface and become a taskable object, and be able to listen to previous task to know when to activate itself.

Label 3 — What went wrong?:
Even though the goal of the game was to make a short and impactful story, it was very difficult to produce just that. As I wrote earlier, a lot of story elements had to be tweaked or cut off from the final build, since we did not have enough time. One big takeaway from me, is that if you are going to build a system that pretty much is carrying the game's core functions, build a debug system at the same time. If I had built a debug tool which had let me jump in and out of the story, where ever I pleased, it would have saved me A LOT of time. Iteration ate up a lot of days, and if I had figured this out earlier, the game could have become even better than it turned out.

Label 4 — Takeaways:
Audio and sound is a big passion of mine. I downloaded and used Reaper paired with UE's Metasounds. We knew that the audio would play a huge role in this game, but since none of us had made any music or audio for a game previously, we did not grasp how much time it would actually take. I wished that I could have at least one whole week only creating, tweaking, and mixing audio. But unfortunately, the inexperience in scoping properly struck again.

Transcribe that prose character for character. Do NOT reword, retitle, re-punctuate, or repair anything that reads like a grammatical slip — the author has reviewed and approved this text as written. Keep the apostrophe in `game's` as a plain straight apostrophe (safe inside a backtick template literal) and keep `A LOT` capitalised. Wrapping across source lines for indentation is the only permitted change.

Do NOT touch: the Dispater intro, trailer iframe, screenshots, About block, or itch.io badge; any other project entry; `src/css/projects.less`; any `.vue` file. No new class names. Dispater has no Technical Overview block and this task does not add one.
  </action>
  <verify>
    <automated>grep -Fc "UE's Metasounds" src/data/GameProjectsData.ts # expect 1</automated>
    <automated>grep -Fc 'become a taskable object' src/data/GameProjectsData.ts # expect 1</automated>
    <automated>grep -Fc 'make a 3D game Unreal Engine' src/data/GameProjectsData.ts # expect 1 (author phrasing preserved)</automated>
    <automated>grep -Fc '&lt;summary&gt;Postmortem&lt;/summary&gt;' src/data/GameProjectsData.ts # expect 3</automated>
    <automated>grep -c 'tech-overview' src/data/GameProjectsData.ts # expect 10 (5 details + 5 content lines)</automated>
    <automated>grep -A 1 -F 'yrgo.itch.io/dispater' src/data/GameProjectsData.ts # closing div follows; details block begins on the next line</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>Dispater ends with a Postmortem details block placed after its itch.io badge, containing the four supplied paragraphs verbatim; no Technical Overview was added; no other entry and no CSS file changed; `npm run lint` passes.</done>
</task>

<task type="auto">
  <name>Task 3: Add the Floor Zero Postmortem block</name>
  <files>src/data/GameProjectsData.ts</files>
  <read_first>src/data/GameProjectsData.ts (lines 182-185 — the Floor Zero itch.io badge paragraph and the closing backtick line)</read_first>
  <action>
In the Floor Zero `ProjectData` htmlDescription template literal only, append one new `<details class="tech-overview">` block immediately after the closing tag of the itch.io badge `paragraph center` div. It becomes the last element in the entry, sitting directly above the closing template-literal backtick.

Same structure as Tasks 1 and 2: `<details class="tech-overview">` → `<summary>Postmortem</summary>` → `<div class="tech-overview-content">` → four `<div class="paragraph"><strong>Label</strong><br/>prose</div>` blocks → close both. Labels carry no trailing colon; the ampersand in the first label is escaped as `&amp;`.

Label 1 — Scope &amp; goals:
Prior to this I had no experience on building a somewhat competent AI. I had just finished playing Resident Evil 9, and got inspired to making my own horror/action/survival-esque game. The one thing I really wanted to accomplish and deliver was a game where you are scared and chased by an entity, which you'd later get your revenge on for putting you through all that misery.

Label 2 — What went well?:
I built a modular interaction system which was extremely handy. After laying the ground work of the system, anything could basically be turned into an item to pick up. The keys, crowbar, hammer and of course, the AK-47.

Label 3 — What went wrong?:
The enemy AI can hear the player walking, but can also hear when the player either thuds a painting or vase in the level. Instead of just falling, I wanted to make this objects actual get destroyed. To get this done, I had to make a copy of the mesh (geometry collection) that which would swap itself out with the original mesh in runtime. This was quite tricky and broke plenty of times. Either the object did not want to break or broke too early. After plenty of research, I finally made it work.

Label 4 — Takeaways:
During this project I really got to understand how blueprints work, and that they are not that far from your standard programming practise. I got to learn how interfaces work and the power they wield and the hierarchical structure of actors and components work.

Transcribe that prose character for character. Do NOT reword, retitle, re-punctuate, or repair anything that reads like a grammatical slip — the author has reviewed and approved this text as written, including the awkward clauses. Keep the apostrophe in `you'd` as a plain straight apostrophe. Wrapping across source lines for indentation is the only permitted change.

Do NOT touch: the Floor Zero intro, video, screenshots, About block, or itch.io badge; any other project entry; `src/css/projects.less`; any `.vue` file. No new class names. Floor Zero has no Technical Overview block and this task does not add one.
  </action>
  <verify>
    <automated>grep -Fc 'geometry collection' src/data/GameProjectsData.ts # expect 1</automated>
    <automated>grep -Fc 'the AK-47' src/data/GameProjectsData.ts # expect 1</automated>
    <automated>grep -Fc 'standard programming practise' src/data/GameProjectsData.ts # expect 1 (author spelling preserved)</automated>
    <automated>grep -Fc '&lt;summary&gt;Postmortem&lt;/summary&gt;' src/data/GameProjectsData.ts # expect 4 (all four entries)</automated>
    <automated>grep -c 'tech-overview' src/data/GameProjectsData.ts # expect 12 (6 details + 6 content lines)</automated>
    <automated>grep -A 1 -F 'juice-f.itch.io/floorzero' src/data/GameProjectsData.ts # closing div follows; details block begins on the next line</automated>
    <automated>grep -n '&lt;summary&gt;' src/data/GameProjectsData.ts # expect 6 lines in order: Postmortem, Technical Overview, Postmortem, Postmortem, Postmortem, Technical Overview</automated>
    <automated>git diff --stat # expect exactly one file changed: src/data/GameProjectsData.ts</automated>
    <automated>npm run lint</automated>
    <human-check>Run `npm run serve`, open /game-projects. Open each of SwingSpace, Dispater and Floor Zero. Confirm: a collapsed Postmortem toggle appears in the right slot for each (SwingSpace above its Technical Overview; Dispater and Floor Zero at the very bottom under the itch.io badge); each expands to four bold-labelled paragraphs and rotates its triangle; each looks identical in styling to the Drag Rush Postmortem toggle. Spot-check one paragraph per project against the author's text for wording drift. Then reopen the Drag Rush overlay and confirm it is visually unchanged.</human-check>
  </verify>
  <done>All four game project entries have a Postmortem details toggle; Floor Zero's sits after its itch.io badge with the four supplied paragraphs verbatim; only `src/data/GameProjectsData.ts` was modified across the whole plan; `npm run lint` passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| htmlDescription string → `v-html` in ProjectDetailsOverlay.vue | Raw HTML is injected into the DOM without sanitization |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-QUICK-01 | Tampering (XSS sink) | `v-html` render of GameProjectsData htmlDescription | low | accept | Markup is build-time-static and author-supplied: no script tags, no inline event handler attributes, no new href/src, no externally-sourced or user-supplied input reaches this string. `<details>`, `<summary>`, `<div>` and `<strong>` are inert layout elements. Pre-existing sink, not widened by this change. ASVS L1 — accepted. |

No package-manager installs in this plan, so the package legitimacy gate does not apply.
</threat_model>

<success_criteria>
- SwingSpace, Dispater and Floor Zero each gain exactly one `<details class="tech-overview">` Postmortem block
- SwingSpace's block sits between the About block and the Technical Overview; Dispater's and Floor Zero's are the last element of their entry, after the itch.io badge
- All twelve supplied paragraphs are present word for word, with the author's original wording, spelling and punctuation intact
- Labels match the Drag Rush convention: no trailing colon, ampersand escaped as `&amp;`
- Zero new CSS class names, zero changes to `src/css/projects.less` or any `.vue` file
- The Drag Rush entry and both Technical Overview blocks are unchanged
- `npm run lint` passes
</success_criteria>

<output>
Create `.planning/quick/260726-ny2-add-postmortem-collapsible-dropdowns-to-/260726-ny2-SUMMARY.md` when done.
</output>
