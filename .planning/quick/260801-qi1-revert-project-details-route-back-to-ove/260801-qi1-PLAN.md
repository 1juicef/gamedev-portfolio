---
phase: quick-260801-qi1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ProjectDetailsOverlay.vue
  - src/views/GameProjects.vue
  - src/components/ProjectsList.vue
  - src/views/Resume.vue
  - src/router/index.ts
  - src/views/ProjectDetails.vue
  - src/views/OtherStuff.vue
autonomous: false
requirements: [QUICK-260801-QI1]

must_haves:
  truths:
    - "Clicking a project card (thumbnail, title, or View Details cue) on the Projects timeline opens the write-up as a modal dialog layered over the current page — the URL does not change and the page behind it does not scroll"
    - "Closing the overlay (X, Close button, or Escape) leaves the visitor at the exact scroll position they clicked from, so they never have to scroll back down the timeline"
    - "The overlay renders the same htmlDescription content with identical src/css/projects.less styling it had before the routed-page experiment"
    - "The overlay and its backdrop paint above the sticky header instead of the header showing through them"
    - "Resume 'Selected Projects' links still open a project's write-up, via the legacy /game-projects?project=<id> deep link"
    - "No /project/:id route, view file, or call site remains anywhere in src/"
    - "The Resume page shows a left-aligned 'Resume' page heading in the same style as the Contact and Other Stuff page headings"
    - "The /other-stuff page heading reads 'Other Projects' while the nav button label stays 'Other Stuff' and the URL stays /other-stuff"
  artifacts:
    - src/components/ProjectDetailsOverlay.vue
    - src/views/GameProjects.vue
    - src/components/ProjectsList.vue
    - src/router/index.ts
    - src/views/Resume.vue
    - src/views/OtherStuff.vue
  key_links:
    - "The overlay's `v-html` wrapper must stay inside a `class=\"dialog-content\"` element — every project-content style in src/css/projects.less is nested under `.dialog-content`, so losing that class silently unstyles all project HTML"
    - "Overlay z-index must exceed the sticky header's `z-index: 10` (Header.vue). The restored file's original values (10 / 11) predate the sticky header and only win by DOM order — they must be raised so the stacking is explicit"
    - "GameProjects.vue `mounted()` reads `$route.query.project`; Resume.vue's router-link writes it. Those two must stay in sync or every Resume project link becomes a dead scroll to the timeline"
    - "`showDetails(project)` is called from six places in the GameProjects.vue template (three in the WIP block, three in the timeline loop). Keep the method name and signature so no template edit is needed"
---

<objective>
Put project details back into the stacked modal overlay, and fix the original bug properly this time: opening it must not scroll the page behind it. Then add a "Resume" page heading and rename the Other Stuff page heading.

Purpose: The previous quick task (260801-pnq) fixed the wrong thing. The real complaint was that `showDetails()` called `window.scrollTo(0, 0)`, so clicking a project threw the timeline back to the top and the visitor lost their place. That task solved it by turning the modal into a routed page at `/project/:id` — the user has reviewed it and does not want a separate page. The overlay comes back; the scroll-to-top call does not.

Output: `ProjectDetailsOverlay.vue` restored and layered above the sticky header, all three entry points back on popup state, the routed page and its router plumbing deleted, a "Resume" heading on the resume page, and "Other Projects" as the Other Stuff page heading.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

@src/views/ProjectDetails.vue
@src/router/index.ts
@src/css/projects.less
@src/views/Contact.vue
</context>

<key_facts>
Established by reading the current tree and git history — treat as given, do not re-derive:

- **The file to restore is in git.** `ProjectDetailsOverlay.vue` was deleted in commit `f4b6e2b`. Its last committed content is at `f4b6e2b^`. Restore it with git rather than retyping 158 lines.
- **What the previous task changed, and what stays.** `git diff f8be049^..15f8ff9` touched nine files. Three of them are the sticky-header work the user explicitly likes and this plan must not touch: `src/components/Header.vue`, `src/css/variables.less` (the added `@headerBgColor`), and `src/App.vue` (the `.main, .footer` max-width rule). The other six are what this plan reverts.
- **The bug being fixed.** Before `f8be049`, both `GameProjects.vue` and `ProjectsList.vue` ended `showDetails()` with `window.scrollTo(0, 0)`. That is the entire root cause of "it moves up to the top when I click a project". The overlay itself is `position: fixed` and pins to the viewport, so it needs no help from a scroll reset.
- **Z-index collision, new since the overlay was written.** The restored file sets `.overlay { z-index: 10 }` and `.dialog { z-index: 11 }`. `Header.vue` now sets `.header { position: sticky; z-index: 10 }` — that sticky rule did not exist when the overlay was authored. Both live in the root stacking context, so the overlay currently wins only because it appears later in the DOM. That is fragile and must be made explicit.
- **`.dialog-content` is load-bearing.** `src/css/projects.less` nests every project-content style under a single `.dialog-content { … }` block plus one `.dialog-content h2` rule, imported globally and unscoped by `App.vue`. Both the overlay and the routed page used that wrapper; the restored overlay already has it.
- **Resume needs no overlay of its own.** Before `f8be049`, `Resume.vue` linked projects via `router-link :to="/game-projects?project=<id>"`, and `GameProjects.vue`'s `mounted()` read `$route.query.project` and opened the overlay for it. Restoring that pair is the least-disruptive wiring: Resume stays a pure static page with zero popup state. This also keeps `OnePage.vue` simple — it embeds `Resume` and `GameProjects` as sibling sections, and a Resume project link there navigates away to `#/game-projects`, exactly as it did before 260801-pnq.
- **The deep-link path is the one place a scroll reset still belongs.** Arriving at `#/game-projects?project=<id>` from the Resume page is a route entry, not an in-page click — there is no position to preserve, and hash-mode routing with no `scrollBehavior` keeps the old scroll offset. Resetting scroll there (in `mounted`, not in `showDetails`) is correct.
- **`/other-projects` is a legacy orphan.** `OtherProjectsData.ts` exports an empty array and no nav link points at `/other-projects`, so `ProjectsList.vue` renders nothing today. Rewire it for correctness, but do not expand its scope. `OtherProjects.vue` also carries an unrelated `<h1>Other stuff</h1>` — that page is not the one the user is talking about; leave it alone.
- **Heading conventions.** Page titles across the site are a bare `<h1>` as the first element of the view (`Contact.vue` → `<h1>Contact</h1>`, `OtherStuff.vue` → `<h1>Other Stuff</h1>`, `GameProjects.vue` → `<h1>Hello there!</h1>`). `App.vue` already styles every `h1` globally: Russo One, `font-size: 2.5em`, `font-weight: 100`, and an explicit `h1, h2, h3, h4, h5 { text-align: left; }` rule. A plain `<h1>` therefore needs no new CSS to be left-aligned and on-brand.
- **Working tree state.** `src/data/GameProjectsData.ts` carries unrelated uncommitted edits (a `cpp-sokoban` entry). `GameProjects.vue` is committed but carries `cpp-sokoban` entries in `thumbVideos` / `thumbPosters` / `summaries`, a `wipProject` computed, a `timelineProjects` filter, and the `project-cue` markup. Layer the overlay restoration on top of the current file — never `git checkout` an older version of it.
- No test suite exists. Verification = `npm run build`, `npm run lint`, targeted greps, and the human-verify checkpoint.
</key_facts>

<tasks>

<task type="tracer">
  <name>Task 1: Restore the overlay and rewire every entry point — no scroll jump</name>
  <files>src/components/ProjectDetailsOverlay.vue, src/views/GameProjects.vue, src/components/ProjectsList.vue, src/views/Resume.vue</files>
  <read_first>src/views/GameProjects.vue, src/components/ProjectsList.vue, src/views/Resume.vue, src/views/ProjectDetails.vue, src/components/Header.vue</read_first>
  <action>
**Step 1 — bring the component back from history.**

Run `git checkout f4b6e2b^ -- src/components/ProjectDetailsOverlay.vue` from the repo root. Do not hand-write the file; the restored copy is the reference implementation for everything below. Read it once it is on disk.

Then make exactly two style edits to it, both forced by the sticky header that this plan is keeping:

- Raise the stacking so the modal is unambiguously above the header rather than winning on DOM order alone. `Header.vue` pins `.header` at `z-index: 10`; give the backdrop `.overlay` rule `z-index: 20` and the `.dialog` rule `z-index: 21`.
- Add `overscroll-behavior: contain;` to the `.dialog` rule. The dialog has its own `overflow-y: auto`; without this, reaching the end of the write-up chains the wheel through to the page underneath and quietly moves the visitor's position — the exact problem this task exists to eliminate.

Change nothing else in that file. In particular: do **not** add backdrop-click-to-close. The original had only the corner X, the bottom Close button, and the Escape key, and "behaves exactly as it did" is the requirement — a new close affordance is scope creep here.

**Step 2 — `src/views/GameProjects.vue`.**

This file has unrelated content that must survive untouched: the `cpp-sokoban` entries in `thumbVideos` / `thumbPosters` / `summaries`, the `wipProject` and `timelineProjects` computed properties, the whole `project-cue` button markup, and every style rule. Edit surgically; do not restore an older revision of this file.

- Template: add the overlay as the last child of `.journey-page`, immediately after the closing tag of the `<section class="game-jams">` block. Bind it the way the pre-`f8be049` version did — a `close` listener that sets `showPopup` back to false, plus `visible`, `title`, `htmlContent`, and `color` props fed from the popup data properties below.
- Script: import `ProjectDetailsOverlay` from `@/components/ProjectDetailsOverlay.vue` and register it in `components` alongside `LazyVideoThumbnail`.
- `data()`: add back the four popup properties — a boolean visibility flag defaulting to false, and title / color / content strings. Match the names the overlay bindings and the pre-existing code used (`showPopup`, `popupTitle`, `popupColor`, `popupContent`) and keep `popupColor`'s original default of `"#000000"`.
- `showDetails(item: ProjectData)`: keep the name and signature — six template call sites depend on it. Body sets the three popup content properties from the item's `name`, `accentColor`, and `htmlDescription`, then flips the visibility flag to true. **Nothing else.** No scroll manipulation of any kind belongs in this method; the overlay is `position: fixed` and pins itself to the viewport, and leaving the page exactly where it was is the entire point of this task.
- `mounted()`: keep the existing legacy deep-link branch — read `$route.query.project`, and when it is a string matching a known project id, open the overlay for it by calling `showDetails`. Replace the current `$router.replace` call with that. Because this branch runs on route *entry* (a visitor arriving from a Resume link) rather than an in-page click, there is no scroll position worth preserving and hash-mode routing will have carried the previous page's offset over — so reset the window scroll to the top **in this branch only**, before opening the overlay.

**Step 3 — `src/components/ProjectsList.vue`.**

Mirror the same treatment against its pre-`f8be049` shape: overlay element appended after the `.projects-list` div inside the existing wrapper, component imported and registered, the four popup properties added to a `data()` function (this component currently has none), and `showDetails` setting popup state only. Same rule as above — the method must not touch scroll. Do not reinstate the commented-out `if (event) { alert(...) }` block that used to sit inside that method; it was dead code. Leave the grid markup, props, and styles alone.

**Step 4 — `src/views/Resume.vue`.**

In the Selected Projects section, point the `router-link` back at the legacy query-string deep link: `` :to="`/game-projects?project=${project.id}`" ``. Resume deliberately gets **no** overlay instance of its own — it stays a static page and defers to `GameProjects.vue`'s `mounted()` handler, which is both the pre-existing behavior and the only option that keeps `OnePage.vue` (which embeds Resume and GameProjects as sibling sections) free of duplicated popup state. Change nothing else in the file; the heading work is Task 3.
  </action>
  <verify>
    <!-- planner-discipline-allow: window.scrollTo -->
    <!-- planner-discipline-allow: /project/ -->
    <automated>cd "D:/Kodning/Portfolio/gamedev-portfolio" && test -f src/components/ProjectDetailsOverlay.vue && grep -c 'dialog-content' src/components/ProjectDetailsOverlay.vue && grep -c 'overscroll-behavior: contain' src/components/ProjectDetailsOverlay.vue && grep -c 'z-index: 20' src/components/ProjectDetailsOverlay.vue && grep -c 'z-index: 21' src/components/ProjectDetailsOverlay.vue && grep -c 'ProjectDetailsOverlay' src/views/GameProjects.vue && grep -c 'ProjectDetailsOverlay' src/components/ProjectsList.vue && grep -c 'showPopup' src/views/GameProjects.vue && grep -c 'showPopup' src/components/ProjectsList.vue && grep -c 'game-projects?project=' src/views/Resume.vue && test $(grep -l "/project/" src/views/GameProjects.vue src/components/ProjectsList.vue src/views/Resume.vue | wc -l) -eq 0 && test $(sed -n '/^  methods:/,$p' src/views/GameProjects.vue | grep -v '^[[:space:]]*//' | grep -c 'window.scrollTo') -eq 0 && test $(sed -n '/^  methods:/,$p' src/components/ProjectsList.vue | grep -v '^[[:space:]]*//' | grep -c 'window.scrollTo') -eq 0 && grep -c 'cpp-sokoban' src/views/GameProjects.vue && grep -c 'project-cue' src/views/GameProjects.vue && npm run build && npm run lint</automated>
    <human-check>On `#/game-projects`, scroll down to SwingSpace and click its thumbnail: the write-up appears as a dialog over the timeline, the address bar still reads `#/game-projects`, and the timeline behind the dim backdrop has not moved. Close it — you are still looking at the SwingSpace row.</human-check>
  </verify>
  <done>Clicking any project entry point opens the modal overlay in place with no URL change and no page movement; the overlay renders project HTML with its usual styling and sits above the sticky header; Resume links target the legacy query deep link; the `cpp-sokoban` data and `project-cue` markup are intact; build and lint pass.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the routed project page and its router plumbing</name>
  <files>src/router/index.ts, src/views/ProjectDetails.vue</files>
  <read_first>src/router/index.ts</read_first>
  <action>
Now that nothing navigates to it, remove the routed-page experiment.

- `src/router/index.ts`: delete the route entry whose path is the project-detail param route (the one lazily importing `../views/ProjectDetails.vue`), leaving the surrounding route entries and their ordering untouched. Then remove the `scrollBehavior` option from the `new VueRouter({ … })` call so it reduces back to passing `routes` alone. That option existed only to make routed-page navigation land at the top and restore position on back; with an overlay there is no navigation to manage, and leaving it in would reintroduce scroll jumps on ordinary nav clicks. Leave the router in default hash mode — add no `mode` or `base` option, and do not touch any other route (`/other-stuff`, `/one-page`, `/404`, and the `*` redirect all stay).
- Delete the file `src/views/ProjectDetails.vue` (use `git rm` so the deletion is staged). Its markup and styling now live in the restored overlay component, and git history preserves it.

Leave no commented-out or stubbed references to either the deleted route or the deleted view anywhere in `src/`.
  </action>
  <verify>
    <!-- planner-discipline-allow: scrollBehavior -->
    <!-- planner-discipline-allow: ProjectDetails -->
    <automated>cd "D:/Kodning/Portfolio/gamedev-portfolio" && test ! -f src/views/ProjectDetails.vue && test $(grep -v '^[[:space:]]*//' src/router/index.ts | grep -c 'scrollBehavior') -eq 0 && test $(grep -v '^[[:space:]]*//' src/router/index.ts | grep -c 'ProjectDetails') -eq 0 && test $(grep -rl "ProjectDetails.vue" src/ | wc -l) -eq 0 && grep -c "'/other-stuff'" src/router/index.ts && grep -c "'/one-page'" src/router/index.ts && grep -c "redirect: '/404'" src/router/index.ts && npm run build && npm run lint</automated>
  </verify>
  <done>The project-detail route and its view file are gone, the router is back to `new VueRouter({ routes })` in hash mode, every other route is untouched, nothing in src/ references the deleted view, and build and lint pass.</done>
</task>

<task type="auto">
  <name>Task 3: Add a left-aligned "Resume" page heading</name>
  <files>src/views/Resume.vue</files>
  <read_first>src/views/Resume.vue, src/views/Contact.vue, src/App.vue</read_first>
  <action>
The resume page is the only view with no page title — it opens straight into the name block. Give it one, matching the convention the other views use.

- Insert `<h1>Resume</h1>` as the very first child of the `.resume-page` root div, above `<header class="resume-header">`. Use a bare `h1` with no new class: `App.vue` already styles every `h1` globally (Russo One, `2.5em`, `font-weight: 100`) and already declares `h1, h2, h3, h4, h5 { text-align: left; }`, so this is left-aligned and visually identical to the `Contact` and `Other Stuff` page titles with zero new CSS. Do not add any `text-align` declaration of your own, and do not override the global `h1` margins — matching the other pages' spacing is the point.
- The page now needs a single top-level heading, so demote the name: change `<h1>Josef Ubaka</h1>` inside `.resume-heading` to `<h2 class="resume-name">Josef Ubaka</h2>`. It stays the visual anchor of the header block, just one step below the page title.
- In the scoped style block, replace the existing `.resume-heading h1` rule with a `.resume-name` rule carrying `margin: 0 0 4px;`, `font-size: 1.9em;`, `font-weight: 100;`, and `line-height: 1.15;`. Font family and left alignment come from the global heading rules in `App.vue`, so do not restate them. Note this element sits inside `.resume-header`, not inside a `.resume-section`, so the existing `.resume-section h2` rule does not apply to it and must not be widened to.
- Change nothing else: the download button, contact row, summary blockquote, and all `.resume-section h2` subsection headings stay exactly as they are.

Be aware `OnePage.vue` embeds this view as its resume section, so the new heading appears there too — that is correct and consistent with the other embedded sections, which each carry their own page heading.
  </action>
  <verify>
    <automated>cd "D:/Kodning/Portfolio/gamedev-portfolio" && grep -c '<h1>Resume</h1>' src/views/Resume.vue && grep -c 'class="resume-name"' src/views/Resume.vue && grep -c '\.resume-name {' src/views/Resume.vue && test $(grep -c 'resume-heading h1' src/views/Resume.vue) -eq 0 && test $(grep -c 'text-align: center' src/views/Resume.vue) -eq 0 && grep -c 'Selected Projects' src/views/Resume.vue && npm run build && npm run lint</automated>
    <human-check>`#/resume` shows a "Resume" title at the top left, in the same type and position as "Contact" on the contact page, with "Josef Ubaka / GAME PROGRAMMER" and the Download button below it. Nothing is centered.</human-check>
  </verify>
  <done>The resume page opens with a left-aligned "Resume" h1 matching the site's other page titles, the name renders below it as a styled h2, the download button and all other sections are unchanged, and build and lint pass.</done>
</task>

<task type="auto">
  <name>Task 4: Rename the Other Stuff page heading to "Other Projects"</name>
  <files>src/views/OtherStuff.vue</files>
  <read_first>src/views/OtherStuff.vue, src/components/Header.vue</read_first>
  <action>
Text-only change, deliberately narrow in scope.

Change the on-page heading in `src/views/OtherStuff.vue` from `<h1>Other Stuff</h1>` to `<h1>Other Projects</h1>`. That is the whole edit.

Explicitly leave alone — the user asked for the heading, singular, and nothing else:
- The nav button label in `Header.vue` (both the router-link text and the `sectionLinks` entry) stays "Other Stuff".
- The route path `/other-stuff` and its route `name` in `src/router/index.ts` stay as they are; renaming the URL would break any shared link.
- The `other-stuff` section id in `OnePage.vue`, the `SECTION_MASCOTS` key, and the `.other-stuff-*` CSS class names stay as they are — they are internal identifiers, and the mascot map and IntersectionObserver both key off that id.
- `src/views/OtherProjects.vue` (the unlinked legacy `/other-projects` route, which happens to carry its own `<h1>Other stuff</h1>`) is a different page and is not in scope. Do not touch it.
- The intro paragraph copy below the heading stays as written.
  </action>
  <verify>
    <automated>cd "D:/Kodning/Portfolio/gamedev-portfolio" && grep -c '<h1>Other Projects</h1>' src/views/OtherStuff.vue && test $(grep -c '<h1>Other Stuff</h1>' src/views/OtherStuff.vue) -eq 0 && grep -c 'other-stuff-grid' src/views/OtherStuff.vue && test $(grep -c 'Other Stuff' src/components/Header.vue) -eq 2 && grep -c "'/other-stuff'" src/router/index.ts && grep -c 'id="other-stuff"' src/views/OnePage.vue && grep -c '<h1>Other stuff</h1>' src/views/OtherProjects.vue && npm run lint</automated>
    <human-check>`#/other-stuff` now reads "Other Projects" as its page title, while the nav button in the header still says "Other Stuff" and the URL is unchanged. The media gallery below is untouched.</human-check>
  </verify>
  <done>The Other Stuff page heading reads "Other Projects"; the nav label, route path, section id, CSS class names, and the legacy OtherProjects view are all unchanged; lint passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Project details are back in the stacked modal overlay — restored from git, layered above the sticky header, and opened without any scroll manipulation, so the page behind stays exactly where the visitor left it. The `/project/:id` route, its view file, and the router `scrollBehavior` option are deleted. The resume page gained a left-aligned "Resume" title, and the Other Stuff page heading now reads "Other Projects".

Deliberately **not** done, flag it here if you want it: the page behind the overlay is not scroll-locked. Scrolling inside the dialog is contained, but spinning the wheel while the cursor is over the dim backdrop will still move the page underneath. Locking it properly costs a scrollbar-width layout shift on desktop, so it was left out rather than guessed at.
  </what-built>
  <how-to-verify>
Run `npm run serve` and check:

1. **The actual bug — no jump.** On `#/game-projects`, scroll all the way down to SwingSpace and click its thumbnail. The write-up must appear as a dialog over the timeline, and the timeline visible behind the dim backdrop must not have moved. Close it (bottom "Close" button) — you should be looking at the SwingSpace row again, no scrolling back down required. Repeat with the corner X and with the Escape key.
2. **No new page.** While the overlay is open the address bar should still read `#/game-projects` — no `#/project/...` anywhere. Browser Back should leave the projects page entirely, not just close the dialog (this is the pre-existing overlay behavior, not a regression).
3. **All three click targets.** On a timeline row, the thumbnail, the project title, and the "View Details" cue must all open the same overlay.
4. **Content and styling.** Inside the overlay, confirm the title bar, screenshots, videos, and the collapsible Technical Overview / Postmortem sections all render with their usual styling — same as before the routed-page experiment.
5. **Overlay vs. sticky header.** The dim backdrop must cover the header too. Look specifically at the top of the screen: no nav bar or mascot showing through or floating over the dialog.
6. **Long write-up scrolling.** Open Drag Rush (the longest entry) and scroll inside the dialog to the very bottom. When you hit the end, the page behind should not start scrolling.
7. **Resume links.** On `#/resume`, click a project name under Selected Projects — it should land on the projects page at the top with that project's overlay already open.
8. **Resume heading.** `#/resume` shows "Resume" as a left-aligned page title above "Josef Ubaka", styled like "Contact" on the contact page. Check a narrow window too.
9. **Other Projects heading.** `#/other-stuff` reads "Other Projects" as its heading; the nav button still says "Other Stuff".
10. **Header untouched.** Scroll any page — the nav bar is still pinned, still opaque, no seam at the top. `#/one-page` still shows the scroll-button nav with active underline and swapping mascot, and the overlay still works from its projects section.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looks wrong</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL → app | Visitor-controlled `?project=` query value enters `GameProjects.vue`'s `mounted()` handler |
| Static data → DOM | Author-written `htmlDescription` strings are injected with `v-html` inside the overlay |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-QI1-01 | Tampering | `ProjectDetailsOverlay.vue` `v-html` | low | accept | Content is author-written static markup compiled into the bundle, never visitor input. This restores an existing render path rather than widening the boundary. |
| T-QI1-02 | Information disclosure | `?project=` query handling in `GameProjects.vue` | low | mitigate | The value is type-checked as a string and used only as an array `find` key against known ids; a non-match is a silent no-op. It is never used to build a URL, evaluated, or written to the DOM. |
| T-QI1-03 | Denial of service | Overlay scroll containment | low | mitigate | `overscroll-behavior: contain` on the dialog stops wheel chaining, so a long write-up cannot silently displace the page behind it. |
| T-QI1-04 | Tampering | Dependency supply chain | low | accept | No package-manager installs in this plan — no new dependencies added. |
</threat_model>

<verification>
- `npm run build` completes without errors after Tasks 1, 2, and 3
- `npm run lint` reports no new errors after every task
- `grep -rl "ProjectDetails.vue" src/` returns nothing and `src/views/ProjectDetails.vue` does not exist
- `git diff --name-only` shows no changes to `src/components/Header.vue`, `src/css/variables.less`, or `src/App.vue`
- Manual browser pass per the checkpoint above
</verification>

<success_criteria>
- Clicking a project opens a modal overlay over the current page with no URL change and no scroll movement, and closing it returns the visitor to the exact same spot
- The overlay and backdrop paint above the sticky header; scrolling inside the dialog does not chain to the page behind
- The `/project/:id` route, `ProjectDetails.vue`, and the router `scrollBehavior` option are all gone, with no dangling references
- Resume project links open the overlay via the legacy `?project=` deep link; Resume itself holds no popup state
- The sticky opaque header work from 260801-pnq is bit-for-bit untouched
- The resume page has a left-aligned "Resume" heading matching the site's other page titles
- The Other Stuff page heading reads "Other Projects" with nav label, route, and section id unchanged
- Unrelated uncommitted edits in `src/data/GameProjectsData.ts` and the `cpp-sokoban` / `project-cue` content in `GameProjects.vue` survive intact
- Human-verify checkpoint approved
</success_criteria>

<output>
Create `.planning/quick/260801-qi1-revert-project-details-route-back-to-ove/260801-qi1-SUMMARY.md` when done
</output>
