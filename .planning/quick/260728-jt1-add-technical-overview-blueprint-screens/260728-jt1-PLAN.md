---
phase: quick-260728-jt1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/GameProjectsData.ts
autonomous: true
requirements: [QUICK-260728-JT1]

must_haves:
  truths:
    - "Opening the Dispater project overlay shows a collapsible 'Technical Overview' section directly below the existing 'Postmortem' section."
    - "Expanding that section reveals three Unreal Blueprint screenshots, each with an italic caption underneath, framed identically to the Technical Overview section on Floor Zero."
    - "Clicking any of the three screenshots opens the full-resolution PNG in a new browser tab, where the browser's native image viewer provides zoom and pan."
    - "The three screenshots render at the same width as the surrounding overlay content and do not overflow the overlay on mobile."
    - "Floor Zero, Drag Rush and SwingSpace overlays are visually unchanged."
  artifacts:
    - "src/data/GameProjectsData.ts — Dispater entry ends with a `<details class=\"tech-overview\">` Technical Overview block containing three `.tech-snippet` items"
  key_links:
    - "The new `<details>` block sits inside the Dispater template literal only — after the Postmortem `</details>` and before the closing backtick of the `new ProjectData(\"dispater\", ...)` call. Dispater and Floor Zero share the same accent color `#6C3BAA`, so the `\\`, \"#6C3BAA\", false, false),` closing line is NOT a unique anchor; the entry must be located by its `new ProjectData(\"dispater\"` opening, or the section lands in Floor Zero's overlay."
    - "Each `.tech-bp-screenshot` image is wrapped in an `<a>` whose `href` points at the same `img/projects/dispater/*.png` path as its `src` — if href and src drift apart, click-to-zoom silently opens the wrong asset."
    - "`.tech-bp-screenshot` already exists in `src/css/projects.less` from quick task 260728-hop. This plan consumes that rule and adds no CSS; a second definition would be a silent duplicate."
---

<objective>
Add a "Technical Overview" collapsible section to the Dispater entry, showing three Unreal Blueprint screenshots with captions, using the exact pattern shipped for Floor Zero in the immediately-preceding quick task (260728-hop).

Purpose: Dispater's copy claims a modular task manager that "other objects could inherit the interface and become a taskable object" — these three graphs are the proof. They show the data-driven task definition, the executor that walks it, and the prerequisite gate that keeps steps in order, without showing a line of code.

Output: One new `<details class="tech-overview">` block appended to the Dispater `htmlDescription`. No CSS changes. No component changes.

Carried-over spec — implement as written, do not re-derive:
- Exactly these three screenshots, in this order: `EnumForTasksSC.png`, `SnippetFromElevevatorTerminalSC_1.png`, `SnippetFromElevevatorTerminalSC.png`. The user reviewed all five captures and selected these three. Do NOT add `EnumForTasks_0SC.png` or `MetasoundExampleSC.png`.
- Zoom/pan is handled by wrapping each image in a plain `<a target="_blank">` pointing at the full-res PNG. The browser's native image viewer is the whole solution. Do NOT build a lightbox, modal, or any JS image viewer.
- Reuse `.tech-overview` / `.tech-overview-content` / `.tech-snippet` / `.tech-caption` / `.tech-bp-screenshot`. Every one of these already exists in `src/css/projects.less`. This task introduces no new class and touches no stylesheet.
- Like Floor Zero and unlike Drag Rush/SwingSpace, these snippets contain images instead of `<pre><code>` blocks — Unreal Blueprint work is visual and there is no source text to paste.
- Anchors carry `rel="noopener noreferrer"` alongside `target="_blank"`, matching Floor Zero and the Phase 4 Game Jam link convention.

Caption voice: one to two sentences explaining design intent, matching the existing Floor Zero captions. These are not postmortem prose — do not write them in the longer first-person postmortem voice used elsewhere in this file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

@src/data/GameProjectsData.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Wire one screenshot end-to-end — Technical Overview block on Dispater with the task-definition snippet</name>
  <files>src/data/GameProjectsData.ts</files>
  <precondition>All three PNG files exist on disk: `public/img/projects/dispater/EnumForTasksSC.png`, `public/img/projects/dispater/SnippetFromElevevatorTerminalSC_1.png`, `public/img/projects/dispater/SnippetFromElevevatorTerminalSC.png`. Verified present at plan time; assert with `ls public/img/projects/dispater/` before editing and halt if any is missing.</precondition>
  <read_first>
    - `src/data/GameProjectsData.ts` lines 227-249 — the Floor Zero Technical Overview block. This is the exact structure to replicate: `details.tech-overview` / `summary` / `div.tech-overview-content` / `div.tech-snippet` / `a[target,rel]` / `img.tech-bp-screenshot[loading=lazy]` / `p.tech-caption`. Copy its indentation and nesting verbatim, changing only paths, alt text and captions.
    - `src/data/GameProjectsData.ts` lines 128-179 — the Dispater entry. Note it opens at `new ProjectData("dispater"` and its Postmortem `details` closes on line 178, with the template literal closing on line 179.
    - `src/css/projects.less` lines 98-134 — confirm `.tech-overview-content`, `.tech-snippet`, `.tech-bp-screenshot` and `.tech-caption` are all already defined. Nothing in this plan edits this file; read it only to confirm the classes exist before relying on them.
  </read_first>
  <action>
One edit, to `src/data/GameProjectsData.ts` only.

Locate the Dispater entry by its opening `new ProjectData("dispater"` — do not locate it by its closing line, because Dispater and Floor Zero share the same accent color and therefore have byte-identical closing lines.

Inside the Dispater template literal, insert a new `details` element with class `tech-overview` immediately after the Postmortem `details` element's closing tag and immediately before the closing backtick of that template literal, making it the last element of the Dispater description. Its `summary` text is `Technical Overview`. Inside it, a `div` with class `tech-overview-content`, and inside that, one `div` with class `tech-snippet` containing:
  - an `a` element with `href` of `img/projects/dispater/EnumForTasksSC.png`, `target` of `_blank`, and `rel` of `noopener noreferrer`;
  - inside that anchor, an `img` with class `tech-bp-screenshot`, `loading` of `lazy`, `src` of `img/projects/dispater/EnumForTasksSC.png`, and `alt` of `Task definition struct`;
  - after the anchor, a `p` with class `tech-caption` whose text is exactly: Every task is assembled from this one definition — prerequisite, target, ordered sub-steps and required components are all declared as data, so a new task is a new entry rather than another bespoke branch of logic.

Match the surrounding indentation of the Dispater literal (4-space base, same nesting depth as the Postmortem block above it). Paths are root-relative with no leading slash, matching every other asset reference in this file. The caption contains an em dash but no apostrophe, backtick or dollar-brace sequence, so it needs no entity or template-literal escaping — write it as plain text.

Add no CSS. `src/css/projects.less` is not in this plan's file list and must not be modified; every class used here was defined there by quick task 260728-hop. Do not convert these PNGs to WebP and do not add them to the `Helpers.preloadImages` list — they sit behind a collapsed `details` and are lazy-loaded on purpose.
  </action>
  <verify>
    <automated>grep -cF 'new ProjectData("dispater"' src/data/GameProjectsData.ts  # expect 1
grep -cF '<summary>Technical Overview</summary>' src/data/GameProjectsData.ts  # expect 4 (Drag Rush, Floor Zero, SwingSpace, Dispater)
grep -cF 'img/projects/dispater/EnumForTasksSC.png' src/data/GameProjectsData.ts  # expect 2 (href line + src line)
grep -cF 'class="tech-bp-screenshot"' src/css/projects.less  # expect 0 — stylesheet holds the rule, not markup
git diff --name-only  # expect only src/data/GameProjectsData.ts
npm run lint</automated>
  </verify>
  <done>The Dispater entry ends with a Technical Overview `details` block holding one `.tech-snippet` whose image links to `img/projects/dispater/EnumForTasksSC.png` in a new tab; `src/css/projects.less` is unmodified; `npm run lint` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Add the two remaining blueprint snippets</name>
  <files>src/data/GameProjectsData.ts</files>
  <action>
Inside the Technical Overview `div.tech-overview-content` created in Task 1, append two more `div` elements with class `tech-snippet`, in this order, each structured identically to the first (anchor with `target` of `_blank` and `rel` of `noopener noreferrer`, wrapping an `img` with class `tech-bp-screenshot` and `loading` of `lazy`, followed by a `p` with class `tech-caption`). For each, `href` and `src` are the same path.

Second snippet — path `img/projects/dispater/SnippetFromElevevatorTerminalSC_1.png`, alt `Elevator terminal action executor`, caption text exactly: The terminal walks the queued task one step at a time, translating each action value into the in-world effect it maps to. New kinds of step slot into the same switch instead of rewiring the flow that drives them.

Third snippet — path `img/projects/dispater/SnippetFromElevevatorTerminalSC.png`, alt `Prerequisite-gated interaction logic`, caption text exactly: A lever or button only responds if the task it belongs to is actually sitting in the current queue. That check is what keeps the sequence in order, so the station never has to physically lock anything away to stop a player skipping ahead.

Note the two filenames differ only by the `_1` suffix — the executor snippet is the `_1` file and comes second; the prerequisite-gate snippet is the unsuffixed file and comes third. Getting these backwards pairs each caption with the wrong graph.

Do not add a fourth snippet. Do not modify the Postmortem block, the screenshot row above it, `src/css/projects.less`, or any other project entry.
  </action>
  <verify>
    <automated>grep -cF 'img/projects/dispater/SnippetFromElevevatorTerminalSC_1.png' src/data/GameProjectsData.ts  # expect 2
grep -cF 'img/projects/dispater/SnippetFromElevevatorTerminalSC.png' src/data/GameProjectsData.ts  # expect 2
grep -cF 'class="tech-bp-screenshot"' src/data/GameProjectsData.ts  # expect 6 (3 Floor Zero + 3 Dispater)
grep -cF 'class="tech-caption"' src/data/GameProjectsData.ts  # expect 12 (3 Drag Rush + 3 Floor Zero + 3 SwingSpace + 3 Dispater)
git diff --name-only  # expect only src/data/GameProjectsData.ts
npm run build</automated>
    <human-check>Run `npm run serve`, open the Dispater card on /game-projects, scroll to the bottom of the overlay. Confirm: a "Technical Overview" dropdown sits below "Postmortem"; expanding it shows three blueprint screenshots each with an italic caption; the captions describe the graph directly above them (task struct, then the big action switch, then the Pressuriser Logic gate); clicking a screenshot opens the full-res PNG in a new tab where it can be zoomed and panned; the images do not overflow the overlay at a narrow (&lt;620px) window width. Then open the Floor Zero card and confirm its Technical Overview is unchanged.</human-check>
  </verify>
  <done>The Dispater Technical Overview contains exactly three `.tech-snippet` items in the order EnumForTasksSC, SnippetFromElevevatorTerminalSC_1, SnippetFromElevevatorTerminalSC, each with a matching `href`/`src` pair and a caption; `npm run build` completes without errors; `git diff --name-only` lists one file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → `v-html` | The Dispater `htmlDescription` string is rendered unescaped by `ProjectDetailsOverlay.vue` via `v-html`. |
| site → new browser tab | The three new anchors open a same-origin static image in a new tab. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-QT-01 | Tampering | New `target="_blank"` anchors in the Dispater entry | low | mitigate | Each anchor carries `rel="noopener noreferrer"`, matching Floor Zero and the Phase 4 Game Jam link convention. Targets are same-origin static PNGs with no script context, so exposure is nil either way. |
| T-QT-02 | Tampering / Injection | `v-html` render of `htmlDescription` | low | accept | The added markup is an author-written compile-time string literal in a TypeScript source file. No user input, query param, or network data reaches this string. Accepting is consistent with every existing project entry. |
| T-QT-03 | Information Disclosure | Blueprint screenshots published publicly | low | accept | Screenshots are of the author's own student project (`C9`), captured deliberately for publication as proof of work. Confirm at review that no file paths, account names, or third-party licensed asset names are legible in the captured graphs; the three selected captures were reviewed with the user before selection. |

No package-manager installs, dependency changes, or new network calls in this task, so no supply-chain (T-*-SC) threat applies.
</threat_model>

<verification>
1. `npm run lint` exits 0 and `npm run build` completes without errors.
2. `grep -cF 'class="tech-bp-screenshot"' src/data/GameProjectsData.ts` returns 6.
3. `grep -cF '<summary>Technical Overview</summary>' src/data/GameProjectsData.ts` returns 4.
4. Each of the three Dispater image paths appears exactly twice in `src/data/GameProjectsData.ts` (once as `href`, once as `src`).
5. `git diff --stat` shows exactly one file changed: `src/data/GameProjectsData.ts`. `src/css/projects.less` must not appear.
6. The new block is inside the Dispater entry: the line range reported by `git diff` falls between the `new ProjectData("dispater"` line and the `new ProjectData("floor-0"` line.
</verification>

<success_criteria>
- Dispater's overlay ends with a "Technical Overview" dropdown visually indistinguishable in framing from Floor Zero's.
- Three screenshots, three captions, in the specified order, each click-through opening its full-resolution PNG in a new tab.
- Zero new CSS — every class reused from the rules added by quick task 260728-hop.
- No JS, no lightbox component, no new dependency, no changes to any other project entry.
</success_criteria>

<output>
Create `.planning/quick/260728-jt1-add-technical-overview-blueprint-screens/260728-jt1-SUMMARY.md` when done
</output>
