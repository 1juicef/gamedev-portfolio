---
phase: quick-260809-q7g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/Resume.vue
  - public/downloads/Josef-Ubaka-Resume.pdf
autonomous: false
requirements: [QUICK-260809-q7g]

must_haves:
  truths:
    - "public/downloads/Josef-Ubaka-Resume.pdf is exactly one page — `file` reports `1 page(s)` where it reports `2 page(s)` today."
    - "src/css/print.less is byte-for-byte unchanged — `git diff --exit-code src/css/print.less` is clean. The documented floors (9pt font-size, 1.2 line-height, 10mm @page margins) are locked from prior work."
    - "Every resume section still exists: name/title, contact row, summary blockquote, Selected Projects (all 4), Experience (both), Education (both), Tech & Language, Beyond the Code, footer. Text inside them gets shorter; no section is deleted."
    - "The shortened Beyond the Code entries still sound like Josef — first-person, specific, a little personal. Trimmed, not rewritten into corporate/marketing phrasing."
    - "Professional content (Selected Projects, Experience, Education, Tech) keeps every factual claim it makes today — engine, language, team size, duration, role, employer, dates, and what he actually built. Only filler wording is removed."
    - "On-screen /resume still renders correctly at both mobile and desktop widths — this is a data-string edit, no template or style changes."
  artifacts:
    - "src/views/Resume.vue — shortened `summary` blockquote text, shortened `projects[].summary` strings, shortened `personal[].detail` strings"
    - "public/downloads/Josef-Ubaka-Resume.pdf — regenerated, single page"
  key_links:
    - "Chrome's --print-to-pdf reads the SERVED dist/ build, not src/. Every measurement cycle must go through `npm run export:resume-pdf` (which chains `npm run build` + the export script) or the PDF reflects the previous content."
    - "Print column widths set the per-string character budget, and Lekton is monospace so the budget is unusually predictable: ~99 chars/line full-width (190mm content box at 9pt), ~48 chars/line inside either half of the 2-column `.personal-entries` print grid or the 2-column `.resume-columns` print grid. A string one character over a budget costs a whole extra printed line."
    - "print.less reorders `.personal-entry` via grid `order` so Anime(3) and Music(5) share print row 1, Esports(1) and Current favorites(2) share row 2, Off the PC(4) sits alone in row 3. Each grid row is as tall as its TALLER member — shortening only one entry of a pair saves nothing. Cut in pairs."
    - "`page-break-inside: avoid` on .resume-section/.entry/.project-entry/.personal-entry makes page count drop in steps, not smoothly. A cut that saves real height can still leave the count at 2 until the last blocking section fits. Do not read a stalled page count as 'that cut did nothing' — keep going down the lever list."
    - "`.resume-columns` is a 2-column print grid (Experience | Education+Tech). Its height is max(left, right), so trimming Experience detail strings only buys page height while the Experience column is the taller of the two."
---

<objective>
Shrink the text content in `src/views/Resume.vue` until the exported resume PDF fits on exactly one page, down from two.

Purpose: prior work (commits 252f67a, 03a328e, fcbdbe4) drove every CSS density lever in `src/css/print.less` to its documented readability floor and still landed at 2 pages with ~140px of genuine content overflow. The remaining gap is content volume, and the user has now approved cutting content to close it.
Output: shorter strings in Resume.vue's `data()` and a regenerated single-page `public/downloads/Josef-Ubaka-Resume.pdf`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/views/Resume.vue
@src/css/print.less
@scripts/export-resume-pdf.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trim Resume.vue content strings to their print-line budgets</name>
  <files>src/views/Resume.vue</files>
  <action>
Edit only string literals — the `summary` blockquote text in the `<template>` and the `summary`/`detail` values inside `data()`. Do not change the template structure, the `data()` shape, any key/label/name/meta value, the number of array entries, or any style block. Do not open or edit `src/css/print.less`.

Cut in this order, applying these character budgets. The budgets come from Lekton being monospace at 9pt in a 190mm content box: roughly 99 characters per full-width printed line, roughly 48 characters per line inside either 2-column print grid. Landing one character over a budget costs a whole printed line, so count the finished strings rather than eyeballing them.

Lever A — `personal[].detail` (the Beyond the Code section, the primary target). Budget: 50 characters or fewer per detail, all five. Today Anime is ~120 and Music is ~123, each spilling to three lines in a ~48-char grid column; Esports (~60) and Current favorites (~55) each spill to two. Keep all five labels and keep each entry recognisably the same fact — the Overwatch rank and Ana, the same game titles, the childhood Dragon Ball origin, the outdoor running/walking, the singing-rapping-producing with friends. Compress by dropping subordinate clauses and connective filler, not by genericising. Retain first-person voice where the original had it; do not convert to punchy ad-copy.

Lever B — `projects[].summary`. Budget: 95 characters or fewer each, so every one fits a single full-width printed line. Only Drag Rush (~130) and Floor Zero (~110) currently exceed it. Preserve every technical claim verbatim in substance — the systems named, the engines implied, the specific work done. Remove hedging and connective phrasing only ("Helped with overhauling" -> "Overhauled", "together with" -> a comma, and similar).

Lever C — the `summary` blockquote in the `<template>`. Budget: 190 characters or fewer of visible text, so it fits two printed lines instead of four. Keep the two facts that carry weight for a game-programmer application: final term before graduating as a Game Programmer, and the fashion-industry background including co-founding a brand. The "positive, calm, clear approach" sentence and the "independently and in teams" sentence are the ones to drop or merge — they are the least differentiating lines on the page.

Levers A, B, and C together are estimated at roughly 8 printed lines against a ~11-line overflow, so Task 2 is expected to need at least one more pass. Apply A, B, and C now; hold the reserve levers for Task 2.

Run `npm run lint` when done to confirm the file still parses.
  </action>
  <verify>
    <automated>cd "D:/Kodning/Portfolio/gamedev-portfolio" && npm run lint && node -e 'const s=require("fs").readFileSync("src/views/Resume.vue","utf8");const seg=x=>{const i=s.indexOf(x);return s.slice(i,s.indexOf("],",i))};const len=(seg_,k)=>[...seg_.matchAll(new RegExp(k+": \"([^\"]*)\"","g"))].map(m=>m[1].length);const p=len(seg("personal: ["),"detail");const pr=len(seg("projects: ["),"summary");const bq=(s.match(/<blockquote class="summary">([\s\S]*?)<\/blockquote>/)||["",""])[1].replace(/\s+/g," ").trim().length;console.log("personal:",p.join(","),"| projects:",pr.join(","),"| blockquote:",bq);const bad=p.length!==5||pr.length!==4||Math.max(...p)>50||Math.max(...pr)>95||bq>190;process.exit(bad?1:0)'</automated>
  </verify>
  <done>`npm run lint` passes; all five `personal[].detail` strings are 50 characters or fewer, all four `projects[].summary` strings are 95 characters or fewer, and the blockquote's visible text is 190 characters or fewer. All five personal entries and all four project entries still exist.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the PDF and iterate until it reports one page</name>
  <files>public/downloads/Josef-Ubaka-Resume.pdf, src/views/Resume.vue</files>
  <precondition>A system Chrome/Chromium is installed and resolvable by scripts/export-resume-pdf.js (it probes the standard Windows install paths; `CHROME_PATH` overrides). The prior export at commit 8d23d87 succeeded on this machine, so this should already hold — halt and report if Chrome cannot be resolved.</precondition>
  <action>
Run `npm run export:resume-pdf` (it chains `npm run build` and the headless-Chrome export), then read the page count with `file public/downloads/Josef-Ubaka-Resume.pdf`, which prints `N page(s)`.

If it reports 1 page, stop — done.

If it still reports 2 pages, cut more content in `src/views/Resume.vue` and re-run the export. Repeat until it reports 1 page. Under no circumstances edit `src/css/print.less` to close the gap: its 9pt font-size, 1.2 line-height, and 10mm `@page` margins are locked floors from prior work, and every other density value in it was already driven to exhaustion.

Reserve levers, in the order to apply them:

1. Tighten `personal[].detail` further, to 40 characters or fewer. Cut the Anime and Music pair together — print.less pairs them in the same grid row, and a grid row is as tall as its taller member, so trimming only one saves zero height. Same for the Esports / Current favorites pair.
2. Drop the shortest Beyond the Code entry, "Off the PC", from the `personal` array entirely. Five entries in a 2-column grid means three rows with a half-empty third; four entries means exactly two rows, removing a full row of height. If this is applied, also remove the now-dangling nth-child comment mismatch risk by leaving print.less alone — the `order` values still resolve fine with four children, they just leave one gap.
3. Trim `experience[].detail` (Wet Dog ~72 chars, Axel Arigato ~57) to 48 characters or fewer each, so each drops from two printed lines to one. Every factual claim must survive: five years running an independent business, and KPI-driven retail. This only buys height while Experience is the taller of the two `.resume-columns` columns — if a cut here produces no measurable change, that column is no longer the constraint, so move on rather than cutting deeper.
4. Trim `projects[].summary` further, to 80 characters or fewer. Last resort — this is the highest-value content on the page for a game-programmer application.

Do not delete a Selected Projects entry, an Experience entry, an Education entry, a tech chip, the contact row, or the footer. Sections stay; only wording shrinks.

If four full iterations pass and the count is still 2, stop and report what was cut, what the remaining overflow looks like, and which lever you would reach for next — do not start violating the print.less floors to force a pass.
  </action>
  <verify>
    <automated>cd "D:/Kodning/Portfolio/gamedev-portfolio" && git diff --exit-code src/css/print.less && file public/downloads/Josef-Ubaka-Resume.pdf | grep -qE ', 1 page\(s\)' && file public/downloads/Josef-Ubaka-Resume.pdf</automated>
  </verify>
  <done>`file public/downloads/Josef-Ubaka-Resume.pdf` reports `1 page(s)`, and `git diff --exit-code src/css/print.less` exits clean, proving the print stylesheet's locked floors were untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Resume text content was shortened in `src/views/Resume.vue` (Beyond the Code entries, project summaries, and the summary blockquote — plus any reserve levers Task 2 needed) and `public/downloads/Josef-Ubaka-Resume.pdf` was regenerated at exactly one page. `src/css/print.less` was not touched.</what-built>
  <how-to-verify>
1. Open `public/downloads/Josef-Ubaka-Resume.pdf`. Confirm it is one page and that nothing is clipped at the bottom edge — the footer line ("References available on request" / www.josefubaka.com) should be visible.
2. Read the Beyond the Code section. The question to answer: does it still sound like you, or did it get trimmed into something flat and generic? Trimmed is fine; corporate is not.
3. Read the summary blockquote. Confirm the two facts you care about survived: last term before graduating as a Game Programmer, and the fashion background / co-founded brand.
4. Read Selected Projects and Experience. Confirm no technical or factual claim was lost or reworded into something inaccurate.
5. Run `npm run serve` and open /resume. Confirm the on-screen page looks right — the shortened strings should read naturally at screen size too, not like truncated stubs.
  </how-to-verify>
  <resume-signal>Type "approved", or name the specific strings you want re-worded and they will be revised (the character budgets are the constraint — a longer rewrite of one string means a shorter one elsewhere, then a re-export to re-confirm one page).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build output -> local loopback HTTP server -> headless Chrome | Existing export pipeline, unchanged by this task. Server binds 127.0.0.1 on an ephemeral port, rejects paths escaping dist/, and uses a throwaway Chrome profile. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-q7g-01 | Information disclosure | Resume PDF content (personal contact details) | low | accept | Content is already public on the live site by design; this task only shortens it. |
| T-q7g-02 | Tampering | npm/pip/cargo installs | high | mitigate | No packages are installed by this task — `npm run export:resume-pdf` runs the existing build and export script with zero new dependencies. Not applicable. |
</threat_model>

<verification>
- `git diff --exit-code src/css/print.less` exits 0 (locked print floors untouched).
- `git diff --name-only` lists only `src/views/Resume.vue` and `public/downloads/Josef-Ubaka-Resume.pdf` as changed by this task.
- `file public/downloads/Josef-Ubaka-Resume.pdf` reports `1 page(s)`.
- `npm run lint` passes.
- Human checkpoint approved.
</verification>

<success_criteria>
- The exported resume PDF is exactly one page.
- No resume section was deleted; only wording was shortened (with the single sanctioned exception of the "Off the PC" personal entry, and only if Task 2's reserve lever 2 was actually needed).
- `src/css/print.less` is unchanged.
- Josef's voice survives in the Beyond the Code section, confirmed by human read.
</success_criteria>

<output>
Create `.planning/quick/260809-q7g-shrink-resume-vue-content-so-exported-pd/260809-q7g-SUMMARY.md` when done. Record which levers were applied, how many export iterations it took, the final character lengths of the trimmed strings, and the before/after page count.
</output>
