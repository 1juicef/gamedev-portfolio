---
phase: 01-media-performance-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - scripts/convert-media.js
  - public/img/projects/drag-rush/DragRushGif.mp4
  - public/img/projects/drag-rush/DragRushGif-poster.webp
  - public/img/projects/drag-rush/DragrushSC1.webp
  - public/img/projects/drag-rush/DragrushSC2.webp
  - public/img/projects/drag-rush/DragrushSC3.webp
  - public/img/projects/drag-rush/DragrushSC4.webp
  - public/img/projects/drag-rush/DragrushSC5.webp
  - public/img/projects/dispater/DispaterGif2.mp4
  - public/img/projects/dispater/DispaterGif2-poster.webp
  - public/img/projects/dispater/DispaterSC1.webp
  - public/img/projects/dispater/DispaterSC2.webp
  - public/img/projects/dispater/DispaterSC3.webp
  - public/img/projects/dispater/DispaterSC4.webp
  - public/img/projects/dispater/DispaterSC5.webp
  - public/img/projects/floor-0/Floor0gif1.mp4
  - public/img/projects/floor-0/Floor0gif1-poster.webp
  - public/img/projects/floor-0/Floor0SC1.webp
  - public/img/projects/floor-0/Floor0SC2.webp
  - public/img/projects/floor-0/Floor0SC3.webp
  - public/img/projects/floor-0/Floor0SC4.webp
  - public/img/projects/swing-space/SwingSpaceGIF3.mp4
  - public/img/projects/swing-space/SwingSpaceGIF3-poster.webp
  - public/img/projects/swing-space/SwingSpaceSC1.webp
  - public/img/projects/swing-space/SwingSpaceSC2.webp
autonomous: true
requirements: [MEDIA-01, MEDIA-02]

must_haves:
  truths:
    - "sharp@^0.34.0 is installed as a devDependency and require('sharp') loads without error on Node 18.20.4 (NOT 0.35.x, which requires Node >=20.9 per Pitfall 1 / D-01)."
    - "ffmpeg -version runs successfully from the project shell before any conversion is attempted."
    - "Four MP4 thumbnails exist — DragRushGif.mp4, DispaterGif2.mp4, Floor0gif1.mp4, SwingSpaceGIF3.mp4 — each smaller than its source GIF, H.264/yuv420p, with no audio stream."
    - "Four poster WebPs exist (<basename>-poster.webp), each extracted from its source GIF's first frame."
    - "Sixteen screenshot WebPs exist (DragrushSC1-5, DispaterSC1-5, Floor0SC1-4, SwingSpaceSC1-2), each no larger than its source PNG."
    - "The SwingSpace timeline video is derived from SwingSpaceGIF3.gif per D-15, NOT SwingSpaceGIF.gif."
    - "Each conversion writes a uniquely-named output derived from its input filename; no conversion overwrites another conversion's output (edge: MEDIA-02 adjacency)."
    - "convert-media.js exits non-zero (fails loud) if any listed source file is missing on disk, rather than silently skipping it (edge: MEDIA-02 empty)."
    - statement: "Re-running convert-media.js on unchanged sources reproduces equivalent .webp/.mp4 outputs (fixed CRF 28 / quality 82 encode parameters, deterministic) (edge: MEDIA-02 ordering)."
      verification: backstop
  prohibitions:
    - "Must not delete, overwrite, or modify the original source GIF/PNG master files — every conversion output is a new sibling file (D-03)."
    - "Must not over-compress screenshots below a crisp, professional visual bar (D-12) in pursuit of smaller files."
    - "Must not import, require, bundle, or otherwise ship sharp, ffmpeg, or scripts/convert-media.js into the browser runtime — they are dev-time-only tooling."
  artifacts:
    - scripts/convert-media.js
    - public/img/projects/{drag-rush,dispater,floor-0,swing-space}/*.mp4 (4)
    - public/img/projects/{drag-rush,dispater,floor-0,swing-space}/*-poster.webp (4)
    - public/img/projects/{drag-rush,dispater,floor-0,swing-space}/*SC*.webp (16)
    - package.json sharp devDependency
  key_links:
    - "convert-media.js -> ffmpeg (child_process.execFile) -> .mp4 + poster PNG"
    - "convert-media.js -> sharp -> .webp (screenshots + posters)"
---

<objective>
Build the one-time, dev-machine media-conversion pipeline that both downstream vertical slices consume: install the tooling (ffmpeg + sharp), write `scripts/convert-media.js`, and produce + commit every converted asset — four MP4 video thumbnails, four WebP posters, and sixteen WebP screenshots.

Purpose: The timeline slice (01-02) and the overlay slice (01-03) both sit on top of these compressed assets. This plan is the shared media backbone; it introduces no user-visible change on its own, but nothing loads fast until these outputs exist. This is why it is planned as its own Wave-1 plan rather than pretending to be a vertical slice.
Output: `scripts/convert-media.js`, the `sharp` devDependency, and 24 converted asset files (4 mp4 + 4 poster.webp + 16 screenshot.webp) committed alongside their untouched source masters.
</objective>

<phase_goal>
**As a** recruiter or hiring lead scanning Josef's portfolio, **I want to** have the project timeline and overlays load fast and lean — video thumbnails and compressed, lazy-loaded images instead of multi-megabyte GIFs and PNGs — **so that** the site feels polished and professional within a 10-second scan without heavy downloads.

This plan delivers the conversion pipeline the two user-facing slices (01-02 timeline, 01-03 overlays) build on.
</phase_goal>

<artifacts_this_phase_produces>
Symbols and files created by this plan:
- `scripts/convert-media.js` — plain Node CLI script (no TypeScript). Functions: `gifToMp4(inputGif, outputMp4)`, `extractPoster(inputGif, outputPosterWebp)`, `toWebp(inputPng, outputWebp)`, plus a top-level asset manifest array driving the batch run.
- `package.json` — new `sharp` entry under `devDependencies`, pinned `^0.34.0`.
- 4 MP4 files: `DragRushGif.mp4`, `DispaterGif2.mp4`, `Floor0gif1.mp4`, `SwingSpaceGIF3.mp4` (each in its per-project folder).
- 4 poster WebPs: `<basename>-poster.webp` for each of the four source GIFs.
- 16 screenshot WebPs: `DragrushSC1-5.webp`, `DispaterSC1-5.webp`, `Floor0SC1-4.webp`, `SwingSpaceSC1-2.webp`.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-media-performance-optimization/01-CONTEXT.md
@.planning/phases/01-media-performance-optimization/01-RESEARCH.md
@.planning/phases/01-media-performance-optimization/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and verify ffmpeg + sharp tooling</name>
  <files>package.json</files>
  <read_first>
    - package.json (current devDependencies — where sharp is added)
    - .planning/phases/01-media-performance-optimization/01-RESEARCH.md (Standard Stack, Pitfall 1, Pitfall 2, Environment Availability)
  </read_first>
  <action>
Add `sharp` to `devDependencies` in package.json pinned to `^0.34.0` (per D-01 and Pitfall 1: the unpinned `latest` resolves to `0.35.x`, whose `engines.node` is `>=20.9.0` and will fail on this machine's Node 18.20.4 — confirmed `node -v` = 18.20.4). Run `npm install` to fetch it.

Install ffmpeg as a Windows system binary via `winget install Gyan.FFmpeg --accept-source-agreements --accept-package-agreements` (Gyan.FFmpeg is the verified full-build winget package bundling ffmpeg/ffprobe/ffplay). Per Pitfall 2, the winget PATH registration is not always live in the same shell session — after install, open a fresh shell and verify. If `ffmpeg -version` is still not recognized after a fresh shell, run `where ffmpeg` to inspect; the documented fallback is a manual full-build download from gyan.dev with its `bin` folder added to PATH by hand. Do not proceed to Task 2 until both tools verify.
  </action>
  <verify>
    <automated>node -e "console.log(require('sharp').format ? 'sharp-ok' : 'sharp-bad')" && ffmpeg -version</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `devDependencies` contains a `sharp` entry whose range is `^0.34.0` (NOT `latest`, NOT `^0.35`): `grep -c '"sharp": "\^0.34' package.json` returns 1.
    - `node -e "require('sharp')"` exits 0 (module loads on Node 18.20.4).
    - `ffmpeg -version` exits 0 and prints a version banner.
  </acceptance_criteria>
  <done>sharp@^0.34.0 is installed and loadable; ffmpeg is installed and invokable from the project shell.</done>
</task>

<task type="auto">
  <name>Task 2: Write convert-media.js and convert the four GIF thumbnails to MP4 + poster</name>
  <files>scripts/convert-media.js, public/img/projects/drag-rush/DragRushGif.mp4, public/img/projects/drag-rush/DragRushGif-poster.webp, public/img/projects/dispater/DispaterGif2.mp4, public/img/projects/dispater/DispaterGif2-poster.webp, public/img/projects/floor-0/Floor0gif1.mp4, public/img/projects/floor-0/Floor0gif1-poster.webp, public/img/projects/swing-space/SwingSpaceGIF3.mp4, public/img/projects/swing-space/SwingSpaceGIF3-poster.webp</files>
  <read_first>
    - .planning/phases/01-media-performance-optimization/01-RESEARCH.md (Code Examples: ffmpeg GIF->MP4, poster extraction, safe execFile invocation)
    - .planning/phases/01-media-performance-optimization/01-PATTERNS.md (convert-media.js pattern assignment, naming/style conventions)
    - .planning/phases/01-media-performance-optimization/01-CONTEXT.md (D-02, D-03, D-10, D-11, D-15)
  </read_first>
  <action>
Create `scripts/convert-media.js` as a plain Node CLI script (no TypeScript — it runs via `node scripts/convert-media.js`, outside the Vue CLI build, per Claude's Discretion in CONTEXT.md). Follow project conventions: camelCase function names (`gifToMp4`, `extractPoster`, `toWebp`), 4-space indentation. Shell out to ffmpeg using `child_process.execFile` with an argument array (never `exec` with a concatenated string) — this is the T-01-01 mitigation.

`gifToMp4` must call ffmpeg with these flags (per D-02 MP4/H.264-only and the RESEARCH Code Examples): `-movflags +faststart`, `-pix_fmt yuv420p`, `-vf scale=trunc(iw/2)*2:trunc(ih/2)*2` (even dimensions are an H.264 requirement), `-c:v libx264 -crf 28 -preset veryslow`, `-an` (strip audio). `extractPoster` must run ffmpeg `-vframes 1` on the source GIF to a temporary PNG, pipe that PNG through the same sharp WebP pipeline as Task 3 to write `<basename>-poster.webp`, then delete the temp PNG.

Before converting, the script MUST check each source exists (e.g. `fs.existsSync`) and throw / exit non-zero if any listed source is missing — never silently skip (edge: MEDIA-02 empty). Convert exactly these four sources (per the locked source-GIF decisions), writing each MP4 and poster next to its source, leaving the source GIF untouched (D-03):
- Drag Rush: `public/img/projects/drag-rush/DragRushGif.gif` -> `DragRushGif.mp4` + `DragRushGif-poster.webp` (D-10 — NOT DragRushGif2.gif).
- Dispater: `public/img/projects/dispater/DispaterGif2.gif` -> `DispaterGif2.mp4` + `DispaterGif2-poster.webp` (D-10 — NOT DispaterGif.gif).
- Floor 0: `public/img/projects/floor-0/Floor0gif1.gif` -> `Floor0gif1.mp4` + `Floor0gif1-poster.webp` (D-11).
- SwingSpace: `public/img/projects/swing-space/SwingSpaceGIF3.gif` -> `SwingSpaceGIF3.mp4` + `SwingSpaceGIF3-poster.webp` (D-15 — SwingSpaceGIF3.gif, NOT the currently-referenced SwingSpaceGIF.gif; D-15 explicitly overrides the RESEARCH Pitfall 5 default).

Run the script to produce the four MP4s and four posters.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const f=['drag-rush/DragRushGif','dispater/DispaterGif2','floor-0/Floor0gif1','swing-space/SwingSpaceGIF3'];let ok=true;for(const b of f){const mp4='public/img/projects/'+b+'.mp4';const pos='public/img/projects/'+b+'-poster.webp';const gif='public/img/projects/'+b+'.gif';if(!fs.existsSync(mp4)||!fs.existsSync(pos)){console.log('MISSING',b);ok=false;continue;}if(fs.statSync(mp4).size>=fs.statSync(gif).size){console.log('NOT-SMALLER',b);ok=false;}}console.log(ok?'videos-ok':'videos-fail');process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - All four `.mp4` files and all four `-poster.webp` files exist at the paths above (the automated node check prints `videos-ok`).
    - Each `.mp4` is strictly smaller than its source `.gif` (checked by the automated script).
    - `scripts/convert-media.js` uses `execFile` (not `exec`): `grep -c 'execFile' scripts/convert-media.js` returns >= 1 and `grep -c "child_process').exec(" scripts/convert-media.js` returns 0.
    - `ffprobe public/img/projects/drag-rush/DragRushGif.mp4 2>&1 | grep -ci 'audio'` returns 0 (no audio stream).
    - The four source GIFs still exist on disk (masters preserved, D-03).
  </acceptance_criteria>
  <done>Four muted H.264 MP4 thumbnails and four WebP posters exist, each smaller than its source; source GIFs untouched; script uses safe execFile invocation.</done>
</task>

<task type="auto">
  <name>Task 3: Convert the sixteen project screenshots PNG -> WebP</name>
  <files>scripts/convert-media.js, public/img/projects/drag-rush/DragrushSC1.webp, public/img/projects/drag-rush/DragrushSC2.webp, public/img/projects/drag-rush/DragrushSC3.webp, public/img/projects/drag-rush/DragrushSC4.webp, public/img/projects/drag-rush/DragrushSC5.webp, public/img/projects/dispater/DispaterSC1.webp, public/img/projects/dispater/DispaterSC2.webp, public/img/projects/dispater/DispaterSC3.webp, public/img/projects/dispater/DispaterSC4.webp, public/img/projects/dispater/DispaterSC5.webp, public/img/projects/floor-0/Floor0SC1.webp, public/img/projects/floor-0/Floor0SC2.webp, public/img/projects/floor-0/Floor0SC3.webp, public/img/projects/floor-0/Floor0SC4.webp, public/img/projects/swing-space/SwingSpaceSC1.webp, public/img/projects/swing-space/SwingSpaceSC2.webp</files>
  <read_first>
    - scripts/convert-media.js (the script started in Task 2 — extend it)
    - .planning/phases/01-media-performance-optimization/01-RESEARCH.md (Code Examples: sharp PNG->WebP; Open Question 3 on resizing; D-12/D-13)
  </read_first>
  <action>
Extend `scripts/convert-media.js` with the screenshot batch. `toWebp` uses `sharp(input).resize({ width: 1000, withoutEnlargement: true }).webp({ quality: 82, effort: 4 }).toFile(output)`. Rationale: `quality: 82` sits inside D-12's ~80-85% "still looks crisp" target; the resize is within Claude's Discretion (CONTEXT.md explicitly leaves resolution open) and addresses RESEARCH Open Question 3 — screenshots display at a CSS cap of ~400px desktop (retina 2x = ~800px) so capping the long edge at 1000px is free savings with no visible quality loss, and `withoutEnlargement: true` guarantees already-small sources (e.g. SwingSpaceSC1 at 63KB) are never upscaled. No `<picture>`/PNG fallback (D-13) — plain WebP only.

Convert exactly these sixteen screenshots, writing each `.webp` next to its `.png`, leaving the source PNG untouched (D-03):
- Drag Rush: `DragrushSC1.png` .. `DragrushSC5.png` -> `.webp`
- Dispater: `DispaterSC1.png` .. `DispaterSC5.png` -> `.webp`
- Floor 0: `Floor0SC1.png` .. `Floor0SC4.png` -> `.webp`
- SwingSpace: `SwingSpaceSC1.png` .. `SwingSpaceSC2.png` -> `.webp`

Reuse the same existence-check/fail-loud guard from Task 2 for every source. Run the script.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const m={'drag-rush':['DragrushSC1','DragrushSC2','DragrushSC3','DragrushSC4','DragrushSC5'],'dispater':['DispaterSC1','DispaterSC2','DispaterSC3','DispaterSC4','DispaterSC5'],'floor-0':['Floor0SC1','Floor0SC2','Floor0SC3','Floor0SC4'],'swing-space':['SwingSpaceSC1','SwingSpaceSC2']};let ok=true,n=0;for(const d in m)for(const b of m[d]){const w='public/img/projects/'+d+'/'+b+'.webp';const p='public/img/projects/'+d+'/'+b+'.png';if(!fs.existsSync(w)){console.log('MISSING',b);ok=false;continue;}n++;if(fs.statSync(w).size>fs.statSync(p).size){console.log('LARGER',b);ok=false;}}console.log('count',n);process.exit(ok&&n===16?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - All sixteen `.webp` screenshot files exist and the automated check prints `count 16` and exits 0.
    - Each `.webp` is no larger than its source `.png` (checked by the automated script).
    - All sixteen source `.png` screenshots still exist on disk (masters preserved, D-03).
  </acceptance_criteria>
  <done>Sixteen compressed WebP screenshots exist, each no larger than its source PNG; source PNGs untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev shell -> ffmpeg process | The conversion script invokes an external binary with file-path arguments. |
| npm / winget registries -> dev machine | New dependencies (sharp, ffmpeg) are fetched and executed at install time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | scripts/convert-media.js shelling to ffmpeg | low | mitigate | Use `child_process.execFile` with an argument array, never `exec` with string concatenation. All inputs are fixed, developer-authored filenames (no user input reaches this script), but arg-array invocation is a zero-cost hygiene control. |
| T-01-SC | Tampering (supply chain) | npm `sharp` install + winget `Gyan.FFmpeg` install | medium | mitigate | sharp carries an `OK`/Approved verdict in RESEARCH.md Package Legitimacy Audit (74M downloads/week, ~12yr, github.com/lovell/sharp, not archived); pin `^0.34.0`. ffmpeg `Gyan.FFmpeg` verified real/maintained via `winget search`. No `[ASSUMED]`/`[SUS]` packages are introduced, so no blocking-human legitimacy checkpoint is required. |
| T-01-02 | Denial of Service (repo/page weight) | committed media outputs | low | mitigate | Only the compressed outputs are referenced/loaded by the app; the large source masters are kept (D-03) but stay unreferenced and are excluded from any preload/eager-fetch path. |
</threat_model>

<verification>
- `node -e "require('sharp')"` and `ffmpeg -version` both succeed.
- `node scripts/convert-media.js` runs clean and re-running it is idempotent (equivalent outputs).
- 4 mp4 + 4 poster.webp + 16 screenshot.webp exist; every output is <= its source; all source masters remain on disk.
</verification>

<success_criteria>
The media-conversion pipeline exists and has produced all 24 compressed assets that plans 01-02 and 01-03 will wire in, with every original source master preserved.
</success_criteria>

<output>
Create `.planning/phases/01-media-performance-optimization/01-01-SUMMARY.md` when done.
</output>
