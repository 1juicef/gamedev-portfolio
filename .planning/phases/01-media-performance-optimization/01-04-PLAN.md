---
phase: 01-media-performance-optimization
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/convert-media.js
  - public/img/projects/floor-0/Floor0gif1.mp4
autonomous: true
requirements: [MEDIA-01]
gap_closure: true
gap_ids: [G-01-5]

must_haves:
  truths:
    - "Floor 0's timeline video thumbnail (Floor0gif1.mp4) plays a tight ~12.5s loop instead of the full 14.0s source length (G-01-5)."
    - "The other 3 video thumbnails (DragRushGif.mp4, DispaterGif2.mp4, SwingSpaceGIF3.mp4) are byte-identical to their pre-gap-closure versions — untouched, un-re-encoded."
    - "Source Floor0gif1.gif remains untouched on disk (D-03) — only the derived .mp4 is regenerated."
  artifacts:
    - path: "scripts/convert-media.js"
      provides: "gifToMp4() optional durationSeconds trim + floor-0-only videoAssets manifest entry"
    - path: "public/img/projects/floor-0/Floor0gif1.mp4"
      provides: "Re-encoded ~12.5s trimmed video thumbnail (down from 14.0s)"
  key_links:
    - from: "videoAssets floor-0 manifest entry ({ durationSeconds: 12.5 })"
      to: "gifToMp4(inputGif, outputMp4, options) options.durationSeconds"
      via: "convertVideos() destructures a 3rd manifest element and passes it through as gifToMp4's options arg, which appends ffmpeg -t <seconds> when set"
      pattern: "durationSeconds"
---

<objective>
Fix UAT gap G-01-5: Floor 0's timeline video thumbnail (`Floor0gif1.mp4`) runs the full 14.0s of its source GIF, including ~1.5s of trailing dead content the user flagged as making the loop feel too long. Add a minimal, per-asset trim capability to `scripts/convert-media.js`'s `gifToMp4()` function (an optional `durationSeconds` option that appends ffmpeg's `-t` flag), apply it only to the Floor 0 manifest entry, and regenerate only `Floor0gif1.mp4` at ~12.5s.

Purpose: Close the one outstanding minor UAT issue from Phase 1's 01-UAT.md so the phase can be marked fully passed. This is a narrow, single-asset fix — not a rework of the conversion pipeline. The other 3 video assets (Drag Rush, Dispater, SwingSpace) and all 16 screenshot assets are explicitly out of scope and must remain untouched.
Output: `scripts/convert-media.js` with trim capability wired into the manifest/loop, and a re-encoded `public/img/projects/floor-0/Floor0gif1.mp4` at ~12.5s.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-media-performance-optimization/01-UAT.md
@scripts/convert-media.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add per-asset trim capability to gifToMp4() and target the floor-0 manifest entry only</name>
  <files>scripts/convert-media.js</files>
  <read_first>
    - scripts/convert-media.js (gifToMp4, videoAssets, convertVideos — the exact code being modified)
    - .planning/phases/01-media-performance-optimization/01-UAT.md (Gaps section, gap_id G-01-5, root_cause, missing)
  </read_first>
  <action>
Modify `gifToMp4(inputGif, outputMp4)` in `scripts/convert-media.js` to accept a third parameter `options = {}`. Build the ffmpeg arg array in two stages: start with `["-y", "-i", inputGif]`; if `options.durationSeconds` is set, push `"-t", String(options.durationSeconds)` next (this becomes an output-side duration cap — ffmpeg stops encoding once that many seconds of the input have been written, effectively trimming the trailing content the user flagged); then push the existing flags (`-movflags +faststart`, `-pix_fmt yuv420p`, the scale filter, `-c:v libx264 -crf 28 -preset veryslow`, `-an`, `outputMp4`) unchanged. Keep using `execFileAsync("ffmpeg", args)` with the array-based invocation (T-01-01 mitigation — do not switch to a concatenated string). Update the trailing `console.log` to append ` (trimmed to Ns)` when `options.durationSeconds` is set, so a trim is visible in the CLI output.

Update the `videoAssets` manifest: change only the floor-0 entry from `["floor-0", "Floor0gif1"]` to `["floor-0", "Floor0gif1", { durationSeconds: 12.5 }]` — 12.5s is 14.0s (the confirmed ffprobe duration of both the source GIF and current output, per G-01-5's root_cause) minus the reported 1.5s of trailing dead content. Add a one-line comment directly above the manifest array noting the trim is Floor-0-specific (G-01-5) so a future reader doesn't assume it's a general pattern. Leave the `drag-rush`, `dispater`, and `swing-space` entries as plain 2-element arrays — they must NOT receive a third element or any duration cap.

Update `convertVideos()` to destructure the optional third element — `for (const [folder, basename, options] of videoAssets)` — and pass it through: `await gifToMp4(inputGif, outputMp4, options || {})`. `extractPoster()` needs no changes (it always grabs the first frame via `-vframes 1`, which an end-trim doesn't affect) and continues to run once per asset from the same loop.

Do not add CLI argument parsing, a `--only` flag, or any other selector to `main()` — this is a manifest/function-signature change only, not a pipeline refactor. `main()`, `convertScreenshots()`, and `toWebp()` stay untouched.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const src=fs.readFileSync('scripts/convert-media.js','utf8');const hasOptionParam=/function\s+gifToMp4\s*\(\s*inputGif\s*,\s*outputMp4\s*,\s*options\s*=\s*\{\}\s*\)/.test(src);const hasTrimFlag=src.includes('options.durationSeconds')&&src.includes('\"-t\"');const hasLoopThrough=src.includes('[folder, basename, options]')&&src.includes('options || {}');const lines=src.split('\n');const floorLine=lines.find(l=>l.includes('floor-0')&&l.includes('Floor0gif1'));const floorOk=!!floorLine&&floorLine.includes('durationSeconds')&&floorLine.includes('12.5');const otherLines=lines.filter(l=>(l.includes('drag-rush')&&l.includes('DragRushGif')&&!l.includes('DragRushGif.mp4')&&!l.includes('DragRushGif-poster'))||(l.includes('dispater')&&l.includes('DispaterGif2')&&!l.includes('DispaterGif2.mp4')&&!l.includes('DispaterGif2-poster'))||(l.includes('swing-space')&&l.includes('SwingSpaceGIF3')&&!l.includes('SwingSpaceGIF3.mp4')&&!l.includes('SwingSpaceGIF3-poster')));const othersOk=otherLines.length===3&&otherLines.every(l=>!l.includes('durationSeconds'));const ok=hasOptionParam&&hasTrimFlag&&hasLoopThrough&&floorOk&&othersOk;if(!ok){console.log('FAIL',{hasOptionParam,hasTrimFlag,hasLoopThrough,floorOk,othersOk,floorLine,otherLines});process.exit(1);}console.log('trim-capability-ok');"</automated>
  </verify>
  <done>gifToMp4() accepts an optional durationSeconds trim, the floor-0 videoAssets entry alone carries { durationSeconds: 12.5 }, convertVideos() passes the option through, and no CLI/main() refactor was introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate only Floor0gif1.mp4 at ~12.5s and verify the other 3 assets + source GIF are untouched</name>
  <files>public/img/projects/floor-0/Floor0gif1.mp4</files>
  <read_first>
    - scripts/convert-media.js (updated gifToMp4 export from Task 1)
  </read_first>
  <action>
Regenerate `Floor0gif1.mp4` only — do not run the full `node scripts/convert-media.js` pipeline (that would additionally re-encode the other 3 videos and reprocess all 16 screenshots for no reason; 01-01/D4 already established re-encoding is deterministic/equivalent, but redoing that work here is wasted time, not a requirement of this gap). Instead invoke the exported `gifToMp4` function directly for this single asset from the repo root: `node -e "require('./scripts/convert-media.js').gifToMp4('public/img/projects/floor-0/Floor0gif1.gif','public/img/projects/floor-0/Floor0gif1.mp4',{durationSeconds:12.5}).then(()=>console.log('done')).catch(e=>{console.error(e);process.exit(1)})"`. The existing `-y` flag inside `gifToMp4` overwrites the committed `Floor0gif1.mp4` in place.

Do not touch `Floor0gif1.gif` (the source master, D-03) or `Floor0gif1-poster.webp` (the poster only ever captures frame 1 via `-vframes 1`, which an end-trim never changes — it needs no regeneration). Do not re-run conversion for `DragRushGif`, `DispaterGif2`, or `SwingSpaceGIF3` — their `.mp4`/`-poster.webp` files must stay byte-identical to what's already committed.
  </action>
  <verify>
    <automated>node -e "const {execFileSync}=require('child_process');const dur=parseFloat(execFileSync('ffprobe',['-v','error','-show_entries','format=duration','-of','default=noprint_wrapper=1:nokey=1','public/img/projects/floor-0/Floor0gif1.mp4']).toString().trim());if(!(dur>=12.0&&dur<=13.0)){console.log('DURATION-FAIL',dur);process.exit(1);}const status=execFileSync('git',['status','--porcelain','--','public/img/projects']).toString();const lines=status.split('\n').filter(Boolean);const touched=lines.map(l=>l.slice(3).trim());const onlyFloor0Mp4=touched.every(f=>f==='public/img/projects/floor-0/Floor0gif1.mp4');const gifUntouched=!touched.includes('public/img/projects/floor-0/Floor0gif1.gif');if(!onlyFloor0Mp4||!gifUntouched){console.log('SCOPE-FAIL',touched);process.exit(1);}console.log('trim-ok',dur);"</automated>
  </verify>
  <done>ffprobe reports Floor0gif1.mp4's duration between 12.0s and 13.0s; git status under public/img/projects shows only Floor0gif1.mp4 as modified — the source .gif, the poster, and the other 3 video assets are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev shell -> ffmpeg process | The conversion script invokes an external binary with file-path and numeric-duration arguments (unchanged trust boundary from 01-01, now carrying one new argument). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | scripts/convert-media.js shelling to ffmpeg | low | mitigate | Carried forward unchanged from 01-01: `child_process.execFile` with an argument array, never `exec` with string concatenation. The new `-t` trim argument is pushed onto the same array (`args.push("-t", String(options.durationSeconds))`), preserving the mitigation. |
| T-01-06 | Tampering | gifToMp4()'s new `options.durationSeconds` parameter | low | mitigate | `durationSeconds` originates only from the hardcoded, developer-authored `videoAssets` manifest entry (`{ durationSeconds: 12.5 }`) — never from user input, CLI args, or an external source. `String(options.durationSeconds)` is pushed as one discrete `execFile` array element (never concatenated into a shell string), so no injection surface is introduced. |
</threat_model>

<verification>
- Task 1's automated check confirms `gifToMp4()`'s new signature, the `-t` trim wiring, and that only the floor-0 manifest entry carries `durationSeconds` (the other 3 entries are unchanged 2-element arrays).
- Task 2's automated check confirms `Floor0gif1.mp4`'s ffprobe duration is ~12.5s (12.0-13.0s tolerance) and that `git status` shows only that one file modified under `public/img/projects` — the source `.gif`, the poster, and the other 3 video assets are untouched.
</verification>

<success_criteria>
Floor 0's timeline video thumbnail loops at ~12.5s instead of 14.0s, closing UAT gap G-01-5. The trim is scoped to Floor 0 only — no other asset, source master, or pipeline behavior changes.
</success_criteria>

<output>
Create `.planning/phases/01-media-performance-optimization/01-04-SUMMARY.md` when done
</output>
