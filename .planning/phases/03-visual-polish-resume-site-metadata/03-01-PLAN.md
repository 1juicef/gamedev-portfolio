---
phase: 03-visual-polish-resume-site-metadata
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/GameProjects.vue
  - src/data/GameProjectsData.ts
  - scripts/convert-media.js
  - public/img/projects/dispater/DispaterGif.mp4
  - public/img/projects/dispater/DispaterGif-poster.webp
autonomous: true
requirements: [POLISH-01]

must_haves:
  truths:
    - "The timeline reads more restrained: row spacing snapped to the 4px scale (48px desktop row padding + timeline gap, 32px mobile row padding), project title tightened to 2.1em / line-height 1.1, summary line-height tightened to 1.7 (per D-01)"
    - "Timeline project titles carry a persistent underline affordance (border-bottom on .project-title-link) signalling clickability without needing hover, strengthening to the #f4cde6 accent on :hover/:focus-visible (per D-03)"
    - "The alternating left/right timeline row layout (project-row--reverse) is preserved, not restructured to a uniform single-side layout (per D-02)"
    - "Dispater's timeline thumbnail plays DispaterGif.mp4 (freshly converted from DispaterGif.gif); DispaterGif2's content now appears as a playable video inside the Dispater project overlay (per D-04)"
    - "Timeline renders all 4 projects populated — static hand-authored array, no empty/loading/error variant exists or is needed (UI-SPEC: populated)"
    - "All 4 project titles fit on one line at 2.1em on both the 620px mobile breakpoint and desktop — no wrap/truncation behaviour needed for this content set (UI-SPEC: long-text)"
    - "Timeline images stay capped within the row (width:100% + 58/42 flex split desktop, page .main max-width 1280px) with no ultra-wide overflow (UI-SPEC: overflow)"
    - "Timeline row cardinality is fixed at 4; a 5th+ project (game-jam additions) is explicitly out of scope this phase (UI-SPEC: zero-one-many)"
  prohibitions:
    - "MUST NOT add decorative surfaces or ornament during the restraint pass — no cards, drop shadows, glows, gradients, or new color tokens (e.g. @accentColor #6C3BAA). POLISH-01 restraint means tightening existing spacing/typography values, not adding decoration."
  artifacts:
    - path: src/views/GameProjects.vue
      provides: "Restrained timeline styling, persistent title click affordance, swapped Dispater timeline map values"
      contains: "2.1em"
    - path: src/data/GameProjectsData.ts
      provides: "Dispater overlay video embed carrying the former timeline gif content"
      contains: "DispaterGif2.mp4"
    - path: scripts/convert-media.js
      provides: "DispaterGif videoAssets manifest entry"
      contains: "\"DispaterGif\""
    - path: public/img/projects/dispater/DispaterGif.mp4
      provides: "Dispater timeline video (converted from DispaterGif.gif)"
    - path: public/img/projects/dispater/DispaterGif-poster.webp
      provides: "Dispater timeline poster frame"
  key_links:
    - from: "src/views/GameProjects.vue thumbVideos.dispater"
      to: "public/img/projects/dispater/DispaterGif.mp4"
      via: "LazyVideoThumbnail :src binding on the timeline"
      pattern: "DispaterGif\\.mp4"
    - from: "src/views/GameProjects.vue thumbPosters.dispater"
      to: "public/img/projects/dispater/DispaterGif-poster.webp"
      via: "LazyVideoThumbnail :poster binding on the timeline"
      pattern: "DispaterGif-poster\\.webp"
    - from: "src/data/GameProjectsData.ts Dispater htmlDescription <video>"
      to: "public/img/projects/dispater/DispaterGif2.mp4"
      via: "v-html overlay video source in ProjectDetailsOverlay"
      pattern: "DispaterGif2\\.mp4"
---

<objective>
Polish the GameProjects timeline for restraint (POLISH-01 / D-01), keep its signature alternating-row layout (D-02), give project titles a persistent click affordance (D-03), and swap Dispater's assets so DispaterGif.gif drives the timeline while DispaterGif2's content moves into the overlay (D-04).

Purpose: The timeline is the first thing a recruiter scans; snapping spacing/typography to the UI-SPEC scale and adding a visible click cue makes it read premium and discoverable without adding decoration. Dispater's asset swap is a folded todo Josef requested.
Output: Restyled `GameProjects.vue`, a new `DispaterGif.mp4`/`-poster.webp` pair from the existing media pipeline, remapped timeline asset values, and a Dispater overlay video embed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-visual-polish-resume-site-metadata/03-CONTEXT.md
@.planning/phases/03-visual-polish-resume-site-metadata/03-UI-SPEC.md
@.planning/phases/03-visual-polish-resume-site-metadata/03-PATTERNS.md
@src/views/GameProjects.vue
@src/data/GameProjectsData.ts
@scripts/convert-media.js
</context>

## Artifacts this phase produces

This plan EDITS existing files and generates two build-time assets. New symbols/values introduced:

| Kind | Symbol / value | Location |
|------|----------------|----------|
| Generated asset | `DispaterGif.mp4` | `public/img/projects/dispater/` |
| Generated asset | `DispaterGif-poster.webp` | `public/img/projects/dispater/` |
| Manifest entry | `["dispater", "DispaterGif"]` in `videoAssets` | `scripts/convert-media.js` |
| CSS | `border-bottom` affordance + `:focus-visible` rule on `.project-title-link` | `src/views/GameProjects.vue` `<style scoped>` |
| Map value change | `thumbVideos.dispater` / `thumbPosters.dispater` repointed to `DispaterGif` assets | `src/views/GameProjects.vue` `data()` |
| HTML (v-html) | `<video class="pc-video">` block sourcing `DispaterGif2.mp4` | `src/data/GameProjectsData.ts` Dispater `htmlDescription` |

No new CSS class is added — the overlay video reuses the existing `.pc-video` class from `src/css/projects.less`.

## Flagged Assumptions (edge-probe unresolved)

The spec-less edge probe classified POLISH-01 as `unclassified` / `unresolved`: its boundary/null/pagination/concurrency edge taxonomy does not map onto a spacing/typography/affordance requirement, so no edge-derived acceptance criteria were auto-generated. This is surfaced (not silently dropped) as an explicit assumption: **resolved-state coverage for POLISH-01 comes from the UI-SPEC `## UI Considerations` section (4 covered items, lifted into `must_haves.truths` above), plus the end-of-phase human read-through, not from the edge engine.**

<tasks>

<task type="auto">
  <name>Task 1: Timeline restraint pass + persistent title click affordance</name>
  <files>src/views/GameProjects.vue</files>
  <read_first>
    - src/views/GameProjects.vue — the `<style scoped>` block (lines ~120-264) is its own analog; edit values in place
    - .planning/phases/03-visual-polish-resume-site-metadata/03-UI-SPEC.md — Spacing Scale + Typography + Color (#f4cde6) tables hold the exact target tokens
    - .planning/phases/03-visual-polish-resume-site-metadata/03-PATTERNS.md — GameProjects.vue section shows the exact current→target CSS deltas
  </read_first>
  <action>
    Apply the UI-SPEC token deltas to the existing `<style scoped>` block of GameProjects.vue as value edits (keep `em` sizing, no `px` for typography — matches the codebase convention), implementing D-01 (restraint) and D-03 (affordance) while preserving D-02 (alternating `project-row--reverse` layout — do NOT restructure to a uniform single-side layout):
    (1) `.project-timeline`: change `gap: 40px` to `gap: 48px`.
    (2) `.project-row` (mobile/base): change `padding: 28px 0` to `padding: 32px 0`; leave `gap: 24px` unchanged (already on-scale).
    (3) `.project-title-link`: change `font-size: 2.35em` to `2.1em`; change `line-height: 1.05` to `1.1`; add `border-bottom: 1px solid rgba(255, 255, 255, 0.35)` as a persistent affordance; extend the existing `transition` list to also animate the underline color (add `border-bottom-color 0.18s ease` alongside the existing color/opacity transitions).
    (4) `.project-title-link:hover`: add a `:focus-visible` selector to the same rule and set `border-bottom-color: #f4cde6` (reuse the existing `#f4cde6` accent and existing `opacity: 1`; introduce no new color token — do NOT bring in `@accentColor`).
    (5) `.project-summary`: change `margin: 14px 0 0` to `16px 0 0`; change `line-height: 1.95em` to `1.7`.
    (6) In the `@media only screen and (min-width: 620px)` block, `.project-row`: change `padding: 40px 0` to `48px 0`; leave `gap: 48px` unchanged.
    Touch only spacing/typography/affordance values — add no cards, shadows, gradients, or extra decoration.
  </action>
  <verify>
    <automated>npm run lint 2>&1 | grep -qiv 'error' && grep -q '2.1em' src/views/GameProjects.vue && grep -q 'border-bottom-color' src/views/GameProjects.vue && grep -q 'focus-visible' src/views/GameProjects.vue</automated>
    <human-check>Run `npm run serve`, open the game-projects timeline: rows read tighter/more restrained top-to-bottom, project titles show a visible underline before hovering, and the underline brightens to pink on hover/keyboard focus. Alternating left/right rows are intact.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -q '2.1em' src/views/GameProjects.vue` succeeds (title size tightened per UI-SPEC Typography)
    - `grep -q 'border-bottom-color' src/views/GameProjects.vue` succeeds (D-03 underline color transitions on hover/focus)
    - `grep -q 'focus-visible' src/views/GameProjects.vue` succeeds (keyboard-focus affordance present)
    - `grep -q 'project-row--reverse' src/views/GameProjects.vue` still succeeds (D-02 alternating layout preserved)
    - `npm run lint` reports no errors (SFC still parses)
    - No new color token: `.project-title-link` hover/focus uses `#f4cde6`, and `@accentColor` / `#6C3BAA` is not introduced into the timeline style block
  </acceptance_criteria>
  <done>Timeline spacing/typography snapped to the UI-SPEC scale, titles carry a persistent underline affordance that accents on hover/focus, alternating layout preserved, no decoration added, lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Convert DispaterGif.gif to timeline mp4 + poster via the existing pipeline</name>
  <files>scripts/convert-media.js, public/img/projects/dispater/DispaterGif.mp4, public/img/projects/dispater/DispaterGif-poster.webp</files>
  <read_first>
    - scripts/convert-media.js — the `videoAssets` manifest (~lines 89-94) + `convertVideos()` (~lines 96-104); this is the established Phase 1 pipeline (`gifToMp4`/`extractPoster`)
    - .planning/phases/03-visual-polish-resume-site-metadata/03-PATTERNS.md — "Asset conversion" section shows the exact manifest-entry addition
  </read_first>
  <action>
    Implement the conversion half of D-04. Add a second Dispater entry `["dispater", "DispaterGif"]` to the `videoAssets` array in scripts/convert-media.js (alongside the existing `["dispater", "DispaterGif2"]` — do not remove or alter the DispaterGif2 entry; DispaterGif2.mp4 is still needed for the overlay). Do NOT write any new conversion function — this is purely a manifest-entry addition reusing the existing `gifToMp4`/`extractPoster` calls. Then run `node scripts/convert-media.js` (requires ffmpeg + sharp on PATH — the Phase-1 dev-machine tooling, already installed) to produce `public/img/projects/dispater/DispaterGif.mp4` and `public/img/projects/dispater/DispaterGif-poster.webp`. The source `public/img/projects/dispater/DispaterGif.gif` already exists.
  </action>
  <verify>
    <automated>grep -q '"DispaterGif"' scripts/convert-media.js && test -f public/img/projects/dispater/DispaterGif.mp4 && test -f public/img/projects/dispater/DispaterGif-poster.webp</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"DispaterGif"' scripts/convert-media.js` succeeds (new manifest entry present; matches `"DispaterGif"` not `"DispaterGif2"`)
    - `grep -q '"DispaterGif2"' scripts/convert-media.js` still succeeds (existing entry retained for overlay)
    - `test -f public/img/projects/dispater/DispaterGif.mp4` passes (timeline video generated)
    - `test -f public/img/projects/dispater/DispaterGif-poster.webp` passes (poster generated)
  </acceptance_criteria>
  <done>videoAssets manifest carries both Dispater entries, and DispaterGif.mp4 + DispaterGif-poster.webp exist on disk.</done>
</task>

<task type="auto">
  <name>Task 3: Swap Dispater timeline map values + embed DispaterGif2 in the overlay</name>
  <files>src/views/GameProjects.vue, src/data/GameProjectsData.ts</files>
  <read_first>
    - src/views/GameProjects.vue — the `data()` `thumbVideos` / `thumbPosters` id-keyed maps (~lines 84-95); value-only edit, keys/shape unchanged
    - src/data/GameProjectsData.ts — the Dispater `htmlDescription` (~lines 39-69, currently screenshots + youtube, no gif/video) and the SwingSpace overlay video pattern (~lines 95-100) to mirror
    - src/css/projects.less — the existing `.pc-video` class (~lines 26-29, 82-86) reused by the overlay video
  </read_first>
  <action>
    Implement the wiring half of D-04 (run AFTER Task 2 so the converted assets exist). In GameProjects.vue `data()`: change `thumbVideos.dispater` from the DispaterGif2 mp4 to `img/projects/dispater/DispaterGif.mp4`, and change `thumbPosters.dispater` from the DispaterGif2 poster to `img/projects/dispater/DispaterGif-poster.webp`. This is a value-only swap — leave the map keys, shape, and the template binding (`<LazyVideoThumbnail :src="thumbVideos[project.id]" :poster="thumbPosters[project.id]" />`) untouched.
    Then move DispaterGif2's content into the overlay: in src/data/GameProjectsData.ts, add one new `<div class="paragraph center">` block inside the Dispater `htmlDescription` (place it after the screenshots block, before the "About this game" block) containing a `<video class="pc-video" controls preload="metadata">` element whose `<source>` src is `img/projects/dispater/DispaterGif2.mp4` (type `video/mp4`), mirroring the SwingSpace overlay video markup. Reuse the existing `.pc-video` class — do NOT add a new CSS class, and do NOT remove the existing Dispater screenshots or YouTube trailer.
  </action>
  <verify>
    <automated>grep -q 'DispaterGif\.mp4' src/views/GameProjects.vue && grep -q 'DispaterGif-poster\.webp' src/views/GameProjects.vue && grep -q 'DispaterGif2\.mp4' src/data/GameProjectsData.ts && grep -q 'pc-video' src/data/GameProjectsData.ts && npm run lint 2>&1 | grep -qiv 'error'</automated>
    <human-check>Open the timeline: Dispater's thumbnail now plays the DispaterGif content. Click Dispater to open the overlay: the DispaterGif2 gameplay clip plays as a video alongside the existing screenshots and trailer.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -q 'DispaterGif\.mp4' src/views/GameProjects.vue` succeeds (timeline repointed; matches DispaterGif.mp4 not DispaterGif2.mp4)
    - `grep -q 'DispaterGif-poster\.webp' src/views/GameProjects.vue` succeeds (poster repointed)
    - `grep -q 'DispaterGif2\.mp4' src/data/GameProjectsData.ts` succeeds (overlay video source added)
    - `grep -q 'pc-video' src/data/GameProjectsData.ts` succeeds (video uses the reused overlay class)
    - Dispater screenshots (`DispaterSC1.webp`) and the itch.io link still present in GameProjectsData.ts (not removed)
    - `npm run lint` reports no errors
  </acceptance_criteria>
  <done>Dispater timeline plays DispaterGif.mp4, the overlay carries a DispaterGif2 video via the reused .pc-video class, screenshots/trailer/link intact, lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author source → v-html overlay | Dispater `htmlDescription` (hand-authored TS string) is rendered via `v-html` in ProjectDetailsOverlay; this task adds a static `<video>` element to it |
| dev-machine build pipeline | `scripts/convert-media.js` runs ffmpeg/sharp locally at build time to produce assets (not part of the shipped app runtime) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | v-html render of Dispater `htmlDescription` (new `<video>` embed) | low | accept | Content is a hand-authored static string in TS source, not user-supplied input — no injection vector is introduced; this mirrors the pre-existing SwingSpace overlay video pattern |
| T-03-02 | Tampering | `scripts/convert-media.js` (ffmpeg/sharp) | low | accept | No new npm/pip/cargo dependency is added this phase — only a manifest-entry addition reusing Phase-1 tooling already vetted; build-time only, no runtime exposure |
| T-03-SC | Tampering | package installs | low | accept | No package installs occur in this plan (manifest-entry addition only) — supply-chain surface unchanged from Phase 1 |
</threat_model>

<verification>
- `npm run lint` passes with no errors after all three tasks
- `grep` assertions in each task's acceptance_criteria all pass
- Generated assets `DispaterGif.mp4` + `DispaterGif-poster.webp` exist
- End-of-phase human read-through confirms the timeline reads restrained/premium, titles show the click affordance, and Dispater's swap is correct (per human_verify_mode=end-of-phase)
</verification>

<success_criteria>
Timeline spacing/typography snapped to the UI-SPEC 4px scale (POLISH-01/D-01), alternating layout preserved (D-02), project titles carry a persistent underline affordance accenting on hover/focus (D-03), Dispater timeline plays DispaterGif.mp4 with DispaterGif2 relocated to the overlay (D-04), no decoration added, lint clean.
</success_criteria>

<output>
Create `.planning/phases/03-visual-polish-resume-site-metadata/03-01-SUMMARY.md` when done
</output>
