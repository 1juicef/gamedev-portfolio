---
phase: 05-dark-theme-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/css/variables.less
  - src/App.vue
  - src/components/ProjectDetailsOverlay.vue
  - src/css/projects.less
autonomous: true
requirements: [REDESIGN-01]

must_haves:
  truths:
    - "Across every route the site renders the dark-gradient background and Lekton/Russo One custom fonts, committed (not a working-tree-only change)."
    - "Opening any of the four project overlays shows the reworked dialog styling (dark dialog background, gradient content panel, light title) rendered against the dark palette."
    - "Overlay content links (inside .dialog-content, rendered from v-html) display in a light, legible color — not the old dim gray #696969 and not the accent purple #6C3BAA (D-01)."
    - "The dark palette is defined as named Less variables in variables.less and referenced by App.vue and ProjectDetailsOverlay.vue; no raw gradient/dialog hex literals remain in those two component style blocks (D-02)."
    - "`npm run build` completes successfully — a production build reflects the dark theme and every @token reference resolves at Less compile time (success criterion 4)."
    - "[FLAGGED — unresolved, specless probe] REDESIGN-01 is a one-time 'commit the existing diff as-is' state change; the deterministic edge-probe engine returned it unclassified/unresolved (no edge-case behavior to classify). Assumption carried unverified: committing the existing uncommitted redesign diff plus the D-01/D-02 formalizations fully satisfies REDESIGN-01, with no hidden edge behavior. Flag surfaced for human confirmation; NOT auto-backstopped."
  artifacts:
    - "src/css/variables.less — new flat palette tokens (@gradientStart, @gradientMid, @gradientEnd, @dialogBgColor) appended in the existing @name: #hex; style."
    - "src/App.vue — html/body gradient literal replaced with @gradientStart/@gradientMid/@gradientEnd token references."
    - "src/components/ProjectDetailsOverlay.vue — style block switched to lang=less with an @import of variables.less; .dialog / .dialog-title / .dialog-content / .dialog-close-button color and background literals replaced with @dialogBgColor / @textColor / gradient tokens."
    - "src/css/projects.less — .dialog-content a link color changed from #696969 to a light off-white."
  key_links:
    - "App.vue <style lang=less> already @imports ./css/variables.less (line 41) — new tokens resolve there without a new import."
    - "ProjectDetailsOverlay.vue <style scoped> is currently plain CSS with NO vue.config.js additionalData injection (confirmed: no vue.config.js in repo) — it MUST become <style scoped lang=less> AND @import '../css/variables.less' or its @token references fail to compile. This is the single most likely break point."
    - "variables.less token definitions must be present in the same build so both consuming files' @references resolve (single atomic commit; Less resolves the shared @imports lazily)."
  prohibitions:
    - "[flagged-unverified] Overlay content link color MUST NOT be set to @accentColor / #6C3BAA — D-01 explicitly excludes the accent purple; the link must read as light (white/off-white family)."
    - "[flagged-unverified] Do NOT change the vue-router mode or webpack publicPath while making redesign edits — untouched by this phase; flagged research pitfall (D-04) that would silently break deep links / the live URL."
    - "[flagged-unverified] Do NOT expand scope into a full-site contrast/readability QA pass or restyle routes/components beyond the four target files — Josef explicitly declined this (D-03); land the diff + D-01/D-02 and stop."
    - "[flagged-unverified] Do NOT restructure variables.less into nested rules, maps, or an @import chain — keep the flat @name: #hex; one-per-line list (PATTERNS convention)."
---

<objective>
Commit the uncommitted dark-theme redesign (dark-gradient background, self-hosted Lekton/Russo One fonts, reworked overlay dialog styling in `App.vue`/`ProjectDetailsOverlay.vue`) as the site's final palette baseline, applying two formalizations from CONTEXT.md before the commit:

- **D-01** — fix the overlay content link color (`.dialog-content a` in `projects.less`) from the old dim gray `#696969` (tuned for the previous light card) to a light off-white so links stay legible on the new dark dialog. Not accent purple.
- **D-02** — formalize the hardcoded dark-palette literals (gradient stops, dialog background) as named Less variables in `variables.less` and reference them from both component style blocks, giving Phase 6 a clean named reference for code-block backgrounds.

Purpose: Establish the finalized dark visual identity as the committed baseline Phase 6's Technical Overview styling is authored against (avoids restyling twice).
Output: Four edited files (`variables.less`, `App.vue`, `ProjectDetailsOverlay.vue`, `projects.less`) committed together, plus a passing production build proving the theme renders and all token references resolve.

Fulfills: REDESIGN-01.
</objective>

<artifacts_produced>
## Artifacts this phase produces

Symbols/changes this phase newly creates or modifies (source-grounding reference — these are created here, not pre-existing drift):

- **New Less variables in `src/css/variables.less`:** `@gradientStart` (`#2b123f`), `@gradientMid` (`#120818`), `@gradientEnd` (`#000000`), `@dialogBgColor` (`#000000`). Appended to the existing flat token list; do not rename or remove the existing `@bodyBgColor`/`@contentBgColor`/`@textColor`/`@skillRateCircleColor`/`@accentColor`.
- **`src/App.vue` `<style lang="less">`:** the `html, body` `background` gradient literal is replaced with a `@gradientStart`/`@gradientMid`/`@gradientEnd` token reference. No new `@import` (variables.less already imported at line 41).
- **`src/components/ProjectDetailsOverlay.vue` `<style scoped>`:** opening tag becomes `<style scoped lang="less">`; a new first line `@import '../css/variables.less';` is added; the `.dialog`, `h1.dialog-title`, `.dialog-content`, and `a.dialog-close-button` color/background literals are replaced with `@dialogBgColor` / `@textColor` / gradient-token references.
- **`src/css/projects.less` `.dialog-content a` rule:** `color:#696969;` replaced with a light off-white literal.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dark-theme-redesign/05-CONTEXT.md
@.planning/phases/05-dark-theme-redesign/05-PATTERNS.md

@src/css/variables.less
@src/App.vue
@src/components/ProjectDetailsOverlay.vue
@src/css/projects.less
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dark-palette Less variables to variables.less (D-02)</name>
  <read_first>
    - src/css/variables.less (the file being modified — the full 5-line existing token list is the pattern to extend)
    - .planning/phases/05-dark-theme-redesign/05-CONTEXT.md (D-02 — color token strategy)
    - .planning/phases/05-dark-theme-redesign/05-PATTERNS.md (variables.less section — flat @camelCaseColor: #hex; naming convention, and the "do not introduce nesting/maps/@import restructure" constraint)
  </read_first>
  <files>src/css/variables.less</files>
  <action>
    Append four new tokens to variables.less, one per line, in the existing flat `@name: #hex;` style (no comments, no grouping, no nesting): `@gradientStart: #2b123f;`, `@gradientMid: #120818;`, `@gradientEnd: #000000;`, `@dialogBgColor: #000000;`. These values are lifted verbatim from the existing uncommitted literals — `@gradientStart`/`@gradientMid`/`@gradientEnd` are the three App.vue body-gradient stops (top→bottom), and `@dialogBgColor` is the overlay dialog background. Leave all five existing tokens (`@bodyBgColor`, `@contentBgColor`, `@textColor`, `@skillRateCircleColor`, `@accentColor`) exactly as-is — do not rename, reorder, or remove them. `@dialogBgColor` is kept as a distinct semantic token (rather than reusing `@bodyBgColor`) so Phase 6 can reference the dialog/code-block background by name.
  </action>
  <verify>
    <automated>grep -cE '@(gradientStart|gradientMid|gradientEnd|dialogBgColor):' src/css/variables.less</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: variables.less contains all four new tokens `@gradientStart`, `@gradientMid`, `@gradientEnd`, `@dialogBgColor` (grep count == 4).
    - Source assertion: the five pre-existing tokens are still present and unchanged.
    - Structural assertion: no nested rules, LESS maps, or new `@import` were introduced — the file remains a flat one-declaration-per-line list.
  </acceptance_criteria>
  <done>Four new palette tokens exist in variables.less in the flat existing style; existing tokens untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Reference palette tokens from App.vue and ProjectDetailsOverlay.vue (D-02)</name>
  <read_first>
    - src/App.vue (the `<style lang="less">` block, lines 38-161 — note line 41 already `@import './css/variables.less';`, line 74 is the gradient literal to replace)
    - src/components/ProjectDetailsOverlay.vue (the `<style scoped>` block, lines 34-119 — currently plain CSS with hardcoded hex literals)
    - src/css/variables.less (for the exact new token names created in Task 1)
    - .planning/phases/05-dark-theme-redesign/05-PATTERNS.md (App.vue and ProjectDetailsOverlay.vue sections — exact literals-to-replace map, and the note that the overlay needs an @import added)
  </read_first>
  <files>src/App.vue, src/components/ProjectDetailsOverlay.vue</files>
  <action>
    In `src/App.vue`, replace the `html, body` background gradient literal `linear-gradient(180deg, #2b123f 0%, #120818 45%, #000000 100%)` with `linear-gradient(180deg, @gradientStart 0%, @gradientMid 45%, @gradientEnd 100%)`. No new import needed — the block already `@import`s variables.less at line 41. Change nothing else in App.vue (leave the `@font-face` declarations, `.main` padding, and existing `color: @textColor` usages alone).

    In `src/components/ProjectDetailsOverlay.vue`, first make its style block Less-capable: change the opening tag from `<style scoped>` to `<style scoped lang="less">` and add `@import '../css/variables.less';` as the first line inside the block. This is mandatory — there is no `vue.config.js` in the repo (verified), so no `additionalData` injects the shared variables globally; the block is presently plain CSS and `@token` references will not compile without both the `lang` switch and the explicit import. The import path is `../css/variables.less` because the component lives in `src/components/` and the tokens live in `src/css/`. Then replace the literals: `.dialog` `background-color: #000000;` → `@dialogBgColor` and `.dialog` `color:white;` → `@textColor`; `h1.dialog-title` `color: #ffffff;` → `@textColor` and `background-color: #000000;` → `@dialogBgColor`; `.dialog-content` `background: linear-gradient(180deg, #000000 0%, #120818 45%, #2b123f 100%);` → `linear-gradient(180deg, @gradientEnd 0%, @gradientMid 45%, @gradientStart 100%)` (the overlay gradient is intentionally the reverse stop order of App.vue's body gradient — preserve that reversal, reusing the same three tokens) and `.dialog-content` `color: #ffffff;` → `@textColor`; `a.dialog-close-button` `color: #ffffff;` → `@textColor`. Do not alter layout, spacing, the media query, or the `.overlay`/`.dialog-close` rules.
  </action>
  <verify>
    <automated>grep -cE '@(gradientStart|gradientMid|gradientEnd)' src/App.vue && grep -q 'lang="less"' src/components/ProjectDetailsOverlay.vue && grep -q "@import '../css/variables.less'" src/components/ProjectDetailsOverlay.vue && grep -cE '@(dialogBgColor|textColor|gradientStart|gradientEnd|gradientMid)' src/components/ProjectDetailsOverlay.vue</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: App.vue's `html, body` background references `@gradientStart`, `@gradientMid`, and `@gradientEnd` (grep count >= 1 each); the raw `#2b123f`/`#120818` gradient literal no longer appears in App.vue.
    - Source assertion: ProjectDetailsOverlay.vue's style tag includes `lang="less"` and the block's first line is `@import '../css/variables.less';`.
    - Source assertion: ProjectDetailsOverlay.vue's `.dialog`, `.dialog-title`, `.dialog-content`, and `.dialog-close-button` reference `@dialogBgColor`/`@textColor`/gradient tokens instead of `#000000`/`#ffffff` literals.
    - Behavior assertion: the reversed gradient stop order in `.dialog-content` is preserved (`@gradientEnd` at 0%, `@gradientStart` at 100%).
  </acceptance_criteria>
  <done>Both component style blocks reference the new tokens; the overlay block compiles as Less with the variables import; no dialog/gradient hex literals remain in either block.</done>
</task>

<task type="auto">
  <name>Task 3: Fix overlay link color (D-01) and verify production build</name>
  <read_first>
    - src/css/projects.less (the `.dialog-content a` rule, lines 63-67 — the single color value to change)
    - .planning/phases/05-dark-theme-redesign/05-CONTEXT.md (D-01 — overlay link color must be light, not dim gray, not accent purple)
    - .planning/phases/05-dark-theme-redesign/05-PATTERNS.md (projects.less section — endorses a self-contained literal off-white here rather than importing @textColor)
  </read_first>
  <files>src/css/projects.less</files>
  <action>
    In `src/css/projects.less`, change the `.dialog-content a` rule's current dim-gray link color (the existing hex value shown at lines 63-67 of that file — read it there) to `color: #e0e0e0;` — a light off-white per D-01: legible on the dark gradient dialog and visually distinct from the pure-white body text. The link color must NOT be set to the accent purple (`@accentColor`, the `#6C…` value in variables.less) — D-01 explicitly excludes it. Keep it a self-contained literal — do not add an `@import` here; projects.less stays free of the shared palette (laziest correct option per PATTERNS, and it avoids the cross-file Less-scope question entirely). Leave `opacity: 1;`, `text-decoration: underline;`, and the `a:hover` rule unchanged. Then run the production build to confirm the whole redesign compiles and every `@token` reference across variables.less / App.vue / ProjectDetailsOverlay.vue resolves (an undefined Less variable fails the build).
  </action>
  <verify>
    <automated>grep -c '#696969' src/css/projects.less; grep -c '#6C3BAA' src/css/projects.less; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `#696969` no longer appears in projects.less (grep count == 0) and the `.dialog-content a` rule now uses a light off-white (`#e0e0e0`).
    - Prohibition assertion: `#6C3BAA` (accent purple) does not appear in projects.less (grep count == 0) — the link color is not the accent (D-01).
    - Command assertion: `npm run build` exits 0 and produces `dist/` — proving all `@token` references resolve at Less compile time and the production build reflects the dark theme (success criterion 4).
  </acceptance_criteria>
  <done>Overlay content links render light off-white; production build succeeds with all palette tokens resolving.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ← same-origin static assets (self-hosted Lekton/Russo One TTF fonts, compiled CSS) | Fonts and styles are served from the site's own origin via `url('/fonts/...')` local paths; this phase introduces no third-party/CDN font or stylesheet fetch. |
| project `htmlDescription` → `v-html` in `.dialog-content` | Pre-existing developer-authored static HTML rendered via `v-html`; unchanged by this phase (D-01 only restyles the link color, not the rendered HTML source). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | `@font-face` `src: url('/fonts/...')` (App.vue) | low | accept | Fonts are local same-origin TTF files (already in the working tree); no external/CDN request is introduced, so no referrer/data leaves the origin. No new externally-loaded resource added this phase — confirmed. |
| T-05-02 | Tampering (XSS) | `.dialog-content` `v-html="htmlContent"` (ProjectDetailsOverlay.vue) | low | accept | The `v-html` data flow is unchanged this phase; content is static developer-authored strings from `GameProjectsData.ts`/`OtherProjectsData.ts`, not user input. D-01 restyles `.dialog-content a` color only and does not alter what HTML is rendered — no new user-controlled data reaches `v-html`. |
| T-05-03 | Tampering (supply chain) | npm/pip/cargo installs | low | accept | No package installs or dependency changes occur in this phase — it is pure CSS/Less/font-reference edits. No new package to audit; the Package Legitimacy Gate does not apply. |

ASVS level: 1. Block threshold: high. No high-severity threats identified — the block condition is satisfied.
</threat_model>

<verification>
## Phase Verification

**Automated (gating):**
- `npm run build` exits 0 and emits `dist/` — the Less compiles end-to-end, all four new `@token` references resolve, and the production build carries the dark theme (proves the redesign is committed rather than working-tree-only).
- `grep -c '#696969' src/css/projects.less` == 0 (old dim-gray link color gone).
- `grep -c '#6C3BAA' src/css/projects.less` == 0 (link color is not the accent purple — D-01).
- variables.less contains `@gradientStart`, `@gradientMid`, `@gradientEnd`, `@dialogBgColor`.
- ProjectDetailsOverlay.vue's style block is `lang="less"` and imports `../css/variables.less`.

**Manual (non-blocking sanity glance — NOT a QA pass; Josef declined a full-site QA per D-03):**
- Optional: `npm run serve`, open the site, click into any of the four project overlays, confirm the dark dialog + light link color render. Since the redesign diff is Josef's own already-authored work, this is a courtesy glance, not a gate.
- Optional (D-04 known pitfall, not a required check): deep-link directly to `/game-projects` after a build to confirm router mode was left untouched.

**Flagged assumption (specless probe — unresolved, surfaced not backstopped):**
- REDESIGN-01 returned `unclassified/unresolved` from the deterministic edge-probe engine (expected for a one-time "commit as-is" state change). The carried assumption — that committing the existing diff plus D-01/D-02 fully satisfies REDESIGN-01 with no hidden edge behavior — is flagged for human confirmation, not auto-verified.
</verification>

<success_criteria>
- variables.less defines `@gradientStart`/`@gradientMid`/`@gradientEnd`/`@dialogBgColor` in the flat existing style; existing tokens untouched.
- App.vue's body gradient and ProjectDetailsOverlay.vue's `.dialog`/`.dialog-title`/`.dialog-content`/`.dialog-close-button` colors reference the tokens — no dialog/gradient hex literals remain in those two style blocks.
- ProjectDetailsOverlay.vue's style block compiles as Less (`lang="less"` + `@import '../css/variables.less';`).
- `.dialog-content a` link color is a light off-white (`#e0e0e0`), not `#696969`, not `#6C3BAA`.
- `npm run build` succeeds — the dark theme is committed and production-buildable (REDESIGN-01, success criterion 4).
</success_criteria>

<output>
Create `.planning/phases/05-dark-theme-redesign/05-01-SUMMARY.md` when done.
</output>
