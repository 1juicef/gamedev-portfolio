---
phase: 04-game-jams-section
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/GameProjects.vue
autonomous: true
requirements: [GAMEJAMS-01, GAMEJAMS-02, GAMEJAMS-03]
must_haves:
  truths:
    - "A 'Game Jams' subheading is visible at the bottom of /game-projects, below the four existing project rows — GAMEJAMS-01"
    - "Two text hyperlinks reading exactly 'The Eldritch Keeper' and 'Mas-Q' appear under the Game Jams subheading — GAMEJAMS-02"
    - "Clicking 'The Eldritch Keeper' opens https://juice-f.itch.io/the-eldritch-keeper in a new browser tab — GAMEJAMS-03"
    - "Clicking 'Mas-Q' opens https://superguardian.itch.io/mas-q in a new browser tab — GAMEJAMS-03"
    - "Both Game Jam links carry rel=\"noopener noreferrer\" so the opened tab cannot reach back into window.opener (reverse-tabnabbing mitigation) — T-04-01"
  prohibitions:
    - "Must not render the Game Jam entries as ProjectData-style cards, timeline rows, or click-through overlays — the whole scope is a subheading + two plain text links (REQUIREMENTS.md Out of Scope)."
    - "Must not add screenshots, gifs, videos, thumbnails, or any media/asset for the Game Jam games (REQUIREMENTS.md Out of Scope)."
    - "Must not make the links open in the same tab/window — each must open its itch.io page in a new browser tab (GAMEJAMS-03)."
  artifacts:
    - "src/views/GameProjects.vue (modified in place — new Game Jams <section> after .project-timeline, plus scoped styles)"
  key_links:
    - "The new Game Jams <section> renders as a sibling immediately after the .project-timeline closing </div> and before <ProjectDetailsOverlay> in the template, so it appears at the bottom of the page below all 4 project rows."
    - "Each <a> uses target=\"_blank\" + rel=\"noopener noreferrer\"; both hrefs are hardcoded string literals (the only two external destinations), so no dynamic or user-controllable href exists."
---

<objective>
Give two game-jam games (The Eldritch Keeper, Mas-Q) a lightweight presence at the bottom of the `/game-projects` timeline: a "Game Jams" subheading plus two plain-text hyperlinks out to itch.io. No project cards, screenshots, videos, or overlays — deliberately lighter than the four main projects.

Purpose: The two jam games get discoverable links on the timeline without the full project-card treatment (GAMEJAMS-01/02/03).
Output: In-place edits to one existing file — a new `<section>` in the template plus a small scoped-style block. No new files, components, data files, or exported symbols.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/views/GameProjects.vue
@src/views/Contact.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Game Jams subheading + two itch.io text links to the timeline</name>
  <files>src/views/GameProjects.vue</files>
  <read_first>
    - src/views/GameProjects.vue (the file being edited — the `.project-timeline` `<div>` closes at line 51 and `<ProjectDetailsOverlay` opens at line 53; the `<style scoped>` block runs lines 121-268; the existing `.project-title-link` uses `font-weight: 100` and hovers to color `#f4cde6` with a bottom border — reuse that hover accent so the new links feel native)
    - src/views/Contact.vue (the closest existing analog — plain `<a href="..." target="_blank">text</a>` links, e.g. the `juice-f.itch.io` link on line 23; match this plain-text-link style rather than the itch-badge image pattern in GameProjectsData.ts)
  </read_first>
  <action>
    In `src/views/GameProjects.vue`, add a new `<section class="game-jams">` in the template, placed AFTER the `.project-timeline` `</div>` closes (currently line 51) and BEFORE the `<ProjectDetailsOverlay ...>` component (currently line 53) — so it renders at the very bottom of the page, below all four project rows (GAMEJAMS-01).

    Inside that section put exactly two things: (1) a subheading element `<h2 class="game-jams-title">` whose text is exactly `Game Jams`; and (2) a `<div class="game-jam-links">` containing exactly two anchor elements, in this source order:
      - first anchor: class `game-jam-link`, `href="https://juice-f.itch.io/the-eldritch-keeper"`, `target="_blank"`, `rel="noopener noreferrer"`, visible text exactly `The Eldritch Keeper`;
      - second anchor: class `game-jam-link`, `href="https://superguardian.itch.io/mas-q"`, `target="_blank"`, `rel="noopener noreferrer"`, visible text exactly `Mas-Q`.

    The link text (GAMEJAMS-02) and the two URLs (GAMEJAMS-03) are locked strings — copy them character-for-character. Both anchors MUST carry `rel="noopener noreferrer"` (this is the reverse-tabnabbing mitigation T-04-01 — even though the four existing itch.io links in this project omit it, the new links add it; do not omit it to "match" the old links).

    Do NOT reuse the ProjectData / `showDetails` / `<LazyVideoThumbnail>` / `.project-row` machinery for these entries, and do NOT add any `<img>`, `<video>`, `<button>`, or thumbnail — the section holds only the `<h2>` and the two `<a>` links (REQUIREMENTS.md Out of Scope).

    Then add a small scoped-style block in the existing `<style scoped>` section for the three new classes: `.game-jams` should sit below the timeline with clear separation (give it a top border and top padding/margin consistent with the `border-top: 1px solid rgba(255,255,255,0.14)` + spacing already used on `.project-row`, and respect the `.journey-page` bottom margin); `.game-jams-title` should read as a section subheading in the same light aesthetic as the page (e.g. `font-weight: 100`, sized smaller than the hero `<h1>` but clearly a heading); `.game-jam-links` should lay the two links out readably (stacked or spaced inline); `.game-jam-link` should look like a text hyperlink (inherit color, underline or bottom border) and on `:hover`/`:focus-visible` adopt the same `#f4cde6` accent used by `.project-title-link`. Keep the styling minimal and consistent with the surrounding component — no new color variables, no new global styles, no edits to `src/css/projects.less`.
  </action>
  <verify>
    <automated>grep -Fq 'Game Jams' src/views/GameProjects.vue && grep -Fq 'The Eldritch Keeper' src/views/GameProjects.vue && grep -Fq 'Mas-Q' src/views/GameProjects.vue && grep -Fq 'https://juice-f.itch.io/the-eldritch-keeper' src/views/GameProjects.vue && grep -Fq 'https://superguardian.itch.io/mas-q' src/views/GameProjects.vue && [ "$(grep -Fc 'rel="noopener noreferrer"' src/views/GameProjects.vue)" -ge 2 ] && [ "$(grep -Fc 'target="_blank"' src/views/GameProjects.vue)" -ge 2 ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Fq 'Game Jams' src/views/GameProjects.vue` matches — the subheading text is present (GAMEJAMS-01).
    - The new `<section class="game-jams">` (with its `<h2>` subheading) appears AFTER the `.project-timeline` `</div>` and BEFORE `<ProjectDetailsOverlay` in the template — i.e. it renders at the bottom, below all four `.project-row` rows (GAMEJAMS-01).
    - `grep -Fq 'The Eldritch Keeper' src/views/GameProjects.vue` and `grep -Fq 'Mas-Q' src/views/GameProjects.vue` both match, each as the visible text of an anchor (GAMEJAMS-02).
    - `grep -Fq 'https://juice-f.itch.io/the-eldritch-keeper' src/views/GameProjects.vue` matches and that URL is the `href` of the anchor whose text is `The Eldritch Keeper` (GAMEJAMS-03).
    - `grep -Fq 'https://superguardian.itch.io/mas-q' src/views/GameProjects.vue` matches and that URL is the `href` of the anchor whose text is `Mas-Q` (GAMEJAMS-03).
    - `grep -Fc 'rel="noopener noreferrer"' src/views/GameProjects.vue` returns >= 2 — both new links carry the tabnabbing mitigation (T-04-01).
    - `grep -Fc 'target="_blank"' src/views/GameProjects.vue` returns >= 2 — both new links open in a new tab (GAMEJAMS-03). (This file had zero `target="_blank"` before this change, so the count comes entirely from the two new links.)
    - The `.game-jams` `<section>` contains only the `<h2>` subheading and the two `<a class="game-jam-link">` anchors — no `<img>`, `<video>`, `<button>`, `<LazyVideoThumbnail>`, thumbnail, card wrapper, or `showDetails`/overlay click handler is inside it (prohibitions — plain text links only).
    - `npm run lint` passes over `src/views/GameProjects.vue` with no new errors, and `npm run build` compiles the template.
  </acceptance_criteria>
  <done>The bottom of `/game-projects` shows a "Game Jams" subheading below the four project rows, with two text hyperlinks "The Eldritch Keeper" and "Mas-Q" that each open their itch.io page in a new tab with `rel="noopener noreferrer"`. No cards, media, thumbnails, or overlay treatment were added, and only `src/views/GameProjects.vue` changed.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

This plan creates NO new files, components, exported symbols, data fields, or global styles. It performs an in-place additive edit to one pre-existing file:

- `src/views/GameProjects.vue`
  - New template block: `<section class="game-jams">` — containing one `<h2 class="game-jams-title">Game Jams</h2>` subheading and one `<div class="game-jam-links">` wrapping two `<a class="game-jam-link">` anchors (The Eldritch Keeper → `https://juice-f.itch.io/the-eldritch-keeper`; Mas-Q → `https://superguardian.itch.io/mas-q`), both with `target="_blank"` + `rel="noopener noreferrer"`.
  - New scoped CSS classes: `.game-jams`, `.game-jams-title`, `.game-jam-links`, `.game-jam-link` (plus the `.game-jam-link:hover` / `:focus-visible` accent state).

No new routes, no new data files, no changes to `ProjectData` / `GameProjectsData.ts` / `ProjectsList.vue` / `ProjectDetailsOverlay.vue`, and no edits to `src/css/projects.less`.
</artifacts_produced>

<flagged_assumptions>
## Spec-less edge-probe items (all 5 surfaced — no silent drops)

No SPEC.md exists for this phase, so the deterministic edge probe ran against the requirement text and returned all 5 items as `unresolved` (the generic engine is algorithm/data-structure shaped and does not map onto plain static-content requirements). Per the no-silent-drop rule each is recorded here as an explicit flagged assumption rather than dropped. It is expected and defensible that a phase this simple resolves zero probe-covered edges.

- **FA-1 — GAMEJAMS-01 (unclassified, unresolved):** The engine could not classify "subheading at the bottom of the timeline." Manual review: "bottom" is satisfied by placing the `<section>` as a sibling immediately after the `.project-timeline` `</div>` (and before the overlay component). **Assumption:** DOM/source position after the last `.project-row` == visually "at the bottom, below the four rows." No hidden algorithmic edge applies.
- **FA-2 — GAMEJAMS-02 (unclassified, unresolved):** The engine could not classify "two hyperlinks reading exactly X and Y." Manual review: this is an exact-string requirement with no data-structure edge. **Assumption:** the visible link text is the literal string with no truncation, ellipsis, casing change, or added decoration.
- **FA-3 — GAMEJAMS-03 (adjacency, unresolved):** "When two things are exactly equal or just touch, do they merge/collide/separate?" — N/A: there are exactly two distinct static anchors to two distinct URLs; static `<a>` elements have no merge/collision semantics. **Assumption:** the two links are fully independent; adjacency behavior is not applicable.
- **FA-4 — GAMEJAMS-03 (empty, unresolved):** "Result for empty/single/null input?" — N/A: the link set is hardcoded (always exactly two anchors), never sourced from a dynamic/empty/null collection. **Assumption:** no empty-state or single-element handling is needed because there is no data source — the two links are static markup.
- **FA-5 — GAMEJAMS-03 (ordering, unresolved):** "When elements compare equal, is output order specified and stable?" — N/A: the two anchors are authored in fixed source order (The Eldritch Keeper first, Mas-Q second); nothing is sorted or computed. **Assumption:** display order is the authored source order and is stable by construction.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| site → external site (itch.io) | Two new outbound `target="_blank"` links from the `/game-projects` page to third-party itch.io pages open in a user-controlled new tab. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering (reverse tabnabbing) | New `<a target="_blank">` Game Jam links in `GameProjects.vue` | low | mitigate | Add `rel="noopener noreferrer"` to both anchors so the opened itch.io tab cannot access `window.opener` or read the referrer. Enforced by the acceptance criterion `grep -Fc 'rel="noopener noreferrer"' >= 2`. |
| T-04-02 | Injection / Tampering (arbitrary href) | The two itch.io `href` values | low | accept | Both `href` values are hardcoded string literals authored directly in the template — there is no user input, no route param, and no dynamic interpolation that could inject or redirect to an attacker-controlled URL. The two locked itch.io URLs are the only external destinations introduced. |

**Supply chain:** No package-manager installs occur in this phase — the supply-chain checkpoint (T-04-SC) is not applicable, and no RESEARCH.md Package Legitimacy Audit is required.

ASVS level 1, block-on-high: the only mitigated threat (T-04-01) is low severity and fully addressed; no high/critical threats exist in scope.
</threat_model>

<verification>
- Task 1 `<automated>` check prints `PASS`.
- `npm run lint` passes over `src/views/GameProjects.vue` (no new lint errors).
- `npm run build` succeeds (the `.vue` template still compiles).
- Manual/UAT spot check (deferred to phase verification): on `/game-projects`, the "Game Jams" subheading is visible below the four project rows, and clicking each link opens the correct itch.io page in a new tab.
</verification>

<success_criteria>
- GAMEJAMS-01: A "Game Jams" subheading is visible at the bottom of the `/game-projects` timeline, below the four existing project rows.
- GAMEJAMS-02: Two text hyperlinks reading exactly "The Eldritch Keeper" and "Mas-Q" appear under that subheading.
- GAMEJAMS-03: "The Eldritch Keeper" opens `https://juice-f.itch.io/the-eldritch-keeper` and "Mas-Q" opens `https://superguardian.itch.io/mas-q`, each in a new browser tab.
- Scope held: no project cards, screenshots, videos, thumbnails, or overlay treatment were added; only `src/views/GameProjects.vue` changed.
</success_criteria>

<output>
Create `.planning/phases/04-game-jams-section/04-01-SUMMARY.md` when done.
</output>
