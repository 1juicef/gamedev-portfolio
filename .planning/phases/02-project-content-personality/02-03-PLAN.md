---
phase: 02-project-content-personality
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/GameProjects.vue
  - src/data/GameProjectsData.ts
  - src/css/projects.less
  - src/components/ProjectDetailsOverlay.vue
autonomous: true
gap_closure: true
gap_ids: [G-02-1, G-02-2, G-02-3, G-02-4, G-02-5]
requirements: [CONT-02, CONT-03, CONT-04, CONT-05, POLISH-02]

must_haves:
  truths:
    - "Hero bio says 'Arkham Horror', not 'Resident Evil' (G-02-1)"
    - "Drag Rush About block has exactly one Beat Conductor mention — the attribution line — with the redundant bullet gone (G-02-2)"
    - "Floor 0 About block's 'no sleep for a week minimum' line ends with a period (G-02-5)"
    - "The overlay 'About this game' h3 is centered above its text in every project overlay (G-02-3)"
    - "The overlay 'Close' link renders white, not gray, in every project overlay (G-02-4)"
  artifacts:
    - src/views/GameProjects.vue
    - src/data/GameProjectsData.ts
    - src/css/projects.less
    - src/components/ProjectDetailsOverlay.vue
  key_links:
    - "Scoped '.dialog-content h3' rule in projects.less must out-specify App.vue's global 'h1..h5 { text-align: left }' (element-targeting rule) — element+class beats element alone (G-02-3)"
    - "Scoped 'a.dialog-close-button' color in ProjectDetailsOverlay.vue must out-specify projects.less's global '.dialog-content a { color: #696969 }' — Vue scoped attribute + class + element (0,2,1) beats class + element (0,1,1) (G-02-4)"
---

<objective>
Close the 5 UAT gaps from 02-UAT.md — three one-line copy fixes and two scoped-CSS overlay-polish fixes. All are already root-caused; this plan lands the exact edits.

Purpose: Convert the phase-2 UAT from 3 pass / 5 issue to a full pass so the personality-boundary read-through is genuinely signed off.
Output: Edits to 4 files (2 content, 2 style), each scoped to the diagnosed lines only.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-project-content-personality/02-UAT.md

# Isolation technique (STATE.md Decisions): these 4 files may carry Josef's
# separate uncommitted redesign work. Scope each commit to ONLY the gap-fix
# lines using the project's blob-isolation approach (git hash-object
# --no-filters on the fixed content + update-index --cacheinfo, or an
# equivalent hand-built minimal patch via git apply --cached), leaving his
# other uncommitted changes in these files untouched and unstaged.
@src/views/GameProjects.vue
@src/data/GameProjectsData.ts
@src/css/projects.less
@src/components/ProjectDetailsOverlay.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Three one-line copy fixes (G-02-1, G-02-2, G-02-5)</name>
  <files>src/views/GameProjects.vue, src/data/GameProjectsData.ts</files>
  <action>
    Three isolated text edits, no other wording changes:

    - G-02-1 — src/views/GameProjects.vue, hero `.intro` sentence (currently "I love Resident Evil, dogs and working out (almost equally)."): replace the single phrase "Resident Evil" with "Arkham Horror". Leave the rest of the sentence identical.

    - G-02-2 — src/data/GameProjectsData.ts, Drag Rush About block: delete the redundant standalone bullet line that reads `Custom made "Beat Conductor".<br/>`. Keep the attribution line ("I mainly built the vehicle animation trees and helped finalize the Beat Conductor.<br/>") and every other bullet in the block. The block already names the Beat Conductor once in the attribution line, so the standalone bullet is the duplicate to remove.

    - G-02-5 — src/data/GameProjectsData.ts, Floor 0 About block: append a period to the line "Guaranteed no sleep for a week minimum" so it reads "...for a week minimum." (it is the last bullet before the closing `</div>`; a `<br/>` is not required, only the missing full stop).

    Scope each commit to only these lines (see context isolation note) — do not stage Josef's other uncommitted edits in these files.
  </action>
  <verify>
    <automated>grep -c "Arkham Horror" src/views/GameProjects.vue   # expect 1</automated>
    <automated>grep -c "Beat Conductor" src/data/GameProjectsData.ts # expect 1 (attribution line only, redundant bullet removed)</automated>
    <automated>grep -c "for a week minimum\." src/data/GameProjectsData.ts # expect 1 (trailing period present)</automated>
  </verify>
  <done>Hero bio reads "Arkham Horror"; Drag Rush About has a single Beat Conductor mention (the attribution line); Floor 0's "...week minimum." line ends with a period.</done>
</task>

<task type="auto">
  <name>Task 2: Two scoped overlay-polish CSS fixes (G-02-3, G-02-4)</name>
  <files>src/css/projects.less, src/components/ProjectDetailsOverlay.vue</files>
  <action>
    Two scoped style additions — neither touches a global/shared rule:

    - G-02-3 — src/css/projects.less: add a rule `.dialog-content h3 { text-align: center; }` (place it within the existing `.dialog-content { ... }` block or immediately after it). This centers the "About this game" h3 in every overlay. Root cause: App.vue's global `h1, h2, h3, h4, h5 { text-align: left; }` targets the h3 element directly and wins over the inherited center from the `.paragraph.center` ancestor; `.dialog-content h3` (element + class) out-specifies the element-only global rule. Do NOT edit the App.vue global rule — it aligns headings sitewide (project titles, nav).

    - G-02-4 — src/components/ProjectDetailsOverlay.vue scoped `<style>` block: add `color: #ffffff;` to the existing `a.dialog-close-button { cursor: pointer; font-size: 1.4em; display: inline-block; margin: 0 auto; }` rule. This makes the "Close" link white in every overlay. Root cause: the link currently inherits gray (#696969) from projects.less's globally-loaded `.dialog-content a` rule; the Vue scoped selector `a.dialog-close-button` (attribute + class + element) out-specifies `.dialog-content a` (class + element), so the white wins. Do NOT touch the shared `.dialog-content a` rule (used by in-content project links).
  </action>
  <verify>
    <automated>grep -c "dialog-content h3" src/css/projects.less        # expect >= 1</automated>
    <automated>grep -c "#ffffff" src/components/ProjectDetailsOverlay.vue # expect >= 1 (close-button color added)</automated>
  </verify>
  <done>Overlay "About this game" heading is centered; overlay "Close" link is white — both in every project overlay, with no change to global App.vue headings or the shared `.dialog-content a` link color.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. All edits are static author-controlled copy and stylesheet rules; no user input, no data flow, no new dependencies, no package installs.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | scoped CSS specificity (G-02-3/G-02-4) | low | accept | Overrides are additive and scoped; the automated grep + re-UAT confirm intended rules apply without altering global App.vue / shared `.dialog-content a` rules. No injectable surface. |
</threat_model>

<verification>
- `grep -c "Arkham Horror" src/views/GameProjects.vue` returns 1
- `grep -c "Beat Conductor" src/data/GameProjectsData.ts` returns 1
- `grep -c "for a week minimum\." src/data/GameProjectsData.ts` returns 1
- `grep -c "dialog-content h3" src/css/projects.less` returns >= 1
- `grep -c "#ffffff" src/components/ProjectDetailsOverlay.vue` returns >= 1
- `npm run lint` passes (no new TS/Vue errors introduced)
</verification>

<success_criteria>
All 5 gaps (G-02-1 through G-02-5) closed: hero says "Arkham Horror"; the redundant Drag Rush Beat Conductor bullet is gone; Floor 0's closing bullet ends with a period; the overlay "About this game" heading is centered in every overlay; the overlay "Close" link is white in every overlay. No global App.vue heading rule and no shared `.dialog-content a` rule were modified. Josef's unrelated uncommitted edits in the 4 files remain unstaged.
</success_criteria>

<output>
Create `.planning/phases/02-project-content-personality/02-03-SUMMARY.md` when done
</output>
