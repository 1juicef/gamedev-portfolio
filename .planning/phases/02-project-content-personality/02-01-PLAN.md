---
phase: 02-project-content-personality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/GameProjectsData.ts
  - src/views/GameProjects.vue
autonomous: true
requirements: [CONT-02, CONT-03, CONT-04, CONT-05]
must_haves:
  truths:
    - "Drag Rush's About block states a team of 6 (3 programmers, 3 artists) and names what Josef personally built (vehicle animation trees + Beat Conductor) — CONT-02 / D-01"
    - "Dispater's About block states a team of 7 (3 programmers, 4 artists) and names what Josef personally built (dialogue/task/interaction systems + audio engineering) — CONT-02 / D-02"
    - "All 4 projects' About blocks are free of the known misspellings and each names engine + dev timeframe + at least one technical challenge — CONT-04 / D-06"
    - "Floor 0 shows a working 'Play on itch.io' link to juice-f.itch.io/floorzero using the same paragraph-center + target=_blank pattern as Drag Rush/Dispater — CONT-05 / D-07"
    - "The hero section still reads warm and personal ('Hello there!') while staying short and professional — CONT-03 / D-05"
  artifacts:
    - "src/data/GameProjectsData.ts (modified in place — attribution lines, corrected spellings, tightened Floor 0 line, Floor 0 itch.io block)"
    - "src/views/GameProjects.vue (modified in place — hero copy finalized/confirmed)"
  key_links:
    - "Floor 0 itch.io <a> uses class 'paragraph center' + target=_blank so it renders identically to the sibling itch.io links via the global src/css/projects.less styles (no new CSS class)"
    - "Attribution/blurb lines live inside each project's existing <div class=\"paragraph center\"> About block, rendered via v-html in ProjectDetailsOverlay — no new markup structure or component change"
---

## Phase Goal

**As a** recruiter or technical lead browsing the portfolio, **I want to** see accurate, warm, technically substantive project information without ever seeing code, **so that** personality comes through within a deliberate, professional boundary.

<objective>
Finalize all project copy so each of the 4 projects reads as accurate, warm, and technically substantive. This plan lands the team-attribution lines for Drag Rush and Dispater (CONT-02 / D-01, D-02, D-03), finalizes the "About this game" blurbs on all 4 projects by fixing the known spelling/wording issues and folding in the attribution facts (CONT-04 / D-06), adds Floor 0's "Play on itch.io" link (CONT-05 / D-07), and confirms/polishes the hero copy (CONT-03 / D-05).

Purpose: This is the content-and-personality substance of Phase 2 — it makes the project info accurate and warm so personality lands without any code being shown.
Output: In-place string edits to two existing files. No new files, components, symbols, or CSS classes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-project-content-personality/02-CONTEXT.md
@.planning/phases/02-project-content-personality/02-PATTERNS.md
@src/data/GameProjectsData.ts
@src/views/GameProjects.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Drag Rush + Dispater team attribution and fix their blurb typos</name>
  <files>src/data/GameProjectsData.ts</files>
  <read_first>
    - src/data/GameProjectsData.ts (the file being edited — Drag Rush block lines 4-36, Dispater block lines 37-65)
    - .planning/phases/02-project-content-personality/02-CONTEXT.md (D-01, D-02, D-03, D-06 — the exact attribution facts and the typo list)
    - .planning/phases/02-project-content-personality/02-PATTERNS.md ("About this game" bullet-line pattern — terse, <br/>-terminated single-sentence bullets)
  </read_first>
  <action>
    In `src/data/GameProjectsData.ts`, edit Drag Rush's and Dispater's existing "About this game" blocks only — the `<div class="paragraph center">` that begins with `<h3>About this game</h3>`. Keep the terse `<br/>`-terminated single-sentence bullet style and add attribution as new bullet lines inside the same div (per D-03 — no new heading, wrapper div, or separate credits section).

    Drag Rush (per D-01): directly after the `Built in Unity (8 weeks).` bullet, add a team-size bullet reading `A team of 6 — 3 programmers, 3 artists.` and a contribution bullet reading `I mainly built the vehicle animation trees and helped finalize the Beat Conductor.`. Also correct the run-together racing/rhythm misspelling flagged in D-06 (currently the second About bullet) so it reads `Unique twist between a racing- and rhythm game.`.

    Dispater (per D-02): directly after the `Made in Unreal Engine (8 weeks).` bullet, add a team-size bullet reading `A team of 7 — 3 programmers, 4 artists.` and a contribution bullet reading `I built the dialogue, task and interaction systems, and did the audio engineering.`. Also correct the misspelled atmospheric bullet flagged in D-06 so it reads `Atmospheric and creepy environment.`.

    Wording is Claude's draft per D-03 and Claude's Discretion (subject to Josef's UAT review) — keep each added line a single terse sentence matching the sibling bullets. Do not touch the youtube/screenshot/paragraph markup or the itch.io blocks in these two projects.
  </action>
  <verify>
    <automated>grep -Fq 'A team of 6 — 3 programmers, 3 artists.' src/data/GameProjectsData.ts && grep -Fq 'vehicle animation trees' src/data/GameProjectsData.ts && grep -Fq 'racing- and rhythm game' src/data/GameProjectsData.ts && grep -Fq 'A team of 7 — 3 programmers, 4 artists.' src/data/GameProjectsData.ts && grep -Fq 'audio engineering' src/data/GameProjectsData.ts && grep -Fq 'creepy environment.' src/data/GameProjectsData.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Fc 'A team of 6' src/data/GameProjectsData.ts` returns >= 1 and `grep -Fc 'A team of 7' src/data/GameProjectsData.ts` returns >= 1 (both attribution lines present — CONT-02)
    - `grep -Fq 'vehicle animation trees'` and `grep -Fq 'Beat Conductor'` both match (Josef's Drag Rush contribution named — D-01)
    - `grep -Fq 'dialogue, task and interaction systems'` and `grep -Fq 'audio engineering'` both match (Josef's Dispater contribution named — D-02)
    - `grep -Fq 'racing- and rhythm game'` matches AND the old run-together misspelling is gone:
      <!-- planner-discipline-allow: rythmgame -->
      `grep -Fc 'rythmgame' src/data/GameProjectsData.ts` returns 0 (D-06 typo fixed)
    - `grep -Fq 'creepy environment.'` matches AND the old misspelling is gone:
      <!-- planner-discipline-allow: enviroment -->
      `grep -Fc 'enviroment' src/data/GameProjectsData.ts` returns 0 (D-06 typo fixed)
    - No new `<h3>` and no new credits-style wrapper div were added — the attribution lines sit inside the pre-existing About `<div class="paragraph center">` (D-03)
  </acceptance_criteria>
  <done>Drag Rush and Dispater About blocks each carry a natural team-size + personal-contribution attribution line, both flagged blurb typos are corrected, and no new markup structure was introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize Floor 0 + SwingSpace blurbs and add Floor 0 itch.io link</name>
  <files>src/data/GameProjectsData.ts</files>
  <read_first>
    - src/data/GameProjectsData.ts (the file being edited — Floor 0 block lines 66-83, SwingSpace block lines 84-106; Drag Rush itch.io block lines 33-35 is the exact pattern to copy)
    - .planning/phases/02-project-content-personality/02-CONTEXT.md (D-06 typo/wording list, D-07 locked itch.io URL and required block)
    - .planning/phases/02-project-content-personality/02-PATTERNS.md ("Itch.io external link" shared pattern — same classes, same target=_blank, label always literally "Play on itch.io")
  </read_first>
  <action>
    In `src/data/GameProjectsData.ts`, edit only Floor 0's and SwingSpace's blocks (both solo projects — add NO attribution lines).

    Floor 0 (per D-06 + D-07): inside the existing About `<div class="paragraph center">`, replace the awkward final bullet flagged in D-06 with a single tightened sentence — use `Unsettling enough to keep you up at night.` as the concrete default (Claude's draft, subject to Josef's UAT review); keep the other Floor 0 bullets unchanged. Then, immediately after Floor 0's About `</div>` closes and before the closing template-literal backtick, add a new sibling block matching Drag Rush/Dispater's itch.io pattern exactly: a `<div class="paragraph center">` whose sole child is `<a href="https://juice-f.itch.io/floorzero" target="_blank">Play on itch.io</a>` (same `paragraph center` classes, same `target="_blank"`, label literally `Play on itch.io`, only the URL differs per D-07).

    SwingSpace (per D-06): inside its About block, correct the misspelled Firebase bullet so it reads `Firebase integration for global highscore chart.`. Leave all other SwingSpace bullets and the video/screenshot markup unchanged.
  </action>
  <verify>
    <automated>grep -Fq '<a href="https://juice-f.itch.io/floorzero" target="_blank">Play on itch.io</a>' src/data/GameProjectsData.ts && grep -Fq 'Unsettling enough to keep you up at night.' src/data/GameProjectsData.ts && grep -Fq 'Firebase integration for global highscore chart.' src/data/GameProjectsData.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Fq 'https://juice-f.itch.io/floorzero'` matches and it is wrapped in `<a ... target="_blank">Play on itch.io</a>` inside a `<div class="paragraph center">` (CONT-05 / D-07 — identical pattern to sibling links)
    - `grep -Fc 'Play on itch.io' src/data/GameProjectsData.ts` returns >= 3 (Drag Rush + Dispater + new Floor 0 link)
    - `grep -Fq 'Firebase integration for global highscore chart.'` matches AND the old misspelling is gone:
      <!-- planner-discipline-allow: intergration -->
      `grep -Fc 'intergration' src/data/GameProjectsData.ts` returns 0 (D-06 typo fixed)
    - Floor 0's awkward final line is replaced by a single clean sentence AND the old phrasing is gone:
      <!-- planner-discipline-allow: sleep less for days -->
      `grep -Fc 'sleep less for days' src/data/GameProjectsData.ts` returns 0 (D-06 wording tightened)
    - No attribution/team line was added to Floor 0 or SwingSpace (both solo)
  </acceptance_criteria>
  <done>Floor 0 has a working itch.io link matching the sibling pattern and a tightened final blurb line; SwingSpace's Firebase typo is fixed; no attribution added to either solo project.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm/finalize warm hero copy</name>
  <files>src/views/GameProjects.vue</files>
  <read_first>
    - src/views/GameProjects.vue (the file being edited — `.hero-copy` block lines 4-16, `.hero`/`.hero-copy`/`.hero-photo` styles lines 125-148 and 225-231)
    - .planning/phases/02-project-content-personality/02-CONTEXT.md (D-05 — hero already reads warm/personal, treat as strong existing draft; polish only)
    - .planning/phases/02-project-content-personality/02-PATTERNS.md (`.hero-copy` analog — copy-only edits in place, no structural change)
  </read_first>
  <action>
    In `src/views/GameProjects.vue`, audit the existing `.hero-copy` block (lines 4-16) against CONT-03's bar: warm + personal, yet short + professional. Per D-05 the current draft (`Hello there!`, the Gothenburg game-dev-student bio, the "projects I have made solo or in team" framing) already reads warm and is treated as a strong existing draft. Preserve its structure — do NOT add wrapper `<div>`s and do NOT change the `.hero` / `.hero-copy` / `.hero-photo` layout or styles. Make only targeted micro-edits if a concrete wording issue is found (e.g. a grammar slip); if none is found, leave the copy as-is and record in the summary that it meets the bar. Do not lengthen the copy. Any wording change is subject to Josef's UAT review.
  </action>
  <verify>
    <automated>grep -Fq 'Hello there!' src/views/GameProjects.vue && grep -Fq 'Game Developer student residing in Gothenburg' src/views/GameProjects.vue && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Fq 'Hello there!' src/views/GameProjects.vue` matches (warm greeting preserved — CONT-03)
    - `grep -c 'class="intro"' src/views/GameProjects.vue` returns >= 3 (the three intro paragraphs preserved — no structural change per D-05)
    - No new `<div>` wrapper was added inside `.hero-copy` and the `.hero`/`.hero-copy`/`.hero-photo` style rules are unchanged
    - Summary records the tone-bar decision (kept as-is, or the specific micro-edit made and why)
  </acceptance_criteria>
  <done>Hero copy is confirmed warm + personal + short + professional; structure and styles are unchanged; any wording tweak is minimal and recorded for Josef's review.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

This plan creates NO new files, components, exported symbols, or CSS classes. It performs in-place string edits to two pre-existing files:

- `src/data/GameProjectsData.ts` — attribution bullet lines (Drag Rush, Dispater), corrected spellings (all affected projects), tightened Floor 0 final line, and one new Floor 0 itch.io `<div class="paragraph center">` block reusing the existing global styles.
- `src/views/GameProjects.vue` — hero copy confirmed/finalized in place (likely unchanged per D-05).

No new symbols means downstream plans and the read-through checkpoint (02-02) reference the same two files by path, not by any new export.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author content → rendered DOM | Author-authored HTML strings in `GameProjectsData.ts` are rendered via `v-html` in `ProjectDetailsOverlay` (existing pattern for all 4 blocks) |
| site → external site (itch.io) | New outbound `target="_blank"` link from the Floor 0 overlay to the third-party itch.io page |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering (XSS via v-html) | `GameProjectsData.ts` `htmlDescription` rendered with `v-html` | low | accept | Content is author-controlled static string literals — no user input crosses the boundary. Reuses the identical v-html pattern already shipping for all 4 existing blocks; no new dynamic interpolation is introduced. |
| T-02-02 | Tampering (reverse tabnabbing) | Floor 0 new `<a target="_blank">` without `rel="noopener"` | low | accept | Locked to match the existing Drag Rush/Dispater link pattern exactly per D-07 (consistency requirement). Modern browsers imply `noopener` for `target=_blank` since 2021. Changing `rel` handling across all itch.io links is out of scope this phase (candidate for POLISH-01/Phase 3). |

**Supply chain:** No package-manager installs occur in this phase — the supply-chain checkpoint (T-{phase}-SC) is not applicable. No RESEARCH.md / Package Legitimacy Audit is required.
</threat_model>

<verification>
- All Task 1-3 `<automated>` checks print `PASS`.
- `npm run lint` passes over the two edited files (no new lint errors introduced).
- `npm run build` succeeds (template literals and the `.vue` template still compile).
</verification>

<success_criteria>
- CONT-02: Drag Rush and Dispater blurbs each state team size + what Josef personally built (grep-confirmed).
- CONT-04: All 4 projects' About blocks name engine + dev timeframe + ≥1 technical challenge and are free of the D-06 typos.
- CONT-05: Floor 0 has a `Play on itch.io` link to `https://juice-f.itch.io/floorzero` matching the sibling pattern.
- CONT-03: Hero reads warm + personal while staying short + professional; structure unchanged.
</success_criteria>

<output>
Create `.planning/phases/02-project-content-personality/02-01-SUMMARY.md` when done.
</output>
